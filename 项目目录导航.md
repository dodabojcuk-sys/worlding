# 天衍项目目录导航

> 本文档是工程定位地图，不是产品需求、架构新方案或工作报告。它只回答：内容应放哪里、代码负责什么、修改会影响哪里、需要做什么验证。

## 1. 使用顺序

1. 涉及产品、功能、体验、信息架构或故事语义时，先读 `TIANYAN_PRODUCT_CORE.md`。
2. 准备新增、移动或拆分代码时，再读本文档定位唯一责任区。
3. 修改前使用 `rg` 查找导入者、持久化所有者和对应测试，不凭文件名猜影响范围。
4. 只有当目录责任、入口、状态所有者或验证方式发生变化时，才更新本文档。

## 2. 内容分区

| 内容 | 固定位置 | 放置规则 |
| --- | --- | --- |
| 产品核心 | `TIANYAN_PRODUCT_CORE.md` | 唯一产品定义，不用工程现状覆盖 |
| 工程规则 | `CORE.md` | 规定目录、资料、Git 和日志边界 |
| Agent 执行规则 | `AGENTS.md` | 给未来 Agent 和开发者的强制工程约束 |
| 项目导航 | `项目目录导航.md` | 根目录唯一代码定位文档 |
| 产品代码 | `apps/`、`src/` | 不在根目录、`data/` 或 `docs/` 中放业务实现 |
| 测试 | `tests/`、`apps/story-studio/scripts/` | 单元/集成测试在 `tests/`；少量浏览器闭环在产品脚本目录 |
| 工程脚本 | `scripts/`、`bin/` | `scripts/` 用于启动与验证；`bin/` 只保留隔离的兼容 CLI |
| 代码依赖的技术契约 | `docs/` | 只保留被代码、lint 或测试直接读取的文件 |
| 日常非代码工作产物 | `data/YYYY-MM-DD_任务名称/` | 以 `CORE.md` 的固定结构保存工作日志、报告、截图、验证证据和少量任务附件；不存代码副本、受保护项目数据或原始资料库 |
| 历史资料和压缩包 | 工作区之外 | 不导入仓库，只在必要时读取 |

## 3. 主运行链

```text
apps/story-studio/src/main.tsx
  → App.tsx / product-shell / components
  → src/lib/localTransport.ts
  → apps/story-studio/server/server.mjs（显式 `api-only` / `combined-static` 运行模式）
  → src/storyControlSurface/*
  → src/storyWorkspace | storyContinuity | storyIntelligence | storyCreation
  → 项目本地状态、Markdown、版本、回执和输出产物
```

Agent 运行链：

```text
天意/女娲 UI
  → 本地传输与服务器路由
  → src/storyAgent/tianyiAgentRuntimePort.ts
  → src/storyAgent/agentRuntimePlugin.ts（稳定宿主 ABI）
  → src/storyAgent/agentRuntimePluginRegistry.ts（内置白名单选择与显式回退）
  → src/storyAgent/plugins/builtinPiAgentRuntimePlugin.ts（仅 Pi 回合、流与取消及 SDK 适配）
  → apps/story-studio/server/providerGateway/aiProviderGateway.mjs（唯一模型 Broker；标准化原生工具帧）
  → src/storyAgent/tianyiProductTools.ts（只声明产品拥有的受控工具）
  → src/storyWorkspace/workspacePathPolicy.ts（Workspace owner 的受限逻辑路径策略，不归 Pi 所有）
  → 受权工具
  → 候选/回执
  → 作者确认
  → 唯一 Canon/Event 写入链
```

Pi Agent 是 Agent 执行底座，不是 Canon、WorldState、Event、Session 或作者确认权的所有者。`AgentRuntimePort` 属于产品层；Pi 适配器只通过既有 Provider Gateway 接收流，并将耐久化事件交回既有 Tianyi Session/Archive。

## 4. 代码目录职责

### `apps/story-studio/`：唯一产品入口

| 路径 | 职责 | 修改的常见影响 |
| --- | --- | --- |
| `src/main.tsx` | React 启动、全局样式入口 | 全应用渲染和样式顺序 |
| `src/App.tsx` | R0 顶层启动组装 | 只选择独立设置 utility 路由或唯一 R0 产品外壳，不承载领域流程 |
| `src/product-shell/` | 全局导航、顶栏、工程目录、页面工具 Dock、右侧工作面、个人中心与 Shell 状态 | 八空间导航、路由与响应式布局；`TianyanR0Shell.tsx` 只组合区域，`WorkspaceDockCoordinator.ts` 是 `NONE / EVENT_DETAILS / EVENT_CREATE / RELATION_REVIEW / TIANYI` 五态右侧工作面的唯一瞬时状态 owner；`workspace/AccountCenterWorkspace.tsx` 只表达无后端账号能力前的个人信息占位，不拥有身份、用户名、充值、订阅或账户写入；`runtime/TianyanShellRuntime.tsx` 只读取既有 bootstrap/版本/天意投影并注入依赖，`runtime/tianyiShellSessionRecovery.ts` 只保存项目范围的浏览器恢复提示，不成为 Session owner；`global-search/` 只读聚合现有路由、世界对象、ObjectCatalog 元数据与来源审查投影，不建立索引、事实或导航状态 owner；工程目录与 Dock 分别位于 `project-directory/`、`right-dock/` |
| `src/product-shell/project-directory/` | “已分类 / 待确认”目录树、角色对象目录、角色检查器、稳定 URL 的 WorldObject 编辑覆盖层和目录 ViewModel | 只拥有导航/组织/引用 UI；待确认只编排既有 Source Import、Candidate Review、Agent Recognition 与 Agent Type Catalog 作者操作，角色编辑只调用既有 WorldObject/Card Presentation owner，不拥有 Canon、Event、WorldState、角色实体或文件系统树。目录密度和排序为浏览器偏好，角色层级仍为 WorldObject `subtype`，分类定义/分配分别读取 workspace layout 与 ObjectCatalog。 |
| `src/product-shell/right-dock/` | 页面工具轨、多面板栈、尺寸边界和工具栈瞬时布局 | 不拥有天意或右侧工作面仲裁状态，不拥有工具业务状态，不做布局持久化或跨侧拖动 |
| `src/components/` | 当前事件线工作面、页面工具与天意侧栏 UI | 只保留由 `main.tsx` 可达的 R0.3 表现组件；不应成为新的持久化所有者，也不保留未挂载的旧工作台副本 |
| `src/components/page-tools/` | 工程日志、专家分析与页面工具展示注册表 | 只读回执投影和建议回调；不成为业务日志、Canon 或 Event owner |
| `src/components/tianyi/` | 天意统一会话大页面、侧栏 Work 表面、输入区控制、能力入口与轻量 Page-scoped Agent Run 投影 | `workspace/` 表达同一 TianyiConversation 的 Creative / Work 两条泳道；`sidebar/` 的“工作”接入同一 Work lane，仅支持专属能力的页面显示 Agent；`composer/`、`capability-launcher/` 只表达 UI。会话、记忆、Agent、版本与 Provider 仍由既有领域层拥有，模型入口为只读配置投影 |
| `src/components/event-observation/` | 正式事件线任务外壳、NarrativeArrangement 图形投影、独立世界时间投影、证据审计与兼容观察视图 | `StoryProgressionWorkspace.tsx` 只组合任务、焦点、staging 与 Writer；`EventGraphCanvas.tsx` 从 NarrativeArrangementProjection 绘制 Placement 主线/分支/合流；`TemporalCanvas.tsx` 从 Event 时间证据绘制连续时间坐标且不依赖 Arrangement；`ParticipationObservation.tsx` 保留兼容的只读证据视图。该目录不拥有 Event、Canon、WorldState、Relation、叙事顺序或对象写入 |
| `src/lib/` | 前端传输、本地存储适配、图谱/时间投影和 UI 辅助 | 前后端合同、错误处理和大量页面调用 |
| `src/styles/` | 当前 R0 外壳、事件线、工程目录、Dock 与天意样式 | 只保留由 `main.tsx` 导入的样式；桌面需检查 1920、1440、1280 和 1152；窄侧边栏浏览器只保证主工作区默认可见、目录为可关闭抽屉且不产生横向溢出，移动端完整产品形态仍独立暂缓 |
| `src/settings/` | 设置主工作区及直接访问 fallback，包含存储、导入导出、Provider、Pi 状态与默认权限区段 | 全局导航选择“设置”时复用现有 Shell 主工作区，不打开 modal 或跳离作品；只消费既有 LocalFolderProvider、模型服务与 Agent 权限 port，不拥有项目、凭据、Agent Run 或文件系统状态 |
| `server/server.mjs` 与 `server/runtimeMode.mjs` | 本地 HTTP 入口、路由、依赖组装与显式运行模式 | `api-only` 仅 API；`combined-static` 才托管 `dist` 与 SPA fallback。所有前后端合同、安全边界和持久化调用；高风险 |
| `server/providerGateway/` | Provider 凭据、预算、请求、重放安全和黄金闭环适配；`providerCatalog.mjs` 定义 protocol/preset/instance 与目录状态，`persistentProviderProfileStore.mjs` 是非敏感 Provider 实例、目录和 probe 投影的唯一持久化 owner | 真实外部调用边界；测试必须使用 Mock 或本地伪服务器；只有作者显式获取、连接测试或 Embedding 验证才允许请求 Provider |
| `server/*Fixture*.mjs` | 当前尚在服务端的过渡 fixture | 当前例外，禁止照此新增生产功能 |
| `scripts/` | Playwright 浏览器闭环和进程清理 | 验收流程、截图和本地测试进程 |

### `src/`：业务、权威与持久化内核

| 路径 | 唯一责任 | 修改的常见影响 |
| --- | --- | --- |
| `storyContracts/` | 前后端共享的故事对象、位置、事件引用、工作区注册契约 | UI、服务器、持久化和迁移；通常需要全量验证 |
| `storyControlSurface/` | 用例编排、作者权限、候选审查、Canon 写入和工作区操作 | 故事正式事实与所有主流程；最高风险 |
| `storyWorkspace/` | 项目文件、文档、关系、卡片、布局、作品版本、受限路径策略和 `.tianyan` 备份仓储 | 磁盘数据、版本、恢复和数据兼容性；导出包含耐久 Session/Archive/回执并排除 cache、lock、run、凭据和绝对路径 |
| `storyWorkspace/objectCatalog.ts` | 项目/工作版本隔离的对象分类与回收站元数据 | 不保存对象字段、标签、归档状态、关系、事件、Canon、来源或记忆 |
| `storyContinuity/` | 天意会话、上下文回执、记忆、停止点、授权和有据回答；`embeddingIndexBinding.ts` 只定义数据集索引 generation 的 Embedding 身份、兼容判定与重建门禁 | 上下文正确性、角色知识边界、恢复与长会话；本轮不建立向量库或完整 RAG |
| `storyAgent/` | 产品 AgentRuntimePort、受控产品工具、能力注册与可替换 Pi 执行适配器 | 天意计划/工具审批、Pi 回合/流/取消；产物/候选调用既有 owner，Provider 与 Session 仍由既有 owner 持有 |
| `storyIntelligence/` | 女娲计划、运行、模拟、候选综合、故事快照和 Agent 识别提案 | 派生内容、模拟状态、候选生成与天意衔接 |
| `storyCreation/` | 创作文档模型、中立故事包、派生交付、格式适配和插件生命周期 | 外部输出、插件安全、来源绑定和产物版本 |
| `storyCardPresentation/` | 卡片模板、预设、表现投影和历史呈现 | 资料卡 UI；不应取代故事事实所有者 |
| `productWorkspace/` | 产品工作区模型与选择 | 工作区身份、选中状态和前端导航衔接 |
| `productWorkspaceRuntime/` | 工作区运行视图投影 | 运行时显示，不应成为第二状态所有者 |
| `skillControl/` | Skill 清单、策略、预算、开关和 Recipe 草案 | Agent 可见能力、授权与预算 |
| `skillRuntime/` | Skill 加载、执行和沙箱 | 工具执行安全和 Agent 运行路径 |
| `memorySkills/` | 外部记忆 Skill 合同和适配 | 记忆读取边界和 Skill 集成 |
| `domainTemplates/storyWorld/` | 确定性故事世界流程模板：证据、意图、决策、预览、提交、模拟与写作 | 基础决策/提交流程与对应模板测试 |

`src/nuwaSceneRuntimeContracts.ts` 和 `src/storyProductPrototypeState.ts` 是目前直接放在 `src/` 根级的历史/过渡文件。新代码不应继续模仿这种放置；它们只能在对应测试保护下归入既有责任区。

### 其他工程目录

| 路径 | 职责 | 注意事项 |
| --- | --- | --- |
| `tests/` | 按 `src/` 责任区对应的单元与集成测试 | 改业务责任时就近修改对应测试，不建第二测试树 |
| `scripts/` | 开发启动、脚本选择、仓库检查、功能索引验证、验收 Node 22/npm 10 门禁与隔离 E2E fixture | 默认验证以 `package.json` 十条脚本为准；`dev` 的唯一验收 UI 是 Vite 4191，`serve` 是 combined-static 的单端口 4192；错误工具链必须在启动前停止，E2E 只清理带本次所有权标记的临时根 |
| `bin/world-os-story.mjs` | 隔离兼容 CLI | 不得成为第二产品入口或第二持久化根 |
| `docs/architecture/FEATURE_INDEX.json` | lint 强制的功能入口、所有者、成熟度和测试索引 | 更改功能所有者或入口时必须同步 |
| `docs/product/TIANYAN_NUWA_RUN_AND_MULTIVERSE_DERIVED_VERSION_BOUNDARY_R0.md` | 女娲/多元派生边界测试输入 | 不是第二产品核心 |
| `docs/research/TIANYAN_MAINLINE_LINEAGE_AND_CAPABILITY_REALITY_MAP_R0.json` | 来源漂移对账测试输入 | 不是当前工程现状报告 |
| `data/` | 按日期和任务索引的非代码工作产物 | 结构、命名与保留范围以 `CORE.md` 为准；不放业务代码、原始资料、数据库、迁移、环境文件或密钥 |
| `data/2026-09-03_天衍R12B1统一工程基线/` | Runtime、Provider 与 R11.1 统一候选的谱系、冲突审查、命令响应和隔离截图证据 | 仅用于 R12-B1 Founder 审核；不属于产品运行路径，也不表示 R12-B2 已开始 |
| `data/2026-09-03_天衍R12B2_1叙事编排权威合同/` | 正式 NarrativeArrangement 合同、Owner/Writer 审计、旧项目兼容与实现测试报告 | 只表示 R12-B2.1 权威切片待 Founder 审核；生产事件线 UI 与 R12-B3 未开始 |

## 5. 不可重复的所有者

以 `docs/architecture/FEATURE_INDEX.json` 的可验证索引为当前工程依据：

| 责任 | 当前唯一所有者 | 禁止行为 |
| --- | --- | --- |
| Canon 写入 | `src/storyControlSurface/storyStudioAuthorControl.ts` | UI、Pi Agent、Provider、女娲或插件绕过作者审查直接写入 |
| WorldState / Event 事实 | `src/storyControlSurface/storyStudioWorkspaceOperations.ts` | 新建平行事件库、时间线事实库或页面本地事实 |
| 候选审查 | `src/storyControlSurface/storyStudioAuthorControl.ts` | 把 AI 结果直接当成已确认内容 |
| Agent 识别提案 | `src/storyIntelligence/agentRecognitionProposalRepository.ts` | 在组件或 Provider 内再维护一份提案状态 |
| Provider 边界 | `apps/story-studio/server/providerGateway/aiProviderGateway.mjs` | UI 直连 Provider，或在测试中使用真实凭据 |
| Provider 实例与模型目录 | `apps/story-studio/server/providerGateway/persistentProviderProfileStore.mjs` | 将 preset 建议伪装成 endpoint 结果，或用厂商名代替稳定 `providerInstanceId` |
| Embedding 索引绑定合同 | `src/storyContinuity/embeddingIndexBinding.ts` | 仅比较模型名、改写旧数据集绑定，或在检索中混用不兼容向量 |
| 作品版本权威 | `src/storyWorkspace/workVersionAuthority.ts` | 用临时 UI 状态替代版本、来源和修订身份 |
| 正式叙事编排 | `src/storyControlSurface/storyStudioWorkspaceOperations.ts` 中 Story Unit Writer + `src/storyContracts/narrativeArrangement.ts` | Event 全局位置、`linkedEntityIds` 数组顺序、UI/localStorage 或独立数据库成为排序事实 |

## 6. 改动影响与最低验证

| 要改的内容 | 必查范围 | 最低验证 |
| --- | --- | --- |
| 八空间、顶级导航、深链接 | 产品核心、`tianyanR0ShellContract.ts`、`TianyanR0Shell.tsx`、`navigation/`、`App.tsx` | `typecheck`、`lint`、工作区注册/导航测试、`build`、`test:e2e` |
| 工程目录、页面工具 Dock 或天意输入区 | `product-shell/project-directory/`、`product-shell/right-dock/`、`components/page-tools/`、`components/tianyi/{workspace,sidebar,composer,capability-launcher}/` | `typecheck`、Shell/R0.2 测试、`build`、`test:e2e`，再人工检查 1920/1440/1280/1152 桌面视口；涉及窄侧栏时另查 242×771 的默认主区、目录开关、关闭按钮和横向溢出 |
| Canon、Event、WorldState 写入 | `storyStudioAuthorControl.ts`、`storyStudioWorkspaceOperations.ts`、仓储与投影 | 对应 ControlSurface/Workspace 测试加 `verify` |
| 事件布局、任务预设、观察镜头、参与轨迹或视图持久化 | `src/storyContracts/eventObservation.ts`、`components/EventLineWorkbench.tsx`、`components/event-observation/StoryProgressionWorkspace.tsx`、`styles/event-line-projection.css`、`FEATURE_INDEX.json` | `eventObservation` / `eventPerspectiveProjection` 单元测试、R11/R12 UI 边界集成测试、Node 22 `test:e2e`、`build` 与 `verify`；人工核对 1440/1280/1152/743、键盘和 200% 缩放，确认零隐式 Provider |
| 正式叙事位置、Story Unit 内顺序或跨 Unit 编排 | `src/storyContracts/narrativeArrangement.ts`、`storyStudioWorkspaceOperations.ts`、`components/event-observation/R0EventLineProjection.tsx`、`components/event-observation/StoryProgressionWorkspace.tsx`、Story Unit Markdown、`FEATURE_INDEX.json` | NarrativeArrangement 纯函数、Owner/兼容/portable/API/生产 UI 集成、`typecheck`、`lint`、`build`、`test:e2e`、`verify`；禁止 Event 全局位置、UI/localStorage 排序与 legacy fallback |
| Pi Agent、天意或 Provider | `storyAgent/`、`storyContinuity/`、`providerGateway/`、`localTransport.ts`、天意 UI | Agent、Continuity、Transport、Provider 集成测试；禁止真 Provider |
| Provider 目录、Embedding 验证或索引绑定 | `providerCatalog.mjs`、protocol adapters、`persistentProviderProfileStore.mjs`、`embeddingIndexBinding.ts`、设置 UI | 目录迁移/状态机、本地伪 Provider 集成、索引指纹兼容测试、`test:e2e`、`verify`；不得记录密钥或向量正文 |
| 磁盘格式、版本、备份或恢复 | `storyWorkspace/`、`storyContinuity/`、`src/settings/`、迁移与恢复 UI | 必须增加重启与空作品库 `.tianyan` 往返、冲突、traversal、symlink、半文件和秘密排除测试，使用临时项目，不破坏真实用户数据 |
| 女娲模拟 | `storyIntelligence/`、`nuwaSceneRuntimeContracts.ts`、女娲 UI、Candidate Review | Intelligence、AuthorControl、Nuwa UI 测试和黄金闭环 |
| 创作、故事包或插件 | `storyCreation/`、创作 UI、服务端插件边界 | Creation 测试、插件权限/回滚测试、来源绑定测试 |
| UI Shell、侧栏、顶栏或全局样式 | `product-shell/`、`App.tsx`、所有全局 CSS | `typecheck`、`build`、Shell 测试、`test:e2e`，再人工检查 1920/1440/1280/1152 桌面视口；改动窄视口覆盖层时补查 242×771 |
| 功能入口、所有者或成熟度 | 生产代码与 `FEATURE_INDEX.json` | `npm run lint` |

`verify` 是目标全量门槛，但不能取代创始人体验验收。当前工程锁定 Node 22，不应在其他主版本下宣称全量验证通过。

## 7. 常见任务从哪里开始

| 任务 | 首先查看 |
| --- | --- |
| 修改顶级空间和导航 | `src/storyContracts/storyStudioWorkspaceRegistry.ts`、`apps/story-studio/src/product-shell/navigation/` |
| 拆分顶层 UI | `apps/story-studio/src/App.tsx`、`apps/story-studio/src/product-shell/` |
| 修改页面与服务器通信 | `apps/story-studio/src/lib/localTransport.ts`、`apps/story-studio/server/server.mjs` |
| 收敛 Pi Agent 底座 | `src/storyAgent/tianyiAgentRuntimePort.ts`（产品 Port）、`src/storyAgent/agentRuntimePlugin.ts` 与 `agentRuntimePluginRegistry.ts`（宿主 ABI/白名单）、`src/storyAgent/plugins/builtinPiAgentRuntimePlugin.ts`（Pi 基础设施）、`src/storyAgent/contextualCapabilityRegistry.ts`、`server/providerGateway/` |
| 修改作者确认和正式写入 | `src/storyControlSurface/storyStudioAuthorControl.ts` |
| 修改事件、世界状态或项目生命周期 | `src/storyControlSurface/storyStudioWorkspaceOperations.ts` |
| 修改正式叙事编排或 Placement | `src/storyContracts/narrativeArrangement.ts`、`src/storyControlSurface/storyStudioWorkspaceOperations.ts`、`apps/story-studio/src/components/event-observation/{R0EventLineProjection,StoryProgressionWorkspace,EventGraphCanvas}.tsx`；先读 `TIANYAN_PRODUCT_CORE.md` 和 R12-B2.1 合同 |
| 修改存储、关系、卡片、版本或备份 | `src/storyWorkspace/` |
| 修改天意上下文、记忆和恢复 | `src/storyContinuity/`、`apps/story-studio/src/components/tianyi/` |
| 修改女娲运行与推演 | `src/storyIntelligence/`、`apps/story-studio/src/components/NuwaPrimaryWorkspace.tsx` |
| 修改创作输出和插件 | `src/storyCreation/`、`apps/story-studio/src/components/Creation*` |
| 修改资料卡片显示 | `src/storyCardPresentation/`、`apps/story-studio/src/components/CardWorkbench.tsx` |

## 8. 当前已知的结构例外

这些是整改对象，不是新代码的参考格式：

- `App.tsx`、`server.mjs`、`localTransport.ts` 和部分 ControlSurface 文件过大。
- R0 已将“数据”确认为第八空间；它只能展示既有 owner 的只读投影，不能成为事实或会话 owner。
- 生产路径仍有 fixture、legacy 和 dev prototype。
- `src/` 根级仍有两个未归类的过渡文件。
- `scripts/verify-story-studio-canonical.mjs` 仍绑定导入前的分支名和 Git 基线；当前仓库已重建 Git 基线，`npm run dev` 的预检需单独修复，不应直接删除安全预检。

## 9. 新增代码的判定规则

1. 先找唯一所有者；有所有者就在其责任区扩展。
2. 只有现有目录无法表达新的唯一责任时，才创建新目录。
3. 不以时间、人名、Agent 名、模型名、“新版”或“重构版”命名代码目录。
4. 不建立第二套 `app`、`core`、`world`、`agentRuntime`、`persistence` 或 `ui` 大目录。
5. 移动代码必须在同一次变更中更新导入、测试、功能索引和本文档，不留兼容副本。
