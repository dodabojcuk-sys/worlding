# 天衍 R5 M3 同一故事连续交互证据

- 代码来源：`616838f37ef4891a52d978ca9a50ddc401db772b`。
- 隔离故事：`长夜将明 R12 事件线主作品`，故事单元 `雾港追踪`；角色 `阿芜`、`林昭`。
- 运行方式：本地工程演练（测试环境显式开启 fake provider），Provider 调用数为 `0`；没有调用真实 Provider，也没有写入 Canon、WorldState 或正式事件。

## 连续过程

1. 选择两个正式角色与同一故事单元，检查各自角色可知范围与排除项。
2. 启动 Run，连续执行两步，验证发言者按角色交替，且后一步只能读取 Run 内已提交的可见结果。
3. 发送作者提示后暂停，刷新页面，验证 Run 仍为暂停态且未重新 dispatch。
4. 恢复、执行一步、停止，并把所选结果送入中央待确认；候选未被自动采纳为正式故事。
5. 切换项目后检查 Run、角色上下文与候选均不跨项目泄漏。

## 附件

- `01-1440-context-boundaries.png`：两个角色的上下文边界。
- `02-1440-two-role-steps.png`：同一 Run 内的两位角色连续步骤。
- `03-1195-paused-refresh.png`：暂停后刷新仍保持暂停。
- `04-1195-stopped-candidate.png`：停止并送入待确认。
- `page@f49ff21b917fa6c1d50d84205669735c.webm`：上述过程的连续录屏。

该证据是本地自动化交互验证，不构成真实 Provider 验证或创始人体验验收。
