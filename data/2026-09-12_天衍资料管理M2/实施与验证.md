# 天衍资料管理 M2 实施与验证

## 作者实际可完成的操作

普通作者入口为 `/library?materialMode=files`。隔离作品 `r061-e2e-73a7b118720f4ad48b2cab7cf1ed0082` 已通过正常界面完成：

1. 一次导入 Markdown、PNG、PDF、WAV 和 DOCX；5 个文件均保持普通文件身份，没有新增物品。
2. 归档并恢复 PDF；新建“雾港专题”，多选 Markdown 与 PNG 后移动到该文件夹。
3. 打开 PNG，明确选择既有“地点示意图”，由现有地图 Owner 建立真实底图修订；原文件身份和原始字节不变。
4. 打开 Markdown，选中“北闸宵禁后只允许持证守卫通行。”，交给本地确定性假 Provider；回执中只有该选段，没有相邻的未选正文。
5. 回答完成后把文件更新为新修订，再从旧回答返回；页面仍打开旧 SHA-256 `b1e8d6b141313cc58ffbd1bec63165d00585e424cc139269008178d2f4ec141e` 的字符范围 14–29。

身份映射见 `world-materials-identity.json`。与该映射同次生成的最新完整录像为 `page@4c4d1ad416553253a98e535e397007f1.webm`（9.72 秒）。三个 524288 字节、无可解析时长的 WebM 是失败运行中断时留下的原始记录；未删除或冒充通过证据。`page@3b485847e431df57b63b13e6fae7f234.webm`（9.44 秒）和 `page@c4a5b8bb4c96165ff3602d027d546f46.webm`（9.92 秒）是较早的通过记录。

## 格式能力矩阵

| 格式 | 保存/下载 | 页面预览 | 正文提取/检索 | 页面编辑 | 天意文本引用 |
| --- | --- | --- | --- | --- | --- |
| TXT / Markdown | 是 | 是 | 是，严格 UTF-8 | 是，新修订 | 是，全文或明确选段 |
| PNG / JPEG / WebP | 是 | 是 | 否 | 否；可明确复制为地图底图修订 | 否 |
| PDF | 是 | 浏览器原生预览 | 否 | 否 | 否 |
| 浏览器支持的音频/视频 | 是 | 浏览器原生播放 | 否 | 否 | 否 |
| Office / 压缩包 / 其他附件 | 是 | 否 | 否 | 否 | 否 |

未实现范围：PDF 文本层提取、OCR、Office 解析、音视频转码/转写、网页抓取和自动合并重复内容。允许上传不代表模型能够理解；非文本文件不会进入 Grounded Context Gate。

## 验证边界

- 定向：22 项通过，覆盖稳定身份、不可变修订、同名异内容、重复字节、部分失败、请求重放、目录循环/过期写保护、真实字节下载、140 条分页与正文搜索、HTTP 鉴权、portable export/import，以及旧文件精确引用。
- 浏览器：`TIANYAN_E2E_SCOPE=world-materials-m1` 通过；隔离作品、本地假 Provider，真实 Provider 0 次。
- 完整门禁：`npm run verify` 通过；typecheck、lint、单元 1202 项、集成 57 项、仓库全量 E2E 和生产 build 均成功。仅保留 Vite 既有的大 chunk 警告。
- 失败记录：一次使用不存在的 `tsx` 加载器，测试在装载前失败，改回仓库规定的 `--experimental-strip-types`；一次新断言错误地去掉了 `.png`，实际地图修订已写入，断言按真实标题与资源路径修正后通过。
- 尚不能据此声称真实模型理解能力或作者验收通过。

## 代表证据

- `09-m2-mixed-files.png`：混合格式导入。
- `09a-m2-organized-and-map-linked.png`：专题整理后的图片预览与地图底图关联。
- `10-m2-tianyi-selection-preview.png`：发送前明确选段。
- `11-m2-file-answer-receipt.png`：假 Provider 回答及实际采用正文。
- `12-m2-old-file-selection-return.png`：文件修改后返回旧修订与原字符范围。
