# MAP-M4 隔离作者流程记录

## TIANYAN_MAP_R2_CLOSEOUT 计划（2026-09-13 13:35 +08:00）

- 现场：继续干净的 `codex/map-m4-closeout`，本地／远端／PR #24 head 均为 `eae4e322472fd7b523d04873a4b9c3581934008b`，两项远端 `verify` 成功。保留 R2 地图集、范围、A/B 选择、室内绘制与全部 M4 保护链，不新增 Owner 或算法。
- 对齐：在现有相似变换和视口内增加局部聚焦、父／子图显隐与透明度、清楚的 A/B／独立检查点；父图选点时以按需浮层显示子图对应点参考，窄屏不参与主画布排版。返回步骤只改变观察步骤，不重置其他控制点。
- 文案：只把地图卡片的“打开／空间视图／父图定位”改为与实际动作一致的“绘制此图／以此图为父图／在上层图中查看位置”，不改导航函数、路由或选择合同。
- 兼容：以引入底图字段时的写入和旧渲染为准，使用合成图片定向验证非零偏移、不同尺寸、缩放以及单图／缩略图／空间消费。缺少显式单位时不静默认定为新单位；若无法在兼容数据内同时保证空间对应，恢复旧行为并记录需要版本字段／迁移的明确分叉。
- 验收：正常页面完成选点、聚焦、显隐／透明度、保存重开、进入子图和返回恢复；检查 1152×720、1440×900，录制正常速度连续操作并绑定代码 SHA。回退仅需 revert 本轮独立提交；不修改正式作品、不调用 Provider、不切换日常服务。

## TIANYAN_MAP_R2_CLOSEOUT 结果（2026-09-13 14:12 +08:00）

- 作者操作：精确对齐预览默认聚焦子图对应区域，可切回整张父图、临时隐藏子图或调整叠加透明度；父图 A/B、子图 A/B 与未参与解算的检查位置同时可辨认。返回任一选点步骤不会清空其他点，父图选点步骤可按需展开子图参考图，1152×720 下主画布仍大于 500×300。地图集入口文案改为“绘制此图／以此图为父图／在上层图中查看位置”，只澄清已有动作，未改路由和选择合同。
- 底图兼容：字段引入时只保存数值，旧单图渲染直接使用 CSS `translate(x px, y px)`；合同没有单位或基准画布。此前 R2 把所有旧值除以源图片尺寸会静默改变既有非零位移，本轮已撤销该解释并恢复原显示像素语义。1000×800 与 500×400 合成图片在 `x=100` 时均保持 `translate(100px, 0px)`；单图、空间总览和缩略图均复用同一渲染组件。明确限制是不同画布尺寸下，旧显示像素无法保证与结构化地图坐标保持相同比例；精确跨视图兼容需要新增版本／单位字段并由作者确认迁移，本轮不伪造或迁移。
- 验证：Node 22.22.0/npm 10 下地图 Owner 定向测试 23/23、`map-m4-management-ai-editing` 定向 E2E、`lint` 与完整 `npm run verify` 均退出 0；完整门禁含 unit、integration、十段隔离 E2E 和 build。定向 E2E 首次失败为旧脚本在第四个点触发自动预览后仍点击已不存在的“适配视图”，修正验收步骤后通过，不改变产品流程。build 只有既有大 chunk 警告。
- 最终证据绑定运行代码 `06a8f8687094e74ca7c84212d1ca0fe719914e82`：项目 `r061-e2e-81389d4303de427cb9feaf56bd3f98e2`、作品版本 `work-version.root.f7a7c35c5a4386a5092024c2b8e7bfe4`。[1152×720 最终对齐](截图/11-R2收尾最终对齐-1152x720-06a8f86.png) 已人工查看；[86.64 秒连续录像](附件/R2收尾连续作者操作-06a8f86.webm) 与 [作品／兼容身份](验证/R2收尾作品身份-06a8f86.json) 记录三地图 ID、变换、第三点和双尺寸底图结果。录像 SHA-256 `642a5394a424f01e30f87ff79768b2e433690cda9d327b50be5b09c6160440f`；真实 Provider 0。
- 阶段边界：唯一 VisualDocument Owner、历史只读、修订冲突、图层锁定和提案补偿链未改变。未调用真实 Provider，未修改正式作品，未部署、合并或切换日常服务；Founder 作者体验仍待人工确认。本阶段在 PR #24 冻结，不自动继续开发。回退代码提交即可恢复上轮界面；后续证据／文档提交不冒充录像源码。
- PR 首次远端收尾 CI：run `34742288670` 在 `map-m2-story-observation` 的“恢复地理范围后查找雾港”超时；此前同一范围保存成功文案仍在页面，旧脚本只等待该文案，可能在新的 Owner 请求完成前继续读取派生子项。修复为明确等待本次 `/visual-documents/update` 返回 200 后再检查子项，本地该 scope 随即通过。此提交只同步既有 M2 验收时序，不改变应用运行代码，故上方截图／录像仍准确覆盖应用源码 `06a8f868…`；新的远端完整门禁以最终 PR head 为准。

## TIANYAN_MAP_AUTHOR_WORKSPACE_R2 实施计划

- 现场核对：原分支 `codex/map-m4-closeout` 干净，PR #24 为 OPEN，远端与本地均为 `c684b3344818829982711ef02e5495d9692d258d`，两项 verify 成功；保留整个 M4 与空间编辑、校准提交链。截图暴露的空网格、同图标列表与校准浮层割裂问题不以 CI 关闭。
- 分工：地图集负责内容辨认、搜索、上层路径与低频管理；空间视图负责在真实父图上选择、放置和调整子图；单图编辑负责图形、图层与对象属性。地图集进入父图空间视图 → 选择子图 → 放置／范围控制点／可选对应点对齐 → 保存 → 进入子图绘制 → 返回原父图与选择。
- 复用：提取现有 MapDrawingOverlay 的可复用内容投影，缩略图、空间父图、对齐与单图共用；VisualDocument、相似变换、修订冲突与提案补偿 Owner 不变。Shell 目录仍通过现有入口和呈现机制操作，不创建目录偏好 Owner。
- 步骤：先共享渲染和地图集密度，再统一空间任务区及图上 A/B 选择，随后收敛单图工具和返回／未保存保护；同步相关测试及导航。以北湾、雾港、室内的隔离故事正常操作，检查 1152×720 与 1440×900 真实截图后修正，再跑适用门禁并录制绑定代码 SHA 的最终连续录像。
- 回退：本轮独立提交可逐笔 revert；不迁移正式数据，不删除地图历史，不移动或删除旧证据；真实 Provider、日常服务和部署保持未执行。工程验证与作者体验待确认分别记录。

### R2 实施与诊断记录

- 新增 MapAtlasWorkspace / MapAlignmentWorkspace，只承载作者交互和派生视图；草稿及正式保存仍由 MapM1Workspace 调用 VisualDocument Owner。共享 MapDrawingContent / MapBackgroundContent，未创建地图副本库。空间中键平移、数值／键盘替代、范围边界整体钳制保留顶点形状；单图与空间返回保存视口、选择和必要筛选，未完成草稿会阻止离开并提示保存或取消。
- 底图渲染纠偏：旧单图页把已保存底图位移当显示屏像素，缩略图无法保持同一对应。现明确按源图像素除源尺寸投影到地图，并让单图、缩略图、父图和校准消费共享组件；设置中明示旧显示行为的变化。不重写图片、原图几何、历史保存值或校准变换。默认零位移不变；旧非零位移的显示位置可能与旧版不同，可在现有底图设置调整。
- 图示名称碰撞按空间距离避让，隐藏标签仍保留可访问名称／悬停标题；缩小时收敛标签。手工标注仍保留，超密集地图不是完整自动排版。对齐采用逐图四步 A/B 选点，支持非默认平移、等比缩放、旋转及视口平移缩放，第三个独立位置另验；高级数字调整按需展开。
- 诊断失败均保留名称与原因，不归为偶发：`test:e2e / map-m4-management-ai-editing` review-1 因避让隐藏名称导致旧 M3 查询失败，修复可访问 title；review-2 同名 title/text 双命中，改为互斥渲染；review-3 父图名与上层名双命中，改为作者可读“上层：”；review-4 SVG 标签与图形之间空白点不命中，补足 placement 可点包围盒。review-5/6 正常链通过，截图暴露焦点粗框后改为细描边并复查。
- `npm run verify` 首次失败：`component stylesheets consume semantic tokens and define no component-local colors`（tianyanR0ShellContract.test.ts:245），新增 CSS 局部颜色违反语义 token 规则；已替换，定向 Shell 10/10 通过。第二次 unit 1209/1209、integration 57/57 与前六个 E2E scope 通过，`map-m2-story-observation` 因故事观察区改为折叠而超时；补正常展开后发现从关系页返回再次折叠（tabs.nth(3) 超时），进一步修复展开状态初始化和返回恢复，`/tmp/tianyan-r2-m2-fix3.log` 已通过。一次未选工具链的命令在 Node 24/npm 11 守卫被拒绝，随后均使用 Node 22.22/npm 10。最终完整门禁结果另列，不用旧 CI 或重跑代替失败解释。
- 本地定向 Owner 测试：`node --experimental-strip-types --test tests/storyWorkspace/mapSpatialAndProposal.test.ts tests/storyWorkspace/visualDocumentRepository.test.ts` 23/23。真实 Provider 0；测试只使用本地确定性伪服务和隔离作品，不作为正式 AI 能力展示。

### R2 最终证据索引

**当前采用版本：`9341831ea76ce91819810c27aba264a33dcdd24c`（含未保存主导航保护）。** 以下新证据覆盖该准确源码；此后只有证据／文档提交，不把其 SHA 当作录像源码。

- 最终连续录像：[48.12 秒，同一隔离作品正常界面链](附件/R2最终连续作者操作-9341831.webm)；包含点击“世界”被未保存地图任务拒绝，以及原三层放置、范围、对齐、重开、绘制、返回和 M4 保护链。真实 Provider 0。
- 最终五图：[地图集](截图/06-R2最终地图集-1440x900.png)、[范围与离开保护](截图/07-R2最终范围控制点-1440x900.png)、[A/B 图上选点](截图/08-R2最终图上对应点-1440x900.png)、[1152 对齐预览](截图/09-R2最终对齐预览-1152x720.png)、[1152 室内绘制](截图/10-R2最终室内绘制-1152x720.png)。旧版对照仍使用下方 calibration-r1-final 原文件，不能用旧 R2 截图替代本版。
- [最终作品身份](验证/R2最终作品身份-9341831.json) / [最终保护链身份](验证/R2最终保护链身份-9341831.json)。项目 `r061-e2e-23b593eced744d9cbf2a0a8ad32beda0`，作品版本 `work-version.root.69d69b253cdf7425bca8cceb6ccdc913`；三图最终修订为北湾 5／雾港 14／海关楼 4；独立第三点约 (54.4854,49.8894)。
- 工程：完整 `npm run verify` 已退出 0（unit 1209、integration 57、十个 E2E、build）；导航追加后 M3/M4/R2 连续链、full-shell、typecheck、lint、build 均已通过，最终远端 head 的完整 CI 见 PR #24 Checks。作者体验待人工确认；真实模型未验；未改日常服务或正式数据。

#### 44860a9 前一轮证据（原位保留，非当前最终）

收尾追加：本地 `npm run verify`（`/tmp/tianyan-r2-verify-3.log`）已完整退出 0，包含十个隔离 E2E scope 和 build。随后定向检查发现 Shell 的页面内主导航不会触发 beforeunload，追加唯一 navigation 责任区的可取消离开请求，地图草稿自行拒绝；不在 Shell 保存地图状态。新增“未保存范围 → 点击世界 → 编辑器仍在”的正常界面断言已通过，typecheck/lint 通过。后续最终证据将使用包含该修复的源码；下列 44860a9 证据保留但只覆盖前一实现，不代表导航补丁。

- 最终实现／录制源码：`44860a9df6071366b390be4de5d6fbf053baa6b6`；本次仅追加证据和文档，不改变运行代码。PR 沿用 [#24](https://github.com/dodabojcuk-sys/worlding/pull/24)，最终提交包含本段证据索引，不能把文档提交 SHA 冒充录制源码。
- 正常入口：资料 → 地图 → 地图管理／地图集 → 地图卡片“空间视图”。父图内“在图上放置”或键盘“输入位置” → 范围拖动／控制点 → 可选精确对齐四步 → 预览并保存 → “进入子图”绘制 → “返回父图空间视图”。
- [连续作者操作录像](附件/R2连续作者操作-44860a9.webm)：1440×900，49.2 秒，自动化经正常界面连续操作，包含 M3/M4 保护回归与同一隔离作品的 R2 三层故事；不是 Founder 手工验收或真实 AI 演示。原文件未剪辑，播放器可降低速度复看。
- 五张最终截图：[地图集](截图/01-R2地图集-1440x900.png)、[范围控制点](截图/02-R2范围控制点-1440x900.png)、[图上对应点](截图/03-R2图上对应点-1440x900.png)、[窄屏对齐预览](截图/04-R2对齐预览-1152x720.png)、[窄屏室内绘制](截图/05-R2室内绘制-1152x720.png)。已逐图查看，修正粗焦点框、拥挤标题和多余校准外层头部；1440×900 的室内画布亦在同次录制检查。
- 对照旧版：[旧地图列表](calibration-r1-final/01-map-manager-list.png) → 新地图集；[旧空间空网格](calibration-r1-final/02-spatial-placement-overview.png) → 新范围视图；[旧窄屏数字校准](calibration-r1-final/02e-calibration-1152x720.png) → 新图上选点／预览。旧图对应源码 `ba76c1fa65379fbaacf810114444eb4c8685067d`，只用于前后对照，不代表 R2。
- [R2 作品身份](验证/R2作品身份.json) 记录隔离项目、作品版本、三地图 ID、修订／hash、运行 SHA 与第三点；[M4 保护链身份](验证/R2保护链身份.json) 记录同作品内校准、范围与补偿修订。隔离项目 `r061-e2e-cf3f30a8ddf842dcb68b17749f17ebad`，北湾修订 5、海关楼修订 4、雾港最终修订 14。独立街道中点投影约 `(54.4854,49.8894)`，不是输入的两个控制点。
- 已完成本地：typecheck、lint、build、unit 1209/1209、integration 57/57、地图 Owner 23/23；M2 返回修复与最终 SHA 的 M3/M4/R2 正常界面链通过，资料 M1 在底图共享前已通过并纳入最终完整门禁再验。完整本地 `npm run verify` 与远端最终 head 的 `verify` 结果以本轮交付及 PR 检查为准，不沿用旧 head 的绿灯。
- 明确限制：校准只支持不反射的二维相似变换；点／范围／校准为显式互斥模式，精确对齐可选。碰撞标签采用轻量避让，不是全地图排版引擎；超密集内容仍需缩放、选择或人工调整标注。旧非零底图位移的显示换算变化见上文，持久化数据未迁移。历史地图只展示该修订，不拼接当前外部地图网络。
- 上传范围只含工程、必要日志、合成隔离作品界面和身份摘要；未上传正式作品、原始素材库、凭据或环境文件。真实模型未验；作者体验待人工确认；日常服务、正式数据、部署、合并均未涉及。

此前 `/tmp/tianyan-map-r2-review-*` 均为开发中间证据，不冒充最终提交。旧 `attempt-*`、closeout、spatial、calibration 证据保持原位，不移动或删除。

- 基线：`origin/main` 的 `0f23cb5fe04eea06497bb7b170f713c0da61b8e2`
- 工作分支：`codex/map-management-ai-editing-m4`
- 运行方式：Node 22.22.0 / npm 10，本地隔离前后端与临时作品
- Provider：确定性本地假服务 1 次；真实 Provider 0 次
- 日常服务：未启动、未停止、未切换 4191/4192

## 最终通过

`attempt-7/` 是最终完整通过记录，包含 6 张截图、连续录像和 `map-m4-identity.json`。流程覆盖地图管理、复制、归档／恢复、空间总览、明确两端的跨图道路、准确历史修订、天意结构化提案和整批接受。

## 失败记录（保留原件）

- 根目录：直接调用脚本时缺少 npm user-agent，运行时守卫在启动产品前拒绝；改由仓库 `npm run test:e2e` 入口执行。
- `attempt-2/`：新增“目标地图”后，旧 M3 的宽泛 E2E 标签同时命中两个下拉框；收紧为精确顶栏标签。
- `attempt-3/`：测试卡片按包含文本定位，同时命中原图和副本；改为按精确标题定位祖先卡片。
- `attempt-4/`：地图副本完成后，统一修订账本拒绝未登记的 `copy` 来源；改为既有 `create`，提案接受复用既有 `save`，细节仍由 copiedFrom 和提案回执保存。
- `attempt-5/`：目标端同时存在通道列表项和画布入口，宽泛测试定位命中两个可用控件；改为明确验证画布入口。
- `attempt-6/`：流程通过，但人工查看截图发现历史页只有只读横幅、没有历史画布；修正异步历史读取后的状态提交，并增加必须出现画布的断言。

这些目录是本轮诊断证据，不代表最终通过。最终交付只引用 `attempt-7/`。

## 阶段收尾复核（2026-09-13 08:02 +08:00）

- 远端事实：PR #23 的 head 为 `99883ec30d5f92714d9c7ca2558f7fdfec00b899`，合并提交为 `0c110e2e2b0daa6652faf6ad2a2f9e6376802383`；两条 `verify` 均为 `SUCCESS`。这是 2026-09-13 07:57 +08:00 的重新查询结果，不沿用旧报告猜测。
- 本地收尾代码检查点：`7b69a90ad233219b90f71ce61914beeceacd1f11`。它修复历史地图混入其他地图当前连接、历史页仍暴露编辑器，以及已接受提案没有受保护补偿的问题；尚未 push 或合并。
- 定向领域验证：Node 22.22.0 / npm 10 下 `mapSpatialAndProposal.test.ts` 4/4 通过，覆盖补偿幂等、恢复基准、拒绝零写入和后续作者修改保护；`typecheck` 与 `lint` 通过。
- 最终门禁：第一次 `npm run verify` 的单元阶段汇总为 1206 项中 1 项失败，但直接输出被成功项截断，未取得可识别的失败名称或错误，因此没有猜测归因或据此修改产品。随后同一 Node 22 环境独立 `test:unit` 全部通过，第二次完整 `npm run verify` 通过（unit 1193 项／1206 tests、integration 57/57、十段隔离 E2E、build）；首个无诊断失败仍作为过程限制保留。
- 浏览器证据：`closeout-attempt-2/` 为最终收尾记录，隔离项目 `r061-e2e-ae0d821260544a4fa11163270b08d79d`，作品版本 `work-version.root.2907cba580949f65ea4d4b4ba8364166`。录像和 7 张截图覆盖地图管理、空间总览、目标端连接、历史只读、提案待审、接受和补偿；补偿后地图为修订 20。假 Provider 1 次，真实 Provider 0 次。
- `closeout-attempt-1/` 同样通过，但补偿截图未把提案卡滚回可读位置；保留为本轮过程证据，不作为最终代表画面。
- 日常状态：本次只读检查未发现 4191/4192 监听或 Story Studio 服务进程；因此只能判定“当前未运行”，不能从旧记录推断健康。正式作品根 `/home/beelink/WorldOS`、Provider 配置根和已有升级备份均只读核对，未迁移、未重启、未切换。
- 切换预检：以本收尾分支 HEAD 为目标运行只读预检，候选 SHA 匹配、10 个项目登记可读、237 个数据文件与 5 个 Provider 配置文件可计数、近 120 秒写入 0、锁 0、活动运行记录 0、可用空间约 744 GB；结果仍为 `blocked`，唯一失败是日常 API 无健康响应且 4191/4192 均无监听。它不是切换就绪或切换授权；文档提交后须以新的完整 HEAD 再跑同一预检。
- 实际缺口：正常作者入口不能调整已放置子图的位置或通用范围，也没有坐标校准闭环；工程包只有导出／校验没有正式导入；普通 Provider 通路没有结构化地图操作协议，视觉／图片模型未接入；外部地图没有成套历史快照。上述均不在本次收尾中临时扩建。
- 恢复：代码回滚只需撤销本地收尾提交；补偿本身保存为新地图修订，不删除提案、接受回执或历史。隔离 E2E 数据与正式作品分离。
- 后续唯一优先动作：阶段交接后，由作者另行确认是否把“可调整点／范围定位与坐标校准”作为下一个独立地图切片；未确认前不启动 M5。

## TIANYAN_MAP_SPATIAL_EDITING_R1 实施计划（2026-09-13 08:35 +08:00）

- 现场：继续使用干净的 `codex/map-m4-closeout`；当前 HEAD `34e6e53b8db0d40eed50344caa6785fac840846c`，恰好保留本地收尾 `7b69a90ad233219b90f71ce61914beeceacd1f11` 与文档冻结提交，`origin/main` 仍为 `0c110e2e2b0daa6652faf6ad2a2f9e6376802383`。不新建 worktree、不重复 cherry-pick。根目录不存在 `README.md`，其余指定核心、导航、路线图和 M4 最终身份已核对。
- 实施：先沿 `visualDocumentRepository` 的既有 placement、几何和 calibrated transform 合同追踪读写与消费方；在同一 Owner 和修订冲突保护内补齐点拖动/数值编辑、范围原几何编辑及可确定时的坐标校准，明确预览、保存、取消、失败和历史只读状态。
- 验收：用隔离作品从正常“资料 → 地图”入口连续验证创建父子图、点定位非中心、调整保存、刷新重开、范围调整与取消、列表返回定位、历史只读；同时覆盖陈旧修订、非法父图和 M4 提案补偿保护。只运行本轮风险相关定向测试及仓库完整门禁，浏览器证据绑定项目、作品版本、地图 ID、代码 SHA 和地图修订；真实 Provider 保持 0。
- 回退：代码以本轮独立提交回退；地图保存始终生成 Owner 新修订，不删除旧修订、提案或补偿回执。隔离临时作品与正式数据分离；不启动、停止或切换日常 4191/4192。

## TIANYAN_MAP_SPATIAL_EDITING_R1 结果（2026-09-13 09:12 +08:00）

- 完成：正常“资料 → 地图 → 地图管理 → 空间总览”可选择父图，点定位可拖动和数值调整；范围可建立、整体移动／缩放和逐顶点编辑。画布与输入明确标记为临时预览，拖动不逐像素写修订；保存才调用一次 `visualDocumentRepository`，取消恢复已保存状态，失败保留预览。列表“父图定位”返回同一父图总览。
- 持久化与保护：点拖动和范围保存各只增加一个父图修订；刷新重开恢复位置和原范围顶点。隔离浏览器在编辑期间通过同一 Owner 写入较新作者修订，陈旧页面保存明确失败，较新标签与位置均未被覆盖。失效 child、父图自引用、循环与跨项目路径继续由既有拓扑／项目边界封闭拒绝；M4 提案拒绝、过期与补偿保护测试仍通过。
- 校准分叉：现有 `calibrated` placement 只存 `translateX/translateY/rotation/scale`，消费方只显示“已校准”状态；仓库没有定义变换方向、子图原点／坐标边界如何进入父图，也没有对应点解算或总览渲染算法。新增输入框会伪造支持，因此本轮不改变核心合同、不引入迁移。最小下一步是先冻结“子图坐标 → 父图坐标”的应用公式、原点／边界语义和至少两点对应的退化条件，再由总览与子图往返共同消费。
- 证据：最终采用 `spatial-editing-r1-final/`，绑定代码 `77282f25aa9e2bdd16504ab2e3fb535564cc555f`、项目 `r061-e2e-b932aa8736b447c0becd1871963fb872`、作品版本 `work-version.root.a94ee88e98858a76f9e0c65f5a6c3d98`、父图 `map.地点示意图`、范围子图 `map.北湾作者地图-副本`、保存时父图修订 20。连续录像 SHA-256 为 `cfcc773d07ab552ff7b7f6ba7e5ef991d33c91e8329b5b0b1d6142f63614d505`；9 张截图含编辑器、重开范围、历史只读、提案接受与补偿。假 Provider 1 次，真实 Provider 0 次。上一代码 SHA 的旧证据目录已移入系统回收站，可恢复且不作为交付证据。
- 验证：Node 22.22.0/npm 10 下定向 `mapSpatialAndProposal.test.ts` 5/5、扩展后的 `map-m4-management-ai-editing` 浏览器闭环、`typecheck`、`lint`、`build` 均通过；最终 `npm run verify` 通过（unit 1207/1207、integration 57/57、默认十段隔离 E2E、build）。首次误用宿主 Node 24/npm 11 被运行时门禁拒绝；切换正确工具链后首次定向测试命令又因误用未安装的 `tsx` loader 失败，改用仓库一致的 `--experimental-strip-types` 后 5/5 通过。浏览器前两次失败均保留准确名称：测试错误假定雾港已有 range；随后取消尚未保存的新 range 后错误尝试重开。修正为先建立并保存 range、再验证取消与重开后通过，没有把它们归为偶发问题。
- 未执行：真实 Provider、视觉／图片模型、正式工程包导入、日常服务切换、部署、正式数据迁移和 Founder 人工体验验收均未执行。
- 回退：回退本轮 `feat(map)` 与后续测试／文档提交即可撤销代码；既有地图历史、M4 两笔本地收尾提交、提案和补偿回执均不删除。隔离作品不进入正式数据根。

## TIANYAN_MAP_CALIBRATION_R1 实施计划（2026-09-13 09:27 +08:00）

- 现场：PR #24、任务分支与本地 HEAD 均为 `ba2c34352ed1ad7346669b03d69963b421d8014b`，工作树干净；两条远端 `verify` 的 typecheck、lint、unit、integration、build 已成功，E2E 仍在运行，尚无失败证据。M4 收尾、点／范围定位提交链完整保留。
- 语义：采用两个对应点确定的不反射二维相似变换；`parent = translate + scale × rotate(child)`，坐标来自各地图 `coordinateSystem.bounds`，轴固定 `x-right-y-down`，单位可分别为空或由地图声明。源点或目标点重合时拒绝；视口 pan/zoom 不进入地图坐标。对应点与解算结果保存在同一父图 placement，父图总览用同一变换投影子图边界和图形。
- 模式：同一父图／子图 placement 在 point、range、calibrated 三种显式模式中择一；转为校准会在一次受冲突保护的新修订中替换当前模式，取消不写入，历史继续保留旧模式，不推导任何世界事实。
- 验收与回退：隔离作品经正常资料／地图入口完成建立、预览、保存、重开、修改、取消；用未参与解算的第三点验证非默认平移、缩放、旋转，并覆盖视口变换、退化输入、陈旧修订、历史只读和 M4 补偿。代码按本轮独立提交回退；不迁移正式数据、不切换日常服务、不调用真实 Provider。

## TIANYAN_MAP_CALIBRATION_R1 结果（2026-09-13 09:55 +08:00）

- 作者闭环：正常“资料 → 地图 → 地图管理 → 空间总览”中，作者可从既有点／范围定位进入“坐标校准”，填写子图 A/B 与父图 A/B 对应点，实时查看父图中的轮廓、子图图形及未参与求解的十字检查点；取消不写入，保存只建立一个受 contentHash 冲突保护的父图修订，刷新重开恢复同一对应点和变换。校准后父图画布实际投影子图图形，视口缩放不改地图坐标变换。
- 冻结语义：两图均以各自 `coordinateSystem.bounds` 为坐标边界，轴固定 x 向右、y 向下；采用不反射二维相似变换 `parent = translate + scale × rotate(child)`，两个对应点确定平移、统一缩放和旋转。子图点／父图点重合、点越界或缩放超出既有 transform 支持范围会在保存前给出原因并禁用保存。point、range、calibrated 是同一 placement 的互斥显式模式；不支持反射、非均匀缩放、仿射、透视、GIS 或真实经纬度。
- Owner 与历史：对应点、解算 transform 和投影边界仍由父图 placement 经唯一 `visualDocumentRepository` 保存；服务端重新解算并拒绝对应点、transform 或 bounds 不一致。陈旧修订与非法 child 继续封闭失败且不产生部分写入。历史地图只读，显示所选父图修订自己的 placement，不把当前子图绘制拼成历史空间网络；M4 提案过期、幂等补偿和后续作者修改保护没有改变。
- 验证：Node 22.22.0/npm 10 下 `mapSpatialAndProposal.test.ts` 7/7 通过；完整 `npm run verify` 通过（unit 1209/1209、integration 57/57、十段隔离 E2E、build）。最终代码 `ba76c1fa65379fbaacf810114444eb4c8685067d` 追加局部响应式布局与 1152×720 几何断言后，`typecheck`、`lint`、`build` 和 `map-m4-management-ai-editing` 定向 E2E 再次通过；远端完整门禁以 PR #24 最终 head 为准。构建仍有既有大 chunk 警告，不是本轮失败。
- 失败与修复记录：首次定向 E2E 的独立检查点断言预期 `(54,28)`，界面当时使用 `(50,75)` 检查点而得到 `(34,36)`；统一界面检查点为合同测试的 `(30,25)` 后通过。首批录像虽通过但传入的完整代码 SHA 写错，身份核对后作废。人工截图检查随后发现校准面板被容器裁切；改为画布列内浮层后，E2E 又精确捕获浮层遮挡右侧保存按钮，最终把面板限制在画布列并为 1152×720 使用单列可滚动布局。所有非最终尝试均不作为交付证据。
- 最终证据：`calibration-r1-final/` 绑定项目 `r061-e2e-59016c109f7b491b8f7600692179701f`、作品版本 `work-version.root.6dd3aafb1fd11e16f4237948b79fdc34`、父图 `map.地点示意图`、子图 `map.雾港-局部地图` 和代码 `ba76c1fa65379fbaacf810114444eb4c8685067d`。保存校准为修订 19；控制点 `(10,10)→(60,20)`、`(50,10)→(60,36)`，变换为平移 `(64,16)`、缩放 `0.4`、旋转 `90°`，第三点 `(30,25)→(54,28)`。连续录像 SHA-256 为 `3818a2c2df1db5832d7ada708970106b9b8cf509924974d381817baea4fc34a5`；12 张截图含 1440 校准预览、1152×720 无遮挡、父图重开叠加和精确历史只读。假 Provider 1 次，真实 Provider 0 次。
- 未执行与回退：未调用真实 Provider，未接视觉／图片模型，未部署、未切换或重启日常 4191/4192，未迁移或修改正式作品数据，未做 Founder 人工体验验收。回退本轮代码提交与后续证据／文档提交即可撤销功能；既有 M4 收尾、点／范围提交、地图历史和提案回执均保留。

## TIANYAN_MAP_REAL_AI_COLLABORATION_R1 实施计划（2026-09-13 +08:00）

- 现场：PR #24 的精确 head 为 `cb31f35096ece4fd2216f4fcf0a0172d97e4eff3`，两条 `verify` 均成功；工作树干净。在同一 worktree 从该 R2 基线创建后续分支 `codex/map-real-ai-collaboration-r1`，不改写 PR #24，不重复入库 R2。
- 实施：地图页仍为主工作面，复用 Shell 唯一天意右侧工作面和同一 TianyiConversation Work lane；服务端在既有 Provider Gateway 内组装受预算的结构化地图上下文，只接受命名工具输出，交给既有 Map Edit Proposal Repository 持久化待审操作。选择范围只允许修改明确对象；新增必须明确未锁图层和区域，参考对象只读。
- 验收：以隔离北湾地图从正常页面验证“选中道路+森林只读参考→绕行且端点不变”和“明确图层+区域→少量新增”，实际 Provider 发送总数不超过 6；检查地图内差异、接受／拒绝、重开、后续人工修改与受保护补偿，失败路径用可控伪服务器／仓库测试覆盖。正式门禁使用 Node 22.22.0/npm 10，最终证据绑定准确源码 SHA、隔离作品、Provider／模型标识和真实发送数。
- 回退：撤销本后续分支的代码／文档提交；待审提案与接受／补偿仍是既有可追溯文件和 VisualDocument 新修订，不删除 R2 历史。真实验收只使用专用隔离作品，不启停日常服务、不读写正式作品。

### TIANYAN_MAP_REAL_AI_COLLABORATION_R1 结果（2026-09-13 +08:00）

- 作者闭环：当前地图在浏览模式选中一个或多个结构化图形后，可在同一 Shell 天意 Work lane 发起“修改所选”；无选择时只允许作者明确选择未锁图层和矩形坐标范围的“区域内新增”，不会扩大为整图权限。天意侧显示地图、修订、可修改对象、只读参考、生成／取消／失败状态、逐项原因和地图内红色修改前／绿色提案后差异；模型结果只创建待审提案，接受、拒绝和受保护补偿继续使用既有 Repository 与唯一 VisualDocument Owner。
- 模型边界：Provider 只接收项目、作品版本、地图身份／修订、x 右 y 下的 0–100 结构化坐标、图层锁定、明确可编辑图形、只读参考、区域、操作上限和作者原话；明确声明没有看到底图图片。唯一工具 `propose_map_edit` 最多 8 项／每图 24 点；选择范围只准更新／删除所选，区域范围只准在指定图层和边界内新增。普通文本、额外字段、越界／退化坐标、参考对象修改、锁层、端点漂移和模型自带对象 ID 均不会直接进入正式地图。
- 真实验证：最终运行源码 `4b45e18bf1e4623444cbb48c1198ca58cc1018b8`，隔离项目 `r061-e2e-d563c6816feb4d6ba72f68c30fcf49b4`、作品版本 `work-version.root.4948322a619a1cfa8a528a4a29de0e83`、地图 `map.北湾-AI-协作隔离图`。通过权威配置中的 `radeon-cloud` / `self-dploy/DeepSeek-V4-Flash-Vision-Exp`：A 将北湾滨海道改为绕开只读雾松林，两个端点 `(10,50)`／`(90,50)` 保持且森林不变，作者接受后保存重开一致；随后人工把路宽改为 5，补偿因后续修订被明确拒绝且人工值保留。B 在明确标记图层和 `(55,55,25,25)` 区域返回一个“北侧观察点”新增提案，几何位于区域内，作者拒绝后正式地图没有该对象。最终两条和早先同内容两条均理解成功；没有模型理解失败或真实调用重试。
- 预算与证据：原权威账本计数已到旧授权上限，未绕过；按本轮作者明确“最多 6 次”授权新增 `map-real-ai-collaboration-r1` 回执，generation/total 上限由当前 `32/36` 增至 `38/42`。本轮共实际发送 4 次：首个 `1af239e` 证据候选 2 次，保留但不代表最后的入口兼容修复；最终 `4b45e18` 证据 2 次。结束计数 `36/40`，无工具续轮或重试，仍余 2 次授权且不再使用。最终证据在 `real-ai-r1-4b45e18/`：[1440 道路差异](real-ai-r1-4b45e18/12-真实AI道路提案预览-1440x900.png)、[接受重开](real-ai-r1-4b45e18/13-真实AI接受后重开-1440x900.png)、[后续人工修改保护](real-ai-r1-4b45e18/14-后续人工修改受补偿保护-1440x900.png)、[1152 区域新增待审](real-ai-r1-4b45e18/15-真实AI区域新增待审-1152x720.png)、[30.44 秒连续录像](real-ai-r1-4b45e18/真实AI地图协作连续作者操作-4b45e18.webm) 与 [身份记录](real-ai-r1-4b45e18/map-real-ai-identity.json)。录像 SHA-256 `ebf64d43fed8a6faa0486561b71b0e6e95cc42f575214c491d959f6fdf9c7114`。
- 工程验证：Provider adapter／地图 Owner 11/11、unit 1213/1213、integration 57/57、完整 E2E scope 集、typecheck、lint、build 均通过；build 仅保留既有大 chunk 警告。首次完整 `npm run verify` 在 unit 阶段 1211/1213 失败：Shell 中文静态合同发现新直写标签，Provider E2E 隔离合同无法识别被条件表达式包裹的 test 环境；分别改用既有中英翻译 Owner，并恢复可静态核验的标准测试根、仅在显式 live 验收切换。修复后定向合同 16/16 与全 unit 通过。随后完整 E2E 在 M3 等待旧“交给天意”地图问答面失败，证明新入口覆盖了既有能力；最终保留“交给天意”问答，并新增语义不同的“AI 编辑”地图内动作，M3、M4、真实 AI 与资料 M1 后续 scope 全部通过。更早的 SVG 点击、异步地图读取、断言词序和预期补偿 400 收集失败也逐项保留并修复，不称为偶发。
- 边界：没有发送正式作品、底图像素、无关正文或凭据；没有接图片模型、修改父子定位／校准、生成世界事实、部署、合并或启停日常服务。R2 作者体验状态仍是待人工确认；真实模型结果证明两条隔离请求可用，不等于所有自然语言要求都可靠。回退本后续分支提交即可移除入口和 Provider adapter；既有地图修订、提案回执和 PR #24 历史不删除。

## TIANYAN_MAP_AI_REVIEW_R2 实施计划（2026-09-13 17:32 +08:00）

- 现场：继续使用 PR #25 的干净分支 `codex/map-real-ai-collaboration-r1`，本地、远端与 PR head 均为 `3cd3a5287552636506567a8121a9797c5aa07d91`；PR #25 继续依赖未合并的 PR #24，两边现有 `verify` 均成功。不新建提案 Owner、会话或地图库。
- 几何核实与修正：旧隔离故事的“雾松林”实际是 `terrain/forest` 折线，淡绿色晕染来自同一折线按 `width=15` 的装饰性宽笔触，树木符号由该路径重采样生成；Provider 只收到中心折线顶点且旧 E2E 只验证道路端点、发生变化及森林对象未被修改，并未判断道路是否避开整片晕染。因此历史“绕开森林、语义失败 0 次”不能作为整个森林范围的几何结论。本轮保留原记录并追加纠正：以作者明确建立的 area 多边形作为只读避让范围，发送同一范围并用确定性线段/多边形相交检查验证道路不进入范围。
- 审阅修改：提案到达后按变化前后联合范围和关键只读参考聚焦；提供“修改前／修改后（未保存）／叠加对比”三个只读视图、查看整图和返回原视口，用户手动浏览后不重复抢焦点。地图上用颜色加线型区分旧/新，标明道路端点、变化路段与只读范围；侧栏分开显示作者要求、修改对象、保持不动的参考、系统已检查与作者仍需判断。
- 状态与布局：面板正文独立滚动，接受／拒绝或终态固定可见；生成中、待审、保存中、已接受、已拒绝、已取消、已过期、已撤销、无法直接撤销和失败分别按实际写入回执解释。用户文案使用“撤销此次 AI 修改”，不改变底层补偿和冲突保护。
- 验收：优先用受控夹具完成三视图、删除／新增／拒绝清理、陈旧／锁层／重复接受和受保护撤销；在 1440×900 与 1152×720 真实截图中核对同视口差异、森林边界、端点和固定决策区。仅在最终需要核对真实模型时使用最多 2 次新增授权，不重放冒充调用，不筛掉失败。最终录像绑定准确源码 SHA。
- 回退：撤销本轮独立代码、测试和文档提交即可恢复 PR #25 现状；地图正式写入仍由唯一 `visualDocumentRepository` 建立新修订，旧提案、接受回执和历史证据不删除，不触及正式作品或日常服务。

### TIANYAN_MAP_AI_REVIEW_R2 结果（2026-09-13 +08:00）

- 作者审阅：待审提案默认按旧新几何与关键只读参考的联合范围聚焦，并将右侧天意面板实际遮挡纳入可见宽度计算；可切换“修改前／修改后 · 未保存／叠加对比”，查看整图或返回原视口。旧路用语义危险色虚线，建议路用强调色实线，端点、变化点和明确范围独立标记；未变对象降权且审阅态隐去非关键标签。面板将作者原话、准备修改、保持不动的参考、系统确定性检查和仍需作者判断分开；正文可滚动，接受／拒绝或终态操作固定可见。
- 状态与保护：生成中、待审、保存中、已接受、已拒绝／取消、已过期、已撤销、无法直接撤销和失败都按实际回执说明是否写入；用户操作改为“撤销此次 AI 修改”。接受时服务端再验基准修订、图层和几何约束；重复接受、陈旧提案、锁层和后续人工修改的受保护撤销仍由既有 Owner 处理。
- 森林几何结论：旧“雾松林”只是 `terrain/forest` 中心折线；淡绿色是同一折线按 `width` 绘制的装饰性宽笔触，树木符号也从该路径重采样。旧 Provider 上下文未发送 width，旧验收只查端点不变、道路发生变化和森林对象不变，所以“语义失败 0 次”只能限定为旧对象保护和端点检查，不能证明绕开整片森林。本轮以作者明确选择的 `area` 多边形作为唯一确定性不可穿越范围，Provider 读取同一顶点集，建议创建及接受前均用线段／多边形相交检查；装饰笔触不冒充占地边界。
- 真实模型：第一次验收命令遗漏已有授权回执，预分派门禁拒绝且账本保持 `36/40`，实际 Provider 发送 0；失败录像保留在 `ai-review-r2-attempt-1-no-dispatch-1dacb2d/`。修正命令后向 `radeon-cloud` / `DeepSeek-V4-Flash-Vision-Exp` 实际发送 1 次，账本为 `37/41`，Provider 传输回执为 success，但 30 秒内正常页面未形成可审提案；没有接受或地图写入，不声称模型已正确绕行。当前证据不足以在结构输出、应用校验或界面回显之间进一步归因；按约定不自动重试，剩余 1 次授权未使用。证据位于 `ai-review-r2-attempt-2-provider-no-proposal-1dacb2d/`。
- 验证与证据：最终源码 `940a72d248db9e1fd7e2012485c027119282cc45`（产品功能提交 `3762eebdf675c347d6aacdc1e25892fc0734ea76`）。完整 `npm run verify` 通过（typecheck、lint、unit 1214/1214、integration 57/57、默认十一段隔离 E2E、build）；首次在 unit 1214 中失败 1 项，名为 `component stylesheets consume semantic tokens and define no component-local colors`，原因是新审阅图层使用了组件内颜色，改用 Shell 语义 token 后合同 10/10 及完整门禁通过。最终 UI 证据使用本地受控 OpenAI-compatible 夹具，不冒充真实模型；在 `ai-review-r2-ui-final-940a72d/` 中包含同视口的修改前、修改后、叠加对比、1152×720 决策区、接受重开、后续人工修改保护和区域新增拒绝。连续录像在关键视图留停，时长 18.60 秒，SHA-256 `92b469913099a052a322b3bf7b13b737f329e4ad3672122a24292917f6680fe5`；隔离项目 `r061-e2e-04be1f490d4b42999825e9a99c4e1b6e`、作品版本 `work-version.root.d3384933a166f11a5702601bce25397a`、地图 `map.北湾-AI-协作隔离图`。
- 边界：未删除历史证据，未读取或修改正式作品，未合并、部署、切换或启停日常服务，未接入图片模型。真实模型的新明确范围案例未产生可审结果，作者人工体验仍待独立确认；本阶段在此冻结。

## 地图作者工作台 R3 实施计划

- 基线：工作树干净，HEAD 为 `df221eda1f09163d7eed8707e337388d262043ff`；PR #25 依赖未合并的 #24，两个候选当前 CI 均成功，沿用既有分支。
- 布局：地图集负责组织，空间视图负责父子放置，单图负责完整地理背景上的手工创作。单图压缩标题与常用操作，工具独立成栏，属性按当前工具/对象展开；AI 审阅与属性协调，不新增 Shell 或地图 Owner。
- 实施：复用 MapM1Workspace、mapAuthoring、图层、VisualDocument 保存/历史和现有 AI 提案链；补齐直接移动、文字编辑和新对象样式，固定端点与避让均以作者显式约束为准。旧几何、旧底图和已保存约束不静默改写。
- 验收：隔离完整区域地图从空白绘制陆水、山林、河路、聚落文字，调整图层与对象、保存重开，并在同一背景审阅本地提案；检查 1152×720 和 1440×900、局部图返回、修订与撤销保护。按仓库门禁验证并保存最终准确 SHA 的截图/连续录像；本轮真实 Provider 0 次，上轮真实请求未形成提案的限制保留。
- 回退：按本轮独立提交反向撤销代码；不删除地图历史、证据或提案回执，不迁移正式数据、不合并/部署/切换日常服务。作者体验仍待人工独立确认。


### R3 实施与定向核对记录

- 工具栏从长表单独立，低频地图管理/历史/底图及标题修改集中到“地图与历史”；属性仍由原 MapM1Workspace 控制，打开天意时暂收属性及绘制工具，并依据侧栏实际几何预留地图区域。工程目录仍使用既有 Shell 入口及偏好，不另建目录状态。
- 单图原有拖动只平移视口；现补图示/文字直接移动和选中节点拖动，临时几何只作预览，松开通过同一 Owner 保存一次修订。方向键支持选中对象/节点精调，文字可选择、改名、复制和删除，撤销/重做沿用原会话栈。区域新增陆地/水域样式，新聚落使用独立 village 图示值，旧 settlement 不静默替换。
- 新道路/河流使用可辨认笔宽；画布文字补偿非等比屏幕拉伸和视口缩放，聚落/选中对象名称优先于低优先级地貌标签。网格为本机可切换显示偏好。空白画布提供从区域开始的明确引导。
- 固定线条两端改为默认关闭的作者选项，服务端只在 true 时建立约束；旧已保存提案约束不改写。只读参考仍不可修改，只有明确勾选的 area 才加不可穿越轮廓和几何检查。普通地图及无端点约束提案不显示起终点标签。
- 隔离 R3 正常页面已完成 10 个独立图示（陆水、三段山林、河路、三聚落）及 1 个文字的绘制、节点拖动、图层显隐/锁定、保存重开和明确标注来源的本地提案拒绝。首轮截图发现线条过细、聚落标签受挤压、文字横向拉伸，已修正后重新检查；旧证据没有移动或删除。
- 失败记录：R3 首次 scope 在提案回执断言失败（undefined !== 201），原因是 postFixture 返回已解析 JSON 而非 Response，改查 data.status；一次图层快速连点在等待“图层锁定状态已保存”超时，发现前一保存仍 busy 时后续动作被忽略，现属性区保存期间禁用，修复后 R3 scope 通过。M3 等待旧常驻“专注地图”、M4 等待旧常驻“跨图通道”、AI 回归同时访问审阅和属性面板分别超时；随实际信息架构更新正常菜单/属性切换路径，没有 force-click 或削弱保护断言。
- 当前定向 typecheck、lint、build 与 M3/R3 浏览器链通过；完整 verify 正在执行，最终状态以后续记录为准。真实 Provider 0 次，仍保留 R2 真实发送未形成可审提案及旧底图显示像素歧义的限制。

- 门禁收尾：`npm run verify` 的 typecheck、lint、unit 1215/1215、integration 57/57 及前六个默认 E2E 已通过，随后 map-m2-story-observation 因访问收起的“故事观察位置”失败。定向修正并逐项复验：M2 的节点切换/资料返回菜单需要显式打开；旧平移坐标落在图形上后现在会移动图形，改由“平移”工具执行并先断言 mapPanX 已写入；属性切换不能覆盖地点详情为真实产品缺陷，已修复互斥切换。M4 的“空间与行政”和“澜星→局部图”、world-materials-m1 的地点/返回资料同样按正常入口更新。所有失败保留名称及上述原因，不认定为偶发。
- 修复后单独 `TIANYAN_E2E_SCOPE=map-m2-story-observation / map-m4-management-ai-editing / map-real-ai-collaboration-r1 / world-materials-m1 npm run test:e2e` 通过；M4 包含 M3 作者、父子图、空间编辑及保护链，未削弱原断言。R3 完整地图 scope 加入默认门禁（共十二段）。`npm run verify -- typecheck lint build` 通过；一次直接 node 调用 run-verify 因 npm_execpath 缺失在运行前拒绝，改回 npm 入口后通过。build 仅保留现有大 chunk 提醒。
- 最终功能冻结前检查：只涉及十个明确代码/测试/导航/日志文件，没有环境文件、凭据、正式作品或数据库。历史证据不移动；最终录制将在提交后绑定源码 SHA，后续只补证据和文档。工程验证不等于创始人体验验收。

### R3 最终证据与阶段边界

- 功能及录像源码：`9ba62d52f8fa72185c3f3c39479875244f5750fe`，继承 `df221eda` 及 PR #25 → #24 全部既有提交。源码冻结后 `TIANYAN_E2E_SCOPE=map-author-workspace-r3 TIANYAN_E2E_SOURCE_REVISION=9ba62d52f8fa72185c3f3c39479875244f5750fe npm run test:e2e` 通过；最终录像另设已有证据目录变量，未调用真实 Provider。后续提交仅含本节、路线图和最终截图/录像/身份，不冒充新源码录像。
- 正常入口：资料 → 地图 → 空白起点 → 建立地图；区域描出陆水，地形连续画山林，线条画河路，标记反复放聚落，文字输入后点图放置。“选择”直接移动对象，“编辑节点”按需显示节点；“平移”不会移动对象。属性/图层、地图与历史和天意按任务切换，保存通过既有 Owner 自动建立修订。
- 同一隔离作品身份：项目 `r061-e2e-2a43d0d3a4264b758d3baf2763438a75`；版本 `work-version.root.31f80caa77537ca9c7d02725e857a8b1`；地图 `map.地点示意图`（作者标题“北湾手绘区域”）；10 个独立图示 + 1 个文字，重开修订 32。完整内容哈希及来源在 [身份回执](验证/R3地图作者身份-9ba62d5.json)。
- 最终采用截图：[全景 1440](截图/01-R3完整区域地图-1440x900.png)、[选中道路节点 1440](截图/02-R3选中道路节点编辑-1440x900.png)、[保存重开 1152](截图/03-R3保存重开-1152x720.png)、[本地提案审阅 1152](截图/04-R3完整地图AI审阅-1152x720.png)、[本地提案审阅 1440](截图/05-R3完整地图AI审阅-1440x900.png)。已查看真实画面：关键地理对象、工具、窄屏接受/拒绝均可达；选中节点与道路标签不遮盖聚落。
- [连续操作录像](附件/R3完整手绘连续操作-9ba62d5.webm)：41.96 秒，原始正常速率、操作间节奏与关键步骤留停，无加速剪接；从空白正常绘制到文字改名/复制/删除/键盘精调、节点拖动、图层显隐/锁定、重开及同背景提案拒绝。SHA-256 `4aee51d03f0346fa66bb8fb32032c1769e7f59d861c47a487d640cf17e8a241e`。本地提案通过既有候选入口建立，只验证审阅呈现和拒绝不写入，不冒充模型生成；锁层方向键移动不改变内容哈希。
- 工程状态：全部门禁组成项本地通过（typecheck、lint、unit 1215、integration 57、原十一段 E2E 完整/定向续验、新增 R3、build），原始完整 verify 的失败与修复上文保留，不把分段续验称为同一次完整成功。最终远端完整十二段 CI 以 PR #25 对应最新 head 为准。两种窗口的画面检查属于工程检查，作者体验仍待人工独立确认。
- 实际限制：山林是可编辑装饰笔触，不自动成为不可穿越面；道路/河流以折线节点编辑，不提供寻路、曲线平滑或交通网络；密集文字仅做轻量避让，极密场景仍需作者调整。撤销/重做沿用本会话历史栈，跨重开追溯使用地图修订；旧底图显示像素歧义没有迁移。R2 那一次真实请求未形成可审提案的状态原样保留，本轮真实发送 0。
- 未触及正式作品、数据迁移、地图事实写入边界、日常服务或部署；不合并 PR。本轮停止于完整手工地图工作台，不扩建固定端点绕林或下一阶段 AI。

## 地图地点与创作衔接 R4 实施计划（2026-09-13 +08:00）

- 现场：工作树干净，分支 `codex/map-real-ai-collaboration-r1` 与 HEAD `2e008b205ddc1a7e7739bd5c4ca85c22b7823a86` 一致；PR #25 仍以 PR #24 任务分支为 base，已见 #25 两条最终检查成功。根目录没有 README；按 AGENTS、产品核心、目录导航、路线图和本日志继续，不建立另一套进度文档。
- 复用与边界：继续以 `MapDrawing.objectId` 表达图示对既有 World Object 的显式引用，以 `MapContent.scopeObjectId`、placements/entrances 表达局部地图及其空间关系；唯一写入仍是 `updateVisualDocument`。地点正文只由 World Object Owner 读取，解除关联不删除地点或局部图。天意继续使用同一 Work lane 的 `mapRef/mapRevision/mapElement` 明确引用，不自动发送。
- 实施：在 R3 上增加选中图示的“关联已有地点”、地点卡和轻量“本图地点”抽屉；关联、局部图选择/进入、资料返回、天意引用准备都恢复地图视口、选择和面板状态。独立根地图不显示误导性的子图定位入口；工具栏在窄屏使用任务分组，当前绘制层与对象所属层明确命名。
- 验收：隔离作品中保留同名地点夹具，正常页面把雾港图示显式关联到指定地点，打开资料/局部图编辑并返回，列表定位另一地点，进入天意检查地图+图示+地点引用但不发送，重开验证后解除关联并确认地点仍存在；覆盖失效引用、隐藏层、保存失败和项目切换清理。检查 1440×900 与 1152×720，最终录像绑定准确源码 SHA。
- 回退：按地点关联、局部图/天意衔接、UI 三个本地提交反向撤销；测试只写隔离目录，随 fixture 回收。若已在隔离地图形成关联修订，代码回退不会删除其历史；需要回退数据时通过 VisualDocument 新修订恢复旧 `objectId`，不删除地点资料、地图或正式历史。本轮不 push、不合并、不部署、不启停日常服务、不读写正式作品、不发真实模型请求。

### R4 实施、验证与证据

- 作者流程：选中聚落等地图图示后，可搜索当前作品的既有地点并再次确认关联；搜索结果显示名称、别名或标签，不按同名自动匹配。已关联图示在浏览模式打开简洁地点卡，可进入资料、显式关联已有局部地图（只写 `scopeObjectId`，不改父子位置）、或选择“新建并放置局部地图”走既有入口。进入局部图完成编辑后，返回恢复上层视口、图示选择和地点卡。
- 边界与恢复：`MapDrawing.objectId` 仍是 VisualDocument 中的可选引用，写前由 Owner 验证地点真实存在；失效目标不会形成半写入。解除关联只把图示引用置空，不删除地点、局部地图或历史。顶部统计改为图示／已关联地点／子图，其中已关联地点是既有 marker 与显式图示引用的去重并集。本图地点抽屉区分已关联资料、纯图示和隐藏图层；不会擅自显示隐藏层。
- 天意衔接：地点卡用现有 `mapRef`、`mapRevision`、`mapElement`、`materialRef` 准备一张地图、一个图示和一条地点资料的明确上下文，不自动发送。未发送上下文的“返回地图继续创作”回到当前可编辑地图；已保存回答回执的“返回来源”仍按精确修订打开历史证据，两种语义不再混用。
- UI：1152×720 的绘图工具保持单行横向溢出，不再把 AI 按钮单独换行；地点卡、本图地点、属性和 AI 工作面互斥。AI 审阅背景对象不再整体降到难辨认的透明度。当前绘图图层与图示所属图层继续使用不同标签。
- 测试：Node 22.22.0/npm 10 下 `mapSpatialAndProposal.test.ts` 9/9、typecheck、lint、build 和 R4 专项浏览器任务通过；完整 `npm run verify` 的 typecheck、lint、unit 1217/1217、integration 57/57 及此前默认 E2E 段通过，随后 `map-m2-story-observation` 因旧断言仍等待“个已放置地点”失败。核实后修正为新统计并补回旧 marker 计数，受影响 M2 E2E 与 R4 E2E 均重新通过；未因这处测试文案再重复整条长门禁。build 只有既有大 chunk 提醒。
- 失败证据：地点卡首轮脚本错误假定夹具正文；第二轮从未发送天意上下文返回历史只读页，修复为“继续创作／证据回看”分流；随后两轮分别暴露脚本在 1152 等隐藏统计、以宽泛同名元素定位的问题。失败录像保留在 `map-r4-attempt-1-e4e598f/` 至 `map-r4-attempt-5-1aaba07/`，不作为最终成功证据。
- 最终源码与证据：功能源码 `1aaba0722da98f28749df111cc10c522054c31da`；隔离项目 `r061-e2e-bd23c78a4695471190ea6e9f6fb3b6f4`、作品版本 `work-version.root.93d975482ef7ac58867aff12ef3b698a`、区域地图 `map.地点示意图`、地点 `location.雾港`。最终目录 `map-r4-final-1aaba07/` 包含完整地图地点卡、局部图返回、天意引用准备三张 1440×900 截图、身份 JSON 和 39.88 秒 1440×900/25fps 连续录像；录像 SHA-256 `6ef1ad7b20db50b823ac44cdc63dc928515e495cbb32c9d25c26701346104678`。真实 Provider 发送 0。
- 限制：地点卡只显示既有地点正文摘要，不创建或补写事实；创建新地点仍复用资料库正式流程，本卡不另造简化入口。多张局部地图以明确列表选择，资料关联不自动决定空间父子关系。旧底图坐标歧义、R2 真实请求未形成提案、复杂标签避让和作者独立体验验收均保持未完成。本轮未 push、合并、部署、启停日常服务或读写正式作品。

## 地图进入天意的创作起步 R5 实施计划（2026-09-13 +08:00）

- 布局与复用：复用 `TianyanShellRuntime` 按作品／模式持久化的草稿、现有 Tianyi 会话与 Grounded Answer 请求链，将地图来源、明确引用和输入器提到 Work 首屏；工作范围、修订、几何与本地夹具收入可展开区。
- 新增边界：引用条目可查看来源、可移除且不静默超额；空草稿提供三条只填充不发送的起步建议。不新建会话、候选或事实 Owner。
- R4 定向修正：局部图空间位置与导航入口分开表达，地图切换清除上一张图的短期操作提示；地点卡的简介、局部图主操作和管理操作降噪。既有坐标不迁移。
- 验收：用同一隔离作品连续完成局部图往返、地图进天意、查看／移除引用、建议填草稿、往返恢复、本地夹具成功与失败两种状态；在 1440×900 和 1152×720 检查首屏输入、返回入口和结果。Provider dispatch 固定为 0。
- 回滚：Tianyi 首屏与引用交互、地图入口／状态文案和测试证据分开提交，可逐个 revert；隔离测试作品单独删除，代码回退不会删除已有作品、引用对象或地图修订。
