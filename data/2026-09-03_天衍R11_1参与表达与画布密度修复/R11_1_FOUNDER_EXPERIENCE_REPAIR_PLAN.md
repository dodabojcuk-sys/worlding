# R11.1 创始人体验修复计划

基线：`28a2b1998fc95ad27a2179933deb6a882efc4605`；范围仅为事件观察的表达与空间修复。

1. 保持同一参与投影与 Event / Owner / evidence refs，新增受兼容约束的 `trajectory` 与保留的 `matrix` 渲染方式；旧 R11 状态安全迁移。
2. 将对象选择器改为按需展开的面板，已选对象仅以紧凑 chip 常驻；详情 Dock 打开时自动收起。
3. 轨迹成为叙事顺序 × 参与的默认阅读表达：共享事件列、对象连续轨迹、未知默认弱化、明确缺席独立可辨；矩阵继续提供四态审计。
4. 复用同一 Event refs 做轻量事件脊柱，保持当前事件、可视范围与详情联动；不建立第二事件列表或事实源。
5. 更新术语、测试、视觉证据和 R11 文档表述；全程不调用 Provider、不写正式事实、不改 Owner、迁移或依赖。
