# 天衍工程重整工作日志

## 2026-08-26｜源码基线导入

- 从 `TIANYAN_TRACKED_SOURCE_R0_e6684922.zip` 导入运行、构建、服务端和测试代码，产品入口为 `apps/story-studio`。
- 根目录 `TIANYAN_PRODUCT_CORE.md` 保持为唯一产品核心，未导入源码包中另一份产品核心草案。
- 排除状态报告、决策记录、变更日志、历史证据、截图及其清单。
- 源码包完整性检查通过，未发现路径穿越风险；保留原包自带的 4 处非功能性空白告警。
- 项目要求 Node 22，而当时环境只有 Node 24，因此尚未安装依赖或运行完整验证。

## 2026-08-27｜工程审计与整改规划

- 状态：已完成首轮静态审计，尚未进入代码重构。
- 范围：核心文档、Pi Agent 底座、代码架构、文件定位、功能完成度、UI、上市门槛和下载资料取舍。
- 原则：只保留能支持代码、验收或追溯的材料，不把资料摘要继续堆入工程。

### 主要发现

- 产品核心确定七大空间，现有代码却把“数据”做成第八个一级空间，属于必须先裁决的产品漂移。
- Pi Agent 已接入，但只是局部模型/工具调用适配层；规划、状态、审批、提供商和技能运行仍有多套路径。
- 最大生产文件包括 6173 行的 `App.tsx`、4180 行的工作区操作层、3354 行的服务器入口和 2808 行的本地传输层，导航、状态、业务调度与传输边界过度集中。
- 生产路径仍包含 fixture、prototype 和 legacy 实现；功能数量大，但首发范围和可用闭环没有被锁定。
- 当前是本地 Web 研发原型：没有安装包、签名、自动更新通道，导入导出界面明确标注尚未实现。
- UI 问题的根因不只是样式，而是工作空间、路由、侧栏、快捷助手和大量功能同时竞争注意力。
- 项目要求 Node 22，当前机器只有 Node 24；因此本轮未在错误运行时下安装依赖或声称验证通过。

### 下载资料处理

- 保留源码 ZIP，直到 Node 22 环境下完成全量验证。
- 保留历史证据 ZIP 作为单一压缩归档，不导入项目。
- 已将与根目录完全相同的下载核心 MD、以及含重复层级和 macOS 垃圾文件的证据解压目录移入系统回收站，可恢复。

### 建议执行顺序

1. 先决定七/八空间、首发形态和唯一黄金闭环，冻结其他表面功能。
2. 建立 Node 22 可重现基线，运行十条规定脚本，将实际失败作为整改起点。
3. 将 Pi Agent 收敛为唯一 Agent 执行内核，但不让它拥有 Canon、Event 或作者确认权。
4. 按既有业务责任拆分超大文件，合并重复运行路径，把 fixture/prototype/legacy 移出生产路径。
5. 围绕黄金闭环重做交互层级，再做发行、数据迁移、备份恢复、隐私和性能硬化。

### 项目目录导航

- 在根目录建立唯一 `项目目录导航.md`，分开产品核心、工程规则、代码、测试、技术契约、日常日志和外部归档。
- 导航记录主运行链、目录责任、唯一写入所有者、常见改动影响和最低验证。
- 将超大入口、七/八空间冲突、fixture/prototype、`src/` 根级过渡文件和过期 Git 预检标为现有例外，禁止后续照此新增。
