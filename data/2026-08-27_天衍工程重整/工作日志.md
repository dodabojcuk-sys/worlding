# 天衍工程重整工作日志

## 2026-08-26｜源码基线导入

- 从 `TIANYAN_TRACKED_SOURCE_R0_e6684922.zip` 导入运行、构建、服务端和测试代码，产品入口为 `apps/story-studio`。
- 根目录 `TIANYAN_PRODUCT_CORE.md` 保持为唯一产品核心，未导入源码包中另一份产品核心草案。
- 排除状态报告、决策记录、变更日志、历史证据、截图及其清单。
- 源码包完整性检查通过，未发现路径穿越风险；保留原包自带的 4 处非功能性空白告警。
- 项目要求 Node 22，而当时环境只有 Node 24，因此尚未安装依赖或运行完整验证。

## 2026-08-27｜工程审计与整改规划

- 状态：已完成首轮静态审计，尚未进入代码重构。
- 范围：核心文档、Pi Agent 底座、代码架构、文件定位、功能完成度、UI、上市门槛和下载资料取舍。
- 原则：只保留能支持代码、验收或追溯的材料，不把资料摘要继续堆入工程。

### 主要发现

- 产品核心确定七大空间，现有代码却把“数据”做成第八个一级空间，属于必须先裁决的产品漂移。
- Pi Agent 已接入，但只是局部模型/工具调用适配层；规划、状态、审批、提供商和技能运行仍有多套路径。
- 最大生产文件包括 6173 行的 `App.tsx`、4180 行的工作区操作层、3354 行的服务器入口和 2808 行的本地传输层，导航、状态、业务调度与传输边界过度集中。
- 生产路径仍包含 fixture、prototype 和 legacy 实现；功能数量大，但首发范围和可用闭环没有被锁定。
- 当前是本地 Web 研发原型：没有安装包、签名、自动更新通道，导入导出界面明确标注尚未实现。
- UI 问题的根因不只是样式，而是工作空间、路由、侧栏、快捷助手和大量功能同时竞争注意力。
- 项目要求 Node 22，当前机器只有 Node 24；因此本轮未在错误运行时下安装依赖或声称验证通过。

### 下载资料处理

- 保留源码 ZIP，直到 Node 22 环境下完成全量验证。
- 保留历史证据 ZIP 作为单一压缩归档，不导入项目。
- 已将与根目录完全相同的下载核心 MD、以及含重复层级和 macOS 垃圾文件的证据解压目录移入系统回收站，可恢复。

### 建议执行顺序

1. 先决定七/八空间、首发形态和唯一黄金闭环，冻结其他表面功能。
2. 建立 Node 22 可重现基线，运行十条规定脚本，将实际失败作为整改起点。
3. 将 Pi Agent 收敛为唯一 Agent 执行内核，但不让它拥有 Canon、Event 或作者确认权。
4. 按既有业务责任拆分超大文件，合并重复运行路径，把 fixture/prototype/legacy 移出生产路径。
5. 围绕黄金闭环重做交互层级，再做发行、数据迁移、备份恢复、隐私和性能硬化。

### 项目目录导航

- 在根目录建立唯一 `项目目录导航.md`，分开产品核心、工程规则、代码、测试、技术契约、日常日志和外部归档。
- 导航记录主运行链、目录责任、唯一写入所有者、常见改动影响和最低验证。
- 将超大入口、七/八空间冲突、fixture/prototype、`src/` 根级过渡文件和过期 Git 预检标为现有例外，禁止后续照此新增。

## 2026-08-27｜P0 可重现基线与 M0 纵向切片核验

### 交付摘要

- `VERDICT`: **BASELINE_FAIL / M0_NOT_CLOSED**。当前仓库能在合规 Node 22 下安装、构建、类型检查、静态约束和运行大部分测试，但 `dev`、`test:unit`、`test` 和 `verify` 未通过；M0 在“天意事件候选交给现有 Event/CandidateReview 所有者”处断开，不能声称端到端闭环。
- `BASE_COMMIT`: `b646db9e1ca46cf90deb90b5ccc0fa00a19c9bac`。
- `WORKTREE_STATUS`: 核验开始和追加本日志前均为 `master` 单一 worktree、无 staged/unstaged/untracked 变更；无 remote。仓库日期目录与本阶段实际开始日一致，不需要改名建议。
- `NODE_RUNTIME`: 系统现有 Node `v24.16.0` / npm `11.13.0` 未被替换；隔离基线使用 Node `v22.23.2` / npm `10.9.8`，满足 `.nvmrc=22`、`engines.node >=22 <23`、`engines.npm >=10 <12` 和 `packageManager=npm@10`。
- `DEPENDENCY_INSTALL`: 官方 Linux x64 Node 包 SHA-256 验证通过；隔离 `npm ci --no-audit --no-fund` 退出码 `0`，新增 171 个包，热缓存执行 1.53 秒，峰值 RSS 460500KB，`node_modules` 245MB，npm 缓存 46MB。首次冷缓存下载约 11.85 秒。为 E2E 只在隔离 HOME 安装 Playwright FFmpeg v1011，退出码 `0`，下载 2.3MiB。
- `FIRST_BLOCKER`: 按规定脚本顺序，首个阻断是 `npm run dev` 退出码 `1`：`Unexpected Story Studio branch: master.`。单元测试中首个失败是 `creationPluginLifecycle.test.ts` 将 cwd 硬编码为不存在的 `/Users/m4-zhi/Documents/codex-workspace/天衍2`，导致 `spawn ... ENOENT`。
- `FILES_CHANGED`: 只有 `data/2026-08-27_天衍工程重整/工作日志.md`。未改生产代码、产品核心、依赖、lockfile 或导航。
- `COMMIT`: 本节追加完成后只允许一个纯日志提交；最终哈希以 Git 交付回执为准，不在被提交内容中自引用本提交哈希。
- `PROVIDER_CALLS`: `0`。所有测试使用 Mock、本地伪服务器或确定性运行时；E2E 自证 `realProviderCalls: 0`。未读取真实密钥或用户作品。

### 仓库真相

- `package.json`、`package-lock.json`、`.nvmrc` 的 SHA-256 分别为 `f42cfac81d29ccc656bef5aecb633e690319b18325419c9a1c45ba022fcbdf90`、`7182d75ed5728765db9fbf27ac265101c97373eb4c870cd0360c24a4e882e86f`、`f14b4987904bcb5814e4459a057ed4d20f58a633152288a761214dcd28780b56`；隔离安装前后未变。
- lockfile 为 v3，共 222 个 package 记录。安装前根仓库没有 `node_modules`，隔离产物也未带回根仓库。
- 目录导航列出的关键文件均存在，行数与当前仓库一致：`App.tsx` 6173、`server.mjs` 3354、`localTransport.ts` 2808、`storyStudioWorkspaceOperations.ts` 4180、`storyStudioAuthorControl.ts` 2577、`tianyiAgentRuntimePort.ts` 506、`FEATURE_INDEX.json` 308。
- lint 实际验证 21 项功能索引，且 Canon Writer、WorldState owner、Event owner 各唯一。
- 产品核心规定七个顶级空间；`storyStudioWorkspaceRegistry.ts` 仍明文注册第八个 `data` / `/data` 空间。这是已证实的产品漂移，本轮没有修改。

### TEST_MATRIX

| 实际命令 | 退出码 | 通过 / 失败 | 首个失败或结果 |
| --- | ---: | ---: | --- |
| `npm run dev` | 1 | 0 / 1 | 旧预检拒绝 `master`；未启动 server/Vite。 |
| `npm run build` | 0 | 1 / 0 | Vite 构建成功；2092 modules，只有 >500kB chunk 警告。 |
| `npm run serve` + HTTP 就绪轮询 + SIGTERM 清理 | smoke 0；服务进程 143 | 1 / 0 | `GET /` 返回 200；不是超时，就绪后主动终止。 |
| `npm run typecheck` | 0 | 1 / 0 | TypeScript `--noEmit` 通过。 |
| `npm run lint` | 0 | 1 / 0 | 10 脚本、21 功能与退役源约束通过。 |
| `npm run test:unit` | 1 | 948 / 3（951 tests，166 files） | #344 首先失败：跨机硬编码 cwd；另有 Linux Codex 路径探测失败和旧预检失败。 |
| `npm run test:integration` | 0 | 33 / 0（8 files） | 全部通过。 |
| `npm run test` | 1 | 948 / 3 | `test:unit` 失败后短路，`test:integration` **未在此命令中运行**。 |
| `npm run test:e2e`（未配浏览器） | 1 | 0 / 0 | 预检阻断：未自动发现 Linux Chrome；不记 PASS。 |
| `PLAYWRIGHT_CHROMIUM_EXECUTABLE=/usr/bin/google-chrome npm run test:e2e`（FFmpeg 未安装） | 1 | 0 / 0 | 运行至录像阶段后因隔离 HOME 缺 FFmpeg 失败；不记 PASS。 |
| `npx playwright install ffmpeg` | 0 | 1 / 0 | 只写入隔离 HOME 缓存。 |
| `PLAYWRIGHT_CHROMIUM_EXECUTABLE=/usr/bin/google-chrome npm run test:e2e`（最终复跑） | 0 | 1 / 0 | E2E PASS；29 张截图、2 个视频、失败请求 0、真实 Provider 0。 |
| `npm run verify` | 1 | 前置 typecheck/lint 通过；unit 948 / 3 | 在 `test:unit` #344 处首次失败；integration、E2E、build **未在 verify 中运行**。 |

三个稳定单元失败：

1. `tests/storyCreation/creationPluginLifecycle.test.ts:356` 使用另一台机器的绝对 cwd，导致 spawn `ENOENT`。
2. `tests/storyIntelligence/nuwaCodexBackend.test.ts:19` 期待 `execAvailable=true`，但 `findKnownCodexExecutable()` 只查找 macOS `/Applications/ChatGPT.app/.../codex`，本机实际 Codex 在 `/usr/lib/chatgpt/resources/codex`。
3. `tests/storyStudio/storyStudioCanonicalPreflightR1.test.ts:9` 依赖旧分支和旧 base，在当前 `master` 上失败。

### STALE_PREFLIGHT_FINDING

- `scripts/verify-story-studio-canonical.mjs` 绑定了两个已过时分支：`codex/tianyan-founder-target-integration-r0` 和 `codex/tianyan-tianyi-primary-workspace-recovery-r0`；绑定的 accepted base `ed4981c31722cfc57e706c34a5a9f696b1ae614b` 在当前仓库中不存在。
- 仍有价值的安全不变量：必须在仓库根执行、拒绝 archive/successor 路径、拒绝 linked worktree/共用 common dir、必须证明可接受的祖先链。
- 已失效的是仓库身份假设：旧分支白名单和缺失 base，而不是上述安全不变量。
- 最小可验证修复方案（本轮未执行）：保留 cwd/path/common-dir/ancestry 检查，将祖先锚点更新为本仓库已存在的源码导入基线 `a86f64cd9c527b06980b5934759feadedb3cdc19`；由项目明确批准当前 canonical 分支策略后替换旧白名单。用临时独立 Git 仓库测试“当前分支+新 base 通过”，以及错误 cwd、archive/successor、linked worktree、detached/未批准分支、缺失或非祖先 base 全部 fail closed。

### PI_PACKAGE_REALITY

- 直接依赖是 `@earendil-works/pi-agent-core@0.84.2`，lockfile 来源为 npm registry；integrity 为 `sha512-8Pn3wSCxj0cfo5I6jxQYVB/3uuQRmHhAlEclyjqpOuMEdQMIODHizRogv56FLdbU+dTiGnybeHQ2N+sV1/L2YA==`，要求 Node `>=22.19.0`。
- 间接包是同版本 `@earendil-works/pi-ai@0.84.2` 和 `@earendil-works/pi-telemetry@0.84.2`。没有安装旧的非 scoped `pi-agent-core` 包；lockfile 中的 `pi-ai` 是 scoped 包暴露的 bin 名，不是第二份包。
- 调用入口在 `apps/story-studio/server/server.mjs` 动态导入 Pi Agent/Pi AI，由 `src/storyAgent/tianyiAgentRuntimePort.ts` 封装为 `pi.agent-core` adapter。
- 当前并非“Pi 唯一运行路径”：Pi 直接调 `streamSimple`；天意创意提取、grounded answer 和 server minimal inference 另有 `ProviderGateway.openChatStream/openChatCompletion`；女娲还有 deterministic/external/Codex backend；Skill Runtime 也是独立路径。
- Pi 运行时实际只向 Agent 暴露 `read_context_manifest`；声明的事件候选等工具没有在该 Pi Agent 工具链执行。Pi 返回的自由文本最终被解析为一个 `unknown/candidate-only` 候选；当前丰富的角色/地点候选来自 fixture，而 fixture 不生成事件候选。

`PI_BOUNDARY_VERDICT`: **边界方向正确，实际收敛未完成**。

```text
Tianyi / Nuwa policy
  -> Tianyan AgentRuntimePort（上下文、权限、状态机、回执）
  -> Pi Adapter（模型循环、工具调用协议、流式输出、取消/重试）
  -> ProviderGateway / Tools（外部能力边界）
  -> Candidate Receipt（天衍格式、来源、不确定性）
  -> AuthorControl（候选/正式事实之间的唯一门）
```

Pi 可持有的是可替换的 Agent 循环与工具协议；天衍必须继续持有 Canon、Event、WorldState、来源、候选状态、上下文策略、权限、持久化与作者确认权。Tianyi/Nuwa 是基于 port 的策略，角色/物品/地点 Agent 不等于常驻 Pi Agent 实例。

### M0_VERTICAL_SLICE_REALITY

| M0 步骤 | 实际状态 | 唯一所有者 / 入口 | 持久化 | 测试与断点 |
| --- | --- | --- | --- | --- |
| 一句故事想法 | **已真实实现** | `Tianyi` UI -> `captureTianyiCreativeAuthorSource()` -> Interaction Archive owner | `<project>/continuity/agents/<tianyi>/sessions/<session>.jsonl`，含来源 event/hash | `tianyiCreativeAuthorSourceCaptureR0`、`tianyiCreativeSessionLifecycleR0` 覆盖。 |
| 天意识别带来源的角色和事件候选 | **fixture / 无法在 Provider=0 下验证真实推理** | 创意提取由 `tianyiSessionOperations` 直接调 ProviderGateway，不是 Pi；候选自带 session/event/hash 来源 | 仍追加到同一 Session JSONL | 真 Provider 路径理论支持 character/event；无 Provider 默认 fixture 只生成 `unknown`，Pi fixture 生成 character/location/unknown 但无 event。`ProviderLoopR2` 是本地伪流。 |
| 作者编辑候选 | **已真实实现** | `editTianyiCreativeCandidate()`，乐观修订检查 | Session JSONL 追加 edit event | `tianyiCreativeGoldenLoopR0` 覆盖 fixture 输入下的 UI/持久化。 |
| 作者确认角色候选 | **已真实实现，但局部** | Tianyi handoff -> `agent-recognition-proposal` -> 既有资料/Agent 审核所有者 | `.world-os/story-intelligence/agent-recognition-proposals/proposals.json`，确认后写正式对象 | `tianyiCreativeOwnerHandoffR0`、proposal repository tests；当前安全 handoff 主要适配 character/item/location，其他类型未齐。 |
| 事件候选进入事件线 | **缺失，是 M0 第一个结构断点** | 候选映射为 `candidate-review`，但 `handoffTianyiCreativeCandidate()` 只接受 `agent-recognition-proposal`，对 event 返回 409 | 事件候选只停留在 Session JSONL，未进入 Event owner | 无 Tianyi-event handoff 回归。另一条手工 `normalEventCreationPort` 能真实经 CandidateReview -> ImpactReview -> AuthorChangeSet 生成已确认 Event，但它与天意候选未连接。 |
| 建立一次有界女娲推演 | **prototype / fixture** | `NuwaPrimaryWorkspace` + deterministic scene runtime；另有受环境开关保护的 `NuwaBoundedScenarioWorkspace` fixture | `.world-os/runs/nuwa/<runId>/...`，scene ledger 为 append-only JSONL | runtime 有 `nuwaSceneSimulationRuntimeR0`；默认 UI 的场景、角色和操作仍是固定开发夹具，不是上一步事件的连续输入。 |
| 作者查看差异并选择性融入 | **fixture 上真实使用正式门禁** | Nuwa candidate -> CandidateReview -> ImpactReview -> AuthorChangeSet -> 唯一 Event owner | candidate reviews: `.world-os/author-control/candidate-reviews`；impact reviews/change sets/explorations 在同一 author-control 根下 | bounded fixture 和 E2E 证明候选不会预写 Canon，作者确认后 Event 持久化；但输入不是本 M0 的天意事件。 |
| 生成可追溯的中立 Markdown 故事包 | **生成器真实，完整交付缺失** | `creationSourceSelectionPort` -> `buildNeutralStoryPackage()`，只接受可验证的已确认 Event/StoryUnit/WorkVersion | OutputArtifact 与 WorkVersion 绑定会持久化；完整 `story.md + manifest.json + provenance.json + projections/*` 文件集未在生产路径落盘 | `neutralStoryPackageAndAdapters`、`workVersionBoundCreationOutputArtifactR0`、`normalProjectWorldEventCreationGoldenLoopR0` 覆盖构建/来源绑定；`packageFiles()` 只被测试调用。 |

### NEXT_SMALLEST_IMPLEMENTATION_SLICE

下一切片不应该是全局重构或 Pi 升级，而是补上一条最短的领域桥：

1. 在现有 `handoffTianyiCreativeCandidate()` 应用协调层支持 `kind=event + targetOwnerKind=candidate-review`，不让 Pi 或 Session 仓库取得 Event 写权。
2. 将已编辑的标题、摘要、不确定性和 session/event/hash 来源，交给现有 `normalEventCreationPort` 所使用的 Planning Event + CandidateReview 所有者链；在 Tianyi Session 只追加 owner receipt。
3. 让作者沿现有 ImpactReview -> AuthorChangeSet -> Event owner 确认或拒绝，不新建第二套事件库或第二套审核状态机。
4. 最小回归必须覆盖：来源锚点原样保留；编辑后 handoff 幂等；拒绝写入 0；确认后唯一 Event 及 Event Line 投影；重启后可恢复；全程 Provider 0。

这一切片完成后，再把该已确认 Event 作为有界 Nuwa Run 的真实输入；不应在事件 handoff 仍断裂时先扩大女娲或重做 UI。

## 2026-08-27｜P0.1 可移植基线定向修复

### 交付摘要

- `VERDICT`: **TARGETED_REPAIRS_PASS / RELEASE_GATE_STOPPED_AT_NEW_E2E_FAILURE**。规定的三个稳定 unit 失败已全部消失，unit、integration、组合 test、build、dev/serve smoke 均通过；E2E 暴露了四类允许修复之外的新产品行为失败，已依停止条件不再修复、不运行 `verify`。
- `BASE_COMMIT`: `6ac7a657a6f640544b1e7579f017428db13c35ae`；起点为 `master`、clean、单 worktree、无 remote，与预期完全一致。
- `NODE_RUNTIME`: 系统 Node 24 未替换；全部实验使用隔离 Node `v22.23.2` / npm `10.9.8`、隔离 HOME、npm cache 与运行数据根。
- `DEPENDENCY_INSTALL`: `npm ci --no-audit --no-fund` 退出码 `0`，新增 171 个包；未升级依赖，`package.json` / `package-lock.json` 未修改。
- `PROVIDER_CALLS`: `0`。未启动真实 Codex，未读取密钥或用户作品；测试只使用 Mock、本地伪服务器、确定性运行时和临时假可执行文件。

### 定向修复结果

1. **Canonical 预检**
   - 只批准 `master`，删除两个过期 `codex/*` 白名单；accepted base 更新为已存在且为当前 HEAD 祖先的源码导入基线 `a86f64cd9c527b06980b5934759feadedb3cdc19`。
   - 保留 fail-closed：非仓库根、archive/successor 路径、linked worktree/共享 common-dir、detached HEAD、非批准分支、base 缺失或非祖先均拒绝；当前 `master` 且祖先链成立时通过。
   - 直接测试全部使用临时独立 Git 仓库，5 / 5 通过；真实仓库根预检亦 PASS。
2. **跨机器 cwd**
   - 删除 macOS 用户绝对路径，改为从测试文件 `import.meta.url` 解析并 `realpath` 的仓库根。
   - 复用既有 bounded process teardown helper；启动失败、预期拒绝服务和正常服务均有有界 SIGTERM/SIGKILL 收尾。测试后递归恢复只读 fixture 权限并删除临时根，成功复跑后近期遗留目录数为 `0`。
   - 定向测试 11 / 11 通过，未修改被测产品语义。
3. **Codex 可执行文件发现**
   - 解析优先级为：显式 `codexExecutable` -> 既有 `WORLD_OS_NUWA_CODEX_PATH` -> 受控 `PATH` 中的 `codex` -> 平台已知候选。
   - 保留 macOS ChatGPT 路径，Linux `/usr/lib/chatgpt/resources/codex` 只是 fallback，不是唯一答案。空路径、不存在、非普通文件、不可执行和错误显式配置均返回不可用，不降级到其他来源误报可用。
   - 单元测试使用临时假可执行文件与受控 PATH/配置，4 / 4 通过，进程调用计数为 `0`。

### TEST_MATRIX

| 实际命令 | 退出码 | 通过 / 失败 | 首个失败或结果 |
| --- | ---: | ---: | --- |
| `npm ci --no-audit --no-fund` | 0 | 171 packages / 0 | 使用 Node 22 隔离 HOME/cache。 |
| canonical 定向测试 | 0 | 5 / 0 | 所有通过与拒绝矩阵通过。 |
| creation lifecycle 定向测试 | 0 | 11 / 0 | 跨平台 cwd、进程和目录清理通过。 |
| Codex backend 定向测试 | 0 | 4 / 0 | 受控假可执行文件，真实 Codex 调用 0。 |
| `npm run typecheck` | 0 | 1 / 0 | TypeScript `--noEmit` 通过。 |
| `npm run lint` | 0 | 1 / 0 | 10 scripts、21 features 及唯一 owner 约束通过。 |
| `npm run test:unit` | 0 | 956 / 0（166 files） | 三个原稳定失败全部消失。 |
| `npm run test:integration` | 0 | 33 / 0（8 files） | 全部通过。 |
| `npm test` | 0 | unit 956 / 0；integration 33 / 0 | 组合命令实际运行了 integration。 |
| `npm run build` | 0 | 1 / 0 | 2094 modules；只有 >500kB chunk 警告。 |
| `npm run dev` 首次 smoke | 1 | 0 / 1 | 现有用户服务占用 Vite 4191；未终止该服务，本轮进程已清理。 |
| `PORT=4398 STORY_STUDIO_VITE_PORT=4399 npm run dev` + HTTP 就绪 + SIGTERM | smoke 0 | server 1 / 0；Vite 1 / 0 | 4398/4399 均就绪，预期 SIGTERM 清理不计失败。 |
| `PORT=4400 npm run serve` + HTTP 就绪 + SIGTERM | smoke 0 | 1 / 0 | 4400 就绪后安全终止。 |
| `npx --no-install playwright install ffmpeg` | 0 | 1 / 0 | FFmpeg v1011 显式安装到隔离 Playwright 目录。 |
| `PLAYWRIGHT_CHROMIUM_EXECUTABLE=/usr/bin/google-chrome npm run test:e2e` | 1 | 0 / 1 | Chrome 151 和 FFmpeg 前置均就绪；第 140 行等待“原工作上下文已失效，已返回当前作品。”30s 超时。 |
| `npm run verify` | **未运行** | 不得记 PASS | E2E 新产品行为失败已触发明确停止条件。 |

### 停止与后续边界

- `NEW_FAILURES`: `apps/story-studio/scripts/story-studio-golden-loop-r0-smoke.mjs:140` 的失效项目上下文恢复提示未出现。这不是 Chrome/FFmpeg、Node、依赖或端口前置失败，而是新产品行为断言失败。本轮未诊断或修改该产品路径。
- `VERIFY_RESULT`: **NOT_RUN_DUE_TO_STOP_CONDITION**，不能声称恢复可信全绿基线。
- `FILES_CHANGED`: canonical 预检及直接测试、creation lifecycle 测试、Codex executable resolver 及直接测试、本工作日志；未修改产品核心、UI、Pi 行为、事件线、女娲、`package.json` 或 lockfile。
- `COMMITS`: 提交 1 为 `d9b2138e261080d8fb435b85dde2ad4003d38286`（canonical 预检）；提交 2 在本日志与剩余限定修复同步提交后以 Git 回执为准，不在提交内容中自引用其哈希。
- `ROLLBACK`: 若需回滚，按逆序对两个提交执行 `git revert <commit>`；不使用 reset、rebase 或覆盖式回退。
