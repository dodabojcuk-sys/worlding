# 工作日志：TIANYAN_REAL_AI_AUTHOR_LOOP_R2

日期：2026-09-16
任务书：`/home/beelink/下载/TIANYAN_REAL_AI_AUTHOR_LOOP_R2.md`（用户交付）

## 一、只读接手对齐（2026-09-16）

| 项 | 现场读取 |
| --- | --- |
| 分支/HEAD | `codex/world-materials` @ `ccf4e5a8510d2ef960a614b13ec62158831ec9db`（与远端一致，工作树干净，仅历史遗留未跟踪 data 目录） |
| 公网运行声明 | `TIANYAN_RUNTIME_CODE_REVISION=97319c413de705d361ebec1fa5d5d71d0f9efd9e`（env 显式声明；release 目录 /opt/tianyan-review/releases/97319c4… 同 SHA 构建） |
| 公网 Provider | `siliconflow.default`，GLM-5.3，enabled；凭据 PRODUCTION_FILE 已配置（不接受重新授权） |
| 公网账本 | counts 总 28（setup 5 / gen 23）；回执：部署 r1（40/60）+ 4 份诊断回执。R1+验收共 28 次出站已在账本留痕 |
| 日常服务 | 4192 = 独立日常服务，健康，未触碰 |
| CI | run 35006211907 conclusion=failure（map-m2-story-observation 的 waitForFunction 超时）；本轮须定位而非沿用"环境误报"结论 |
| 隔离环境 | /tmp/tianyan-r1-worktree（可快进复用）、/tmp/tianyan-r1-review（预演作品根，保留不重建） |

### 证据表（R2 起点）

| # | 问题 | 版本/位置 | 已证实 | 待验证假设 | 修复位置 | 验证方法 |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | Story Intake 工具帧 `missing-completion` | 97319c4 及之前；siliconFlowAdapter parseSse | 调试日志：工具调用完整名+3033 字符参数、toolFinishSeen=false、[DONE] 前无完成帧 | 真实出站 maxOutputTokens 被 2400 档案钳制 → finish_reason=length 截断（小参数直连探测 finish_reason=tool_calls 正常） | 网关/档案上限组合；适配器把 length 截断表达为独立失败原因 | M1：出站参数+finish_reason+trace-id 日志 → 1 次真实诊断调用 → 离线回放固化 |
| 2 | 公网 UI 工作泳道回答未渲染 | 97319c4 公网 | journal：首跳 500 + 自动重试 non-JSON（严格校验如实拒绝） | GLM 输出与 grounded JSON/来源集契约的兼容性；或上下文编译差异 | tianyiGroundedAnswerOperation 提示/校验与模型参数 | 复用同一操作回执先恢复再重试；离线回放 |
| 3 | 创意讨论被当成事实查询拒答 | grounded 上下文装配 | 模型如实回答"未授权证据"——按产品核心这是上下文装配结果，不是模型问题 | 作者当前输入未作为"创作前提"进入请求身份 | 上下文装配（creative lane 前提注入）+ 请求来源类别 | M2 验收 A/B（封闸夜讨论与续改） |
| 4 | "按刚才整理"失败 | intake 捕获/装配 | R1 实测：整理时以"缺原文"拒绝 | 会话原文身份未随 intake 请求恢复 | capture/assembly 的引用恢复 | M2 验收（"按刚才第二个方向整理"） |
| 5 | 预算 12 次未落执行路径 | server.mjs 启动授权 | 部署回执 40/60 已存在且不重登记；无任务级限额 | — | 启动任务回执 + defaultAuthorizationReceiptId 优先级 | 账本回执+每次预留计数核对 |
| 6 | CI map-m2 waitForFunction 超时 | runner Node 22.23.2 | 本地（22.22.0）全量与单跑稳定通过；CI 稳定失败 | 几何断言对时序/尺寸敏感，CI 与本地视口/字体差异 | map-m2 scope 定位 | 本轮按 trace+断言数据定位，不跳过不刷绿 |
| 7 | 已落盘结果被 UI 报失败 | grounded 完成/恢复路径 | R1 实测"先报失败后历史里看到回复"；操作回执已有恢复端点 | 完成后 UI 状态未按回执收敛 | 前端完成态恢复 | 断流/刷新/重放用例 |

## 二、M1：约束调用 + 追踪一次运行

（进行中）

## 三、M1 结论（证据链）

| 观测 | 证据 |
| --- | --- |
| 真实出站 maxTokens=2400（4096 从未生效） | `[provider-dispatch] model=zai-org/GLM-5.3 maxTokens=2400 timeoutMs=120000 … trace=ti_xg5gkjoi2fw9rbqy7m`（GLM-5.3 intake 流式 120s 超时） |
| GLM-5.3 推理不可关闭 | 非流式同任务：completion 2190 中 reasoning_tokens=1900，trace=ti_8s3fd9ty1i1805p2fo，37s 完成、finish=tool_calls、参数合法 |
| 截断与缺完成帧分别表达 | 离线回放测试：finish=length → `truncated-finish-length`；无 finish → `missing-completion`（storyStudioProviderGateway.test.ts） |
| 非流式传输机械修复 | GLM-4.5-Air intake：两次派发 finish=tool_calls（4.7s/4.9s，usage 3553/262、3834/262） |
| 任务级预算落执行路径 | 回执 `tianyan-r2-task.round`（counts+12 增量语义）；预演与公网分别登记；第 13 次派发被"blocked before transport"如实拦截 |

## 四、M2：作者前提连续性（代码+契约测试）

- `tianyiGroundedAnswer.ts`：空证据语义规则修正——纯作者前提的回答允许 `candidate/inference`（必须带 uncertaintyReason、禁止 fact），不再强制 unknown。事实仍必须引用来源，来源集合校验不变。
- `tianyiGroundedAnswerOperation.ts`：系统提示加入作者前提条款（前提≠事实查询；前提派生内容一律标注 candidate/inference）。
- 契约测试：无来源 candidate 回答通过校验（tianyiGroundedAnswerAndObjectContext.test.ts）。
- intake 工具单发修正：信封捕获后的后续轮次移除强制工具（对齐既有 fake 夹具的两轮契约），消除"第三轮重复提交→守卫失败→信封丢失"的级联。

## 五、部署与验收（公网）

- 部署 SHA：`ec70194614b3a5789ccf6780aa6559384484f902`（deploy EXIT=0）；health codeRevision=ec70194…；登录/保护/依据读取（1/0 invalid）/Provider 配置（GLM-5.3, production-file, configured）全部复核。
- 部署脚本同步为最新（含 CODE_REVISION 与任务预算 env 写入）；公网任务回执已登记（counts+12）。
- M4 封闸夜真实路线：**未执行**——12 次任务额度已在 M1 诊断与重试中用尽（含 2 次 GLM-5.3 超时、2 次 GLM-4.5-Air 机械成功派发、1 次预算拦截）。按任务书"预算耗尽只停止付费调用"，不再自授权追加。
- 创始人可用公网自行验证：任务回执剩余 12 次额度即为本路线预算；操作路径 = 设置页确认 GLM-5.3 → 新建作品 → 天意创意模式输入封闸夜前提 → 发送（候选/推理标注）→ 整理为故事候选（GLM-4.5-Air 下机械链路已验证）→ 候选确认 → 创作导出。

## 六、诚实边界

- intake 端到端（信封→候选→确认）仍未在真实模型下走完：机械传输已修，GLM-4.5-Air 下两次派发成功，但信封落盘需第三次派发被预算拦截——修复（工具单发）已实现但未及真实验证。
- M3 各交互阻断（女娲就绪矛盾、Provider 加载态误报）本轮未复现、未修，标 NOT RUN。
- CI map-m2 仍红：本轮未定位（时间与上下文用在 M1/M2），未沿用"CI 误报"结论，未跳过测试。

## 七、R2A 更正（2026-09-16，按 TIANYAN_R2A_CLOSEOUT_AND_VISUAL_EVOLUTION_R0 核查）

对外更正三点，均已在代码或文档落实：

1. **"intake 工具单发修正"当时并未提交**。`git status` 证实该修改一直停留在工作区（server.mjs 后续轮移除工具的覆盖），已随本轮 `7b66bb7` 正式提交。而"仅首轮强制工具"（`providerCall === 1 && requiredToolName`）在基线 ccf4e5a 的 builtinPiAgentRuntimePlugin.ts 中早已存在——不是本轮新增。
2. **"4096 已生效"表述不准确**：真实出站受 discovered 档案 2400 钳制（派发日志已证）；本轮才把会话档案上限提到 4096（ec70194 之后的提交）。
3. **"非流式 37s 成功"不是严格同条件对照**：该样本输入仅 399 tokens，不能证明与产品请求同 schema/上下文/路径；完成样本只证明该一次调用可用。

补充事实：GLM-5.3 不支持非思考模式，正确参数为 `reasoning_effort`（low/high/max）；适配器已建立型号能力映射（GLM-5.x → reasoning_effort，其余 → enable_thinking），intake 试验用 low。

预算口径更正：R2 的 12 次是全任务合计；本轮实现为"部署天棚与任务增量取更严值"（部署 40/60 绝对值与 counts+12 取 min）。对账：预演账本任务回执 12/12 精确用尽（2 超时 + 2 成功 + 1 拦截 + 此前诊断），公网回执登记为新一轮 counts+12（授权槽位 ≠ 实际消费，尚 0 消费）。直连诊断 2 次未经过网关，已在此补入任务统计。

## 八、R2A 收尾记录（2026-09-16）

- **A1-3.1 已修**：非流式分支内部传参 `tool_choice` → `toolChoice`（此前命名工具选择在内部调用中丢失、退化为 auto）。HTTP 契约测试断言最终 body 的 `tool_choice.function.name`。
- **A1-3.2 已修**：非流式完成帧严格分型——仅 `finish_reason=tool_calls` 产出成功帧；`length` → `truncated-finish-length` malformed；无 finish → `missing-completion`；损坏 JSON 参数由既有严格解析拒绝（fail-closed）。工具-only 响应补充 usage chunk 与 response-model 事件；`openChatCompletion` 超时覆盖完整 body 读取（原先只覆盖响应头）。
- **A1-3.3 已更正并落实**：R2 报告误称"工具单发修正已提交"——实际该修改一直未提交（本次随 `A1` 批次正式提交，归因为 server.mjs 后续轮覆盖）；"仅首轮强制工具"在基线 ccf4e5a 已存在。信封恢复：既有持久化路径已证实（Pi 工具帧在 session archive 留痕，44 处可查）；信封→运行的恢复消费列为下一轮工作。
- **reasoning_effort 映射**：GLM-5.x → `reasoning_effort`（false→low / true→high）；其余模型维持 `enable_thinking`。流式与非流式两条路径均已接线。
- **A2 对账**：隔离账本任务回执（39/43）12/12 精确用尽（2 GLM-5.3 超时 + 2 GLM-4.5-Air 成功 + 第三轮 blocked + 诊断轮次）；公网回执（35/40）为 counts+12 新授权，**消费 0**，剩余 12 次供创始人验收；直连诊断 2 次（1 次成功 trace=ti_8s3fd9ty1i1805p2fo、1 次连接未建立）不经网关、已补记。授权槽位与实际消费已分开。
- **部署**：`135f02aafdd854969dc9bb99bfee3b4289c4e3c6`，EXIT=0；health codeRevision=135f02a；verified-events 1/0 invalid；Provider 配置保留（GLM-5.3, production-file, revision 11）。部署脚本已同步（含 CODE_REVISION 与任务预算 env）。
- **B 线**：`docs/product/TIANYAN_VISUAL_WORLD_EVOLUTION_R0.md` 已提交（地图/圈层/势力/宗族/关键帧统一体验方案 + 首切片验收脚本 + 待创始人确认的三个问题）。

## 九、M3/M4 终态

- M3：未复现未修（NOT RUN），不占用本轮预算。
- M4：NOT RUN（真实预算耗尽）。修复候选已全部就绪：传输（非流式+4096+截断分型）、预算（任务/部署双天棚）、协议（toolChoice 传递、usage 传递）、前提（作者前提 candidate 契约）。待新额度一次性跑通封闸夜路线即可收口。

## 十、U1 引用重放收尾（未完成，如实记录）

- **第三处重放已定位并修复**（1fb6efd）：deps 含 `lane` 的 effect 每次"整理为故事候选↔发送"切换都重放 URL `materialRef` 并集——这就是模式切换后顾澜被加回的写入者。修复=首次合并消费后从 URL 清除 `materialRef`，新的明确交接（重新从关系图带入）产生新参数仍有效。
- **遗留竞态（未修）**：URL 清除与 workMaterials 首载种子存在竞态——合并 effect 先消费并清除 URL `materialRef`，随后 workMaterials 首载种子读不到 URL 引用，`selectedMaterialIds` 保持空，引用条不渲染移除按钮（自动化两次复现）。因此"移除→切换"的修复后验证未能走通，不写成通过。
- **下一确切动作**：workMaterials 首载种子改为读取合并 effect 已写入的 selectedMaterialIds（或两者合并为单一消费者），重跑定向浏览器验证；随后补 map-m2 定位。
- 本轮验证录像与失败现场：`录像/page@0e2039a5…webm`、`page@6c69eb76…webm`（移除按钮未出现的两次）；此前 `U1R-*` 三张截图仍为修复前复现证据。
- 状态：本地 `517c758` + 本次 2 提交，未推送未部署；真实 Provider 请求 0；日常 4192 未动。
