# 工作日志

## 现场

- 基线：`codex/character-state-inspector-r0` @ `bfc551feb916b33c16c01a8386717db3542d57b5`
- 分支：`codex/character-agent-context-gateway-r0`
- worktree：`/home/beelink/.codex/worktrees/tianyan-character-agent-context-gateway-r0`
- 运行时：Node 22.23.2 / npm 10.9.8

## 实施顺序

1. fetch 并核对远端基线；创建持久 worktree，不修改主检出。
2. 阅读产品核心、目录导航、交接/代码导航/角色纵切文档和指定目标代码。
3. 先添加失败测试；首次失败为上下文闸门模块尚不存在。
4. 建立 `CharacterKnowledgeHandoff` 前置裁定、稳定 ID 绑定、`NuwaN1Context` 单角色纯装配和共享 Provider 安全投影。
5. 把既有 Nuwa attention 纯算法移到共享合同层，保留原 `storyIntelligence` 导出兼容；浏览器仍不直接依赖 `storyIntelligence`。
6. 在现有 `EntityInspectorDock → Agent 运行` 接入只读预览，不增加运行或保存入口。
7. 更新 FEATURE_INDEX；职责仍在既有 Owner 上，未新增事实 Owner，因此未改目录导航。
8. 运行类型、lint、unit、integration、build、full-shell/E2E 和定向回归。全 scope E2E 额外尝试中，前三个 scope 通过，既有 `nuwa-n1` scope 在当前 UI 已要求显式打开范围设置的情况下，仍直接等待 `.nuwa-n1-participant-options` 而两次超时；本分支未扩大修改该既有流程。
9. 用当前北溟夜航只读角色数据做 1440×900、1195×720 UI 验收；无 Provider 场景只替换隔离的 Provider 配置根，不修改故事数据。

## 既有断言调整

`tests/storyStudio/nuwaN1WorkspaceSource.test.ts` 的 2 个测试、4 条源码位置断言原本要求 `profileBasis` 与 `attention` 过滤字段直接写在 `nuwaN1PiAdapter.mjs`。本任务要求 UI 与 Provider 共用唯一安全投影，因此断言改为同时验证：

- Pi adapter 调用共享 `projectNuwaN1ProviderSafeContext(context)`；
- 共享投影仍保留 `profileBasis` 与 `attention` 原合同。

没有放宽字段边界或行为断言。
