# MEM-A1a R1 工作日志

- 时间：2026-09-11（Asia/Shanghai）
- 工作分支：`codex/mem-a1a-grounded-retrieval`
- 目标：在冻结的关系 R1.1 基线之上，完成天意“按问题检索故事依据”的数量合同、来源定位与回答恢复闭环。

## 基线

- MEM 起点：`13624e1bae0c1e0dc708faf67f10e83e710128c8`。
- 关系 R1.1 基线：`ad627c2079a68758048cd5a91454822ce328ef40`。
- 两者共同祖先：`fc46e03`；以普通 merge 保存两个祖先，产物为 `7ac0c13bfafc0d42b9f4075a7bf1ae465d10e0dd`。

## 完成内容

- 统一前端、默认检索和服务端的依据上限为 6；零相关结果不会被自动补入，显式固定仍保留并受上限保护。
- 修复问题输入后读取事件摘要造成正文缺失的崩溃：检索改从正式对象读取完整事件正文。
- 回答回执恢复改为只读读取已有冻结回执，不会重新检索或发送 Provider 请求。
- 来源跳转携带项目、作品版本、事件与精确修订，并能从事件线返回原问题和回执。

## 版本与边界

- 代码检查点：`1cbbd5ee5e573f5f0ec6e860877ee8fb6921ed20`。
- 本目录记录的浏览器链只使用隔离项目和确定性假 Provider；真实 Provider 发送为 0，设置诊断预算未触碰。
- 日常 4191/4192 服务、正式作品和既有 API 记录均未修改。

## R1 收尾修复

- 工作模式改由既有 Canon 正式事件读取链提供完整正文，不再把资料目录摘要或规划草稿当作可检索故事事实。
- 修正项目/作品版本身份稳定时被无关渲染废弃的读取结果；大量正式事件不再永久显示“正在读取”。
- 刷新与来源返回时，工作模式的只读元数据恢复不再依赖可选创意投影；冻结回执不会触发新的模型发送。
- 本轮再次运行隔离 `r4-workspace` 浏览器链并覆盖本目录截图；真实 Provider 发送仍为 0。
