# 工作日志：日常服务切换主目录 + 实际页面体验检查

日期：2026-09-15（+08:00）
执行：GLM（ZCode 会话延续）
授权：按 `docs/operations/TIANYAN_DAILY_4191_4192_UPGRADE_RUNBOOK.md` 执行一次停旧启新；随后从正常入口做体验检查。

## 一、切换记录

- 切换前现场：4192 由旧 worktree `tianyan-map-m4-closeout` 运行（`codeRevision=54e5c86`，2026-09-14 21:07 启动，combined-static，4191 未监听）。
- 只读预检：`scripts/tianyan-daily-upgrade-preflight.mjs` 通过全部数据安全项（10 个项目登记完好、0 锁、0 进行中运行、最近 120 秒 0 写入、磁盘 742GB 可用）；唯一 blocked 项为"4191 未监听"，属运行手册按 dev 双端口模式书写的假设与现场 combined-static 单端口不符，已人工核实 4192 归属旧服务组（pid 171417→171418→171428，cwd=旧 worktree）后继续。
- 备份：`/home/beelink/Documents/Codex/tianyan-upgrade-backups/20260915-064403-pre-f95fb7c-cutover/`（0700，WorldOS + Provider 配置根，双向 SHA-256 清单校验通过，清单不含凭据正文）。
- 预演：以 `PORT=41993` + 隔离 `WORLD_OS_STORY_STUDIO_ROOT` + `TIANYAN_PROVIDER_PROFILE_DEV_MODE=1` + 隔离 `TIANYAN_PROVIDER_APP_DATA_ROOT` 在空闲端口启动同版本服务，健康回执与产物哈希核对通过后销毁；真实数据根零写入。
- 前端构建：`npm run build`（Node 22.22.0），产物 `apps/story-studio/dist/assets/index-BnmrOtgH.js`，与服务端同源于 `f95fb7c`。
- 停旧启新：2026-09-15 06:45 停止旧服务组并确认 4192 释放；主目录以 `TIANYAN_STORY_STUDIO_RUNTIME_MODE=combined-static npm run serve` 启动（无 PORT/WORLD_OS_STORY_STUDIO_ROOT/TIANYAN_PROVIDER_APP_DATA_ROOT 覆盖，默认端口 4192、作品根 `~/WorldOS`、Provider 权威配置，与旧服务一致）。
- 切换后核对：进程 cwd=`/home/beelink/Documents/Codex/worlding.world-天衍`（pid 829306）；健康回执 `codeRevision=f95fb7ce8f87714de3b177805683b179ea0d7afd`；页面资源为新构建哈希；浏览器实测世界首页/地图库/关系页/创作页/事件线/天意侧栏均正常渲染。
- 回滚位置：备份目录如上；回滚命令为在旧 worktree 以同一 `npm run serve` 方式启动（其 dist 为 54e5c86 构建），健康回执应回到 `54e5c86`。切换为纯代码切换，未迁移/改写任何作品数据。

## 二、体验检查记录（测试作品 experience-check-20260915，正式作品只读）

- 世界首页：正常渲染；有数据与空数据均只有口号+单按钮（发现 #4）。
- 地图绘制：空白画布建图 → 陆地(区域)/河路(线条)/森林·山脉(地形笔触)/标记/文字全部成功；节点编辑拖动保存成功；撤销/重做/刷新重开通过。小毛刺见 #7。
- 底图导入：横长 1600×800 与竖长 800×1600 PNG 均导入成功；编辑画布渲染比例实测 2.00 与 0.50，无失真；标记在缩略图与画布位置一致。发现 #3（重名地图不可区分）。
- 关系：测试作品与全部 12 个日常作品均无作品版本 → 关系页全部为无版本空态（发现 #2）；空态标题在 1152×720 过大（#6）。
- 天意交接：事件"带着这个事件问天意"正确带入多节点推演（推演范围=1 节点，作者要求预填，人物知情比较显示林昭·已确认）；全程未发送真实 Provider 请求。
- 视口：1440×900 与 1152×720 均检查；窄屏下天意侧栏覆盖行为正常、顶栏正确收纳。

## 三、证据

- 截图：`截图/01…07`（切换后首页、空白首页、天意交接、窄屏空态、底图导入×2、横长底图编辑器、窄屏天意侧栏）
- 录像：`录像/连续操作-世界首页到地图绘制撤销重做重开-1440x900.webm`（11 秒）
- 脚本：底图导入与录像使用临时 Playwright 脚本（/tmp，未入库），仅操作系统 Chrome 无头模式指向本机 4192。

## 四、边界

- 未修改正式作品事实；测试数据仅写入 `experience-check-20260915`。
- 真实模型 0 次调用（推演只到任务面板，未点开始）。
- 未删除旧 worktree；未推送/合并 PR；未开展无关开发。

## 五、第二轮：作者首次旅程断点修复（同日稍后）

### 断点与代码对应

| # | 断点 | 代码位置 | 修复 |
| --- | --- | --- | --- |
| 1 | "新建作品"只在无作品空状态渲染 | `ProjectDirectoryPanel.tsx:48`；`GlobalStatusBar.tsx` 菜单无创建项 | 切换菜单尾部内联创建表单，复用 `runtime.createProject`；创建后自动切入，失败/取消不影响原作品 |
| 2 | 新建单元在脊柱视图不可见 | 脊柱按事件归组（`EventLineWorkbench.tsx` groupEventsByUnit） | 脊柱视图新增"未编排单元"可见列表（含生命周期状态） |
| 3 | 单元 draft 无激活入口 | 常规创作链 `createStoryUnit` 直接建 active 单元（normalEventCreationPort） | 接入常规创作面板 |
| 4 | 事件表单"故事单元"为自由文本假绑定 | `EventLineWorkbench.tsx` 旧表单仅写标签 | 常规创作面板用真实单元选择器；confirm 链自动写 `linkedEntityIds`（port 既有行为） |
| 5 | 草稿事件被投影为"正式事件" | `eventSemanticHierarchy.ts` parseStatus 兜底返回 confirmed | 增加 draft/planned→candidate 分支；兜底行为保留 |
| 6 | "建立主故事版本"禁用无说明 | `CreationSourceWorkspace.tsx`；真实条件在 creationSourceSelectionPort | 条件清单（单元/已确认事件）+ 前往事件线/资料库入口；按钮禁用条件与清单一致 |
| 7 | 关系无版本页死胡同 | `FocusedRelationsWorkspace.tsx` | 保留导航，增加路径说明与"前往创作空间/事件线/资料库"行动 |
| ★ | 作者确认链（候选→影响评审→确认）只有服务端端口与 E2E 调用，无任何 UI | `normalEventCreationPort.mjs`；`localTransport.ts` runNormalEventCreationAction 零调用者 | 事件线新增"常规创作"Dock，逐步显式操作；confirm 复用既有 Change Set→评审→应用链，无自动确认、无放宽门槛 |

### 验收（隔离环境：41994 端口临时服务 + /tmp 隔离作品根，UI 驱动，未调用 API/文件/fixture 造数据）

- 两个独立作品从零走通完整路径：新建作品 → 人物/地点 → 新建故事单元 → 建立事件候选 → 影响评审 → 作者确认写入正式事件（自动归属单元）→ 创作空间条件清单 ✓✓ → 建立主故事版本（r1）→ 关系页显示版本化空关系状态 → 刷新重开保持。
- 覆盖 1440×900 与 1152×720；连续录像 28.5 秒（`录像/完整作者路径-新建作品到关系页-1440x900.webm`）。

### 测试与运行版本

- 全量门禁 `npm run verify` 一次：typecheck、lint、unit 224 文件、integration 57/57 全部 PASS；E2E 12 个默认 scope 中 11 个 PASS，`map-m3-author-experience` 出现 30 秒 locator 超时（非断言失败），同一 SHA 单场景隔离重跑 PASS，判定为连续运行后的时序偶发。
- `apps/story-studio/dist` 已精确恢复为 f95fb7c 部署内容（相对路径内容指纹一致）；日常服务未重启，健康回执仍为 `codeRevision=f95fb7ce…`。
- **开发代码（本轮 5 个提交后 HEAD）与日常运行版本（f95fb7c 构建）暂不一致**；下次按运行手册切换即对齐。

### 遗留

- 确认写入后主视图有一次刷新竞态（手动刷新即正确），已记录未修。
- 关系创建入口存在于关系图（事件线"关系网络"），本轮按验收要求只验证到版本化空关系状态，未造关系宣称完整。

## 六、第三轮：确认后一致性修复、双入口核查与关系入口定位（同日）

### 确认后一致性（原"刷新竞态"）

- 诊断结论：服务端写后读即时一致（页面内 120ms 轮询，确认后首个响应 committed 即 +1）；主视图计数表头始终正确（"第一幕 · 3 个事件"与已确认事件数一致）。上轮观察到的"竞态"实为三个真实缺陷与两处探针伪象的叠加。
- 修复（提交 34f3481）：
  1. **锚定修复**：create-candidate 之后的端口 state 改为锚定作者刚建立的候选（服务端路由按 result.planning.id 锚定），不再依赖列表顺序挑选 planning——此前可能锚定到旧候选并把"确认"变成对旧候选的幂等空操作。
  2. **如实提示**：confirm 幂等空操作（applied=null）时界面改为"该候选此前已确认过；本次未重复写入正式事件。"，不再谎报"已写入"。
  3. **闪断消除**：知情投影键变化（新事件确认）时保留上一份投影继续显示（stale-while-revalidate），重建完成前主视图不再闪成"0 个事件"。
- 验证：净室作品确认后计数即时更新；重复确认出现如实提示且未重复写入；空标题禁用；刷新后保持一致。

### 新旧入口双检

- 旧"新建单元"：创建后立即出现在"未编排单元"区块（含"已创建单元"状态与草稿后续路径说明"同样可在'常规创作'中选择使用"）；active 单元正常进入脊柱。
- 旧"保存事件草稿"：表单"故事单元"字段更名"故事单元（仅显示标签）"并加注"只是显示标签，不代表事件已归属单元；正式归属请在'常规创作'中经作者确认完成"——消除假绑定误解。
- 新"常规创作"：与旧路径并存，草稿单元可在其单元下拉中选择使用。

### 关系入口定位（本轮范围：不调用 Provider）

- 已确认的作者路径：事件线"关系网络"图上连接两个已确认事件节点 → onCreateRelation 创建关系候选（带方向）→ "智能连线候选"托盘审查 → 作者确认后成为正式关系。
- 断点一（可用性未证）：合成指针拖拽（CUA 与 Playwright mouse 各两次，命中桩中心）均未触发 onConnect；代码链路完整（onConnect=connect → onCreateRelation 已接线），是否真人鼠标可用无法在无头环境证实——标记"未验证"，不冒充通过。
- 断点二（本轮约束）："智能连线/补充因果候选"工具需要 Provider（3–3 次模型请求）；按本轮不调用 Provider 的约束未运行。
- 关系页读取按版本（r1）正常；候选确认后的关系页五项验证因上述断点未完成，标记"未验证"。

### 证据澄清

- 截图中顶栏"联网同步状态：未连接"是云端同步指示（无云服务时恒为未连接），不代表本地服务离线；全部截图均摄于对应服务在线时。
- 上轮门禁 verify 的输出经过滤仅存摘要行，E2E map-m3-author-experience 失败的 locator 细节未保留——该失败标记"原因未定、单跑通过"；本轮 verify 已留存完整输出（`20260915门禁verify输出留存.log`）。

### 隔离环境

- 本轮隔离服务改在 /tmp worktree（2085cbe→34f3481）构建运行（41994 端口），与主仓库 dist 完全解耦；主仓库 `apps/story-studio/dist` 本轮从未改动，日常服务（4192，f95fb7c）全程未受影响。
- 隔离作品根 /tmp/tianyan-authorpath-worldos 保留（含测试作品与污染数据），未删除。

### 门禁（第三轮最终）

- 第一次全量 verify 失败：unit 2 个契约用例失败——(1) R0EventLineProjection 内新增中文注释违反产品壳扫描集禁中文契约；(2) 知情投影键比较被重构为 `!==` 丢了自己的逐字断言。修复：注释改英文；恢复 `loadedKnowledgeProjectionKey === requestedKnowledgeProjectionKey` 逐字比较，并把 stale-while-revalidate 收窄为"同项目、同观察者"才保留旧投影（跨项目/视角切换仍置空，符合 R4 防泄漏意图）。
- 修复后最终全量 verify（唯一一次计入）：EXIT=0，typecheck、lint、unit 1242/1242（224 文件）、integration 57/57、E2E 全部 12 个默认 scope PASS（含上轮偶发的 map-m3-author-experience）、build 完成（仅既有大 chunk 提醒）。完整日志：`/tmp/tianyan-verify-round2b.log`（并已留存 `20260915门禁verify输出留存.log` 为上轮失败样本）。
- 验证后已将主仓库 `apps/story-studio/dist` 精确恢复为 f95fb7c 部署内容（内容指纹一致）；日常服务健康回执仍为 f95fb7c。
- 最终开发 SHA：a9b5747（codex/world-materials，含本轮 7 个提交）。

## 七、第四轮：人物关系真实作者入口（同日稍晚）

### 关系模型核实清单

| 问题 | 结论 |
| --- | --- |
| 人物—人物关系由谁保存/确认/读取 | Relation Owner：`src/storyWorkspace/relationRepository.mjs` + `src/storyControlSurface/storyStudioRelationOperations.ts`；RelationRecordR0（sourceObjectId/targetObjectId 为任意稳定 WorldObject ID、relationTypeId、direction forward/reverse/both/none、reviewState candidate/confirmed/rejected、evidenceRefs）；确认=confirmRelationCandidate（作者操作，带 expectedRevision 与依据新鲜度核验） |
| 人物—地点、事件—事件 | 同一合同：E2E 中北闸（地点）→林昭（人物）与事件图连线（event→event）都走同一 createRelationCandidate；无第二套存储 |
| "资料→关系"页支持对象与人工入口 | 读取按作品版本过滤已确认关系；搜索/列表/图支持人物与地点；此前**无任何人工创建入口**（候选只来自天意或图连线） |
| 可复用能力 | createRelationCandidate（operationId 幂等、自连拒绝、端点必须是已知 WorldObject）、confirmRelationCandidate（作者确认+证据新鲜度）、relations/types/create（关系名称类型化）、confirmed-event 证据合同（version/projectId/eventId/revisionToken/state/requestedUse） |

结论：Owner 完整表达人物关系，仅缺 UI——按任务分支补齐最小人工表单，不新增存储。

### 实施（提交 2d8b6d1）

- 新增 `RelationCreateForm.tsx` 并接入关系页：双方人物下拉（稳定 ID 绑定、同名以 ID 尾号区分）、方向（来源指向目标/互相）、关系名称（既有类型选择或内联新建，复用 relations/types/create）、依据事件必选（本版本内经作者确认链验证的事件，含修订号显示）；提交前显示"将建立：…→… · 名称 · 依据"预览；建立后先成候选，作者显式"确认建立正式关系"，亦可拒绝/留作候选；取消或关闭表单不写任何数据；失败保留输入并显示原因。
- 空关系状态增加"建立第一条关系"入口；工具栏增加"建立关系"开关。
- localTransport 的 create/confirm 签名补 workVersionId（对齐服务端既有 allowed keys）。

### 验收（隔离作品"关系验收XXXXX"，全程 UI，0 次 Provider 调用）

- 建关系：林昭（ter.林昭）→ 顾澜（ter.顾澜）· 并肩加固 · 依据北闸遇险（修订 3aba5974）——候选→确认→列表即时显示"顾澜 → 并肩加固 → 林昭（由左至右）"。
- 依据：打开依据跳转事件线并携带 eventRevision，详情显示"正式事件·作者确认·Canon 读取已核验"，来源版本 3aba5974 与关系所引修订一致，无"修订不匹配"提示。
- 天意：交给天意后工作台显示"顾澜 → 林昭 · 并肩加固"，本次明确带入 2 项人物 + 1 项正式事件依据（已核对版本），页面明示"尚未发送"；返回关系图后列表与选择恢复。
- 刷新：重载后关系、版本 r1、列表状态保持一致。
- 取消：表单填写后直接关闭，关系数保持 1/1 不变。
- 同名：再建同名"林昭"被系统如实拒绝（"Workspace note already exists"——人物以标题路径为身份，系统层面不允许同名人物并存）；表单选项仍以 ID 尾号区分。
- 第二条：同对人物新建"旧识"并确认后，"并肩加固"与"旧识"并存（同对两条关系互不覆盖；含此前探针轮次所建共 5 条已确认关系）。
- 视口：1440×900 与 1152×720 表单、依据、返回按钮均可达，无横向溢出。
- 录像：`录像/人物关系建立确认与刷新保持-1440x900.webm`（10.5 秒）。

### 门禁与版本

- 全量 verify 在隔离 worktree（2d8b6d1）运行一次：EXIT=0，typecheck、lint、unit 1242/1242、integration、E2E 12/12 scope 全 PASS、build 完成。完整日志 `/tmp/tianyan-verify-round3.log`。主仓库 dist 与日常服务全程未被触碰。
- 区分：**人物关系通过项**＝表单建立、双方/方向/名称/依据、确认链、即时显示、天意带入、刷新保持、取消/第二条/同名处理；**事件连线未解决项**＝关系网络图上手工拖拽连线未能在合成输入下触发 onConnect（代码链路完整，真人鼠标待验），本轮未修。
- 最终开发 SHA：e61024e 三轮累计至 2d8b6d1（codex/world-materials）；实际运行版本：日常 f95fb7c（未变），隔离验收 2d8b6d1。
