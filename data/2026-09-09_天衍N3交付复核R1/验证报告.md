# 天衍 N3 本地交付复核 R1

## 本次结论

本次在 `codex/nuwa-n3` 本地工作树完成两项复核修补：新建固定稿下载为作者阅读版 Markdown v2；A（女娲固定稿）与 B（竞态隔离稿）的证据身份已分开保存。旧固定稿没有迁移、没有重写，仍按已保存的快照和原始字节读取。

这只是本地代码、隔离 fixture 和浏览器交互的交付。真实 Provider 调用为 0；Founder 人工体验未验收；GitHub 同步未完成。

## 作者可读导出

`验证/固定稿A-回溯前.md` 与 `验证/固定稿A-回溯后.md` 均以“作者阅读版 v2”组织为标题、场景、对白、行动、结果和听闻。人物听闻保留了接收者、说话者和原话，例如“林昭 听 阿芜 说：‘北闸已封。’”。

运行标识、步骤、授权与正式应用回执已移到“来源说明”附录。只有明确标注为刻意原始 JSON/代码的输入才保留为 fenced code；本实现没有对任意字符串做全局 JSON 解析或 `\\n` 展开。创作页预览与下载均读取同一份冻结 Markdown。

## A/B 身份与回溯链

- A：`novel.雾港追踪-女娲固定稿`，来源 Run `nuwa-run-2f41e1cc040c`，自动应用回执 `nuwa-n1-auto-application.75b70e67fd2488efba61cca9e2d8c07f`，来源 WorkVersion `work-version.root.121a2de9edd1a18e3ad4dc7b6b23fc77` r3，包 `neutral-story-package-adc6caebf3502f4d0bf6`。
- A 在补偿回溯前、回溯后均由正常创作页实际下载；文件 SHA-256 都是 `87216d6cd7b1dc0711f278a08c2d8b33702b27b2284e5ee279af1eba6ffd5731`。对应截图为 `截图/10-固定稿A-回溯前-1440x900.png` 与 `截图/11-固定稿A-回溯后-1440x900.png`。
- B：`novel.长夜将明-创作稿-938089b7f5`，只用于 A 延迟响应后的竞态隔离。其截图 `截图/12-固定稿B-竞态终态-1440x900.png` 和下载 SHA-256 `4232d98f94b13f17ab8815d2f69ca1316eddbb037659c7e22b1f5851ebdba2bc` 与 A 明确不同。
- 完整可机读映射见 `验证/固定稿身份映射.json`。旧 N3 报告中的 `运行成果/11-1440-nuwa-fixed-after-rollback.png` 已更正说明：它发生在 A 的第二次下载与比较之后，显示的是 B，不能再被读作 A 的回溯后截图。

旧 N3 固定稿的两份历史下载仍逐字节相同，SHA-256 均为 `5260b61199f26781b04ac0968bedf987ebae30f1d467be8e710600ddf6f93614`；本次仅做只读核验。

## 本次验证

- Node `22.22.0`：导出和固定稿快照定向测试 `35/35` 通过。
- `npm run typecheck`、`npm run lint` 通过；lint 继续确认唯一 Canon Writer、WorldState Owner、Event Owner 与 NarrativeArrangement Owner。
- `TIANYAN_E2E_SCOPE=n3-continuous npm run test:e2e` 通过，使用本地伪服务，输出本目录全部截图、下载、映射和 1440×900 连续录像（SHA-256 `464a43b472968ece43ac4eca98ed2bde0f298cb053b09e5e5c0ed6aab99315af`）。
- 最终 `npm run verify` 通过：单元 `1151/1151`、集成 `55/55`、默认浏览器 scope 与生产构建均通过。构建仅保留既有的大包体积提示，没有构建失败。

## 远端边界

本次只进行一次有界 `gh auth status` 检查；15 秒内没有得到可用认证结果。因此没有 fetch、push、PR 或 main 合并，也没有继续轮询或搜索凭据。本地交付和后续独立开发不受此阻塞影响。
