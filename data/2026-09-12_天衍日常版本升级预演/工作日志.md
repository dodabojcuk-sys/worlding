# 天衍日常版本升级预演

## 范围与边界

- 本轮不切换、不停止日常 4191/4192，不调用真实 Provider，不迁移或覆盖正式数据。
- 日常现场代码为 `2781405928bcf224c82ddef0b1122d6b80386e91`，监听工作树为 `/home/beelink/.codex/worktrees/tianyan-n2`。
- 预演基线为已合入的 `7eaf70f1a6d342edcfda8ba851e39d2bab0599fc`。本轮发现并修复旧作品缺少作品版本时关系页伪装成永久加载且无法返回的问题；最终切换 SHA 待该修复通过门禁并合入后固定。

## 数据与隔离

- 正式作品根：`/home/beelink/WorldOS`，10 个登记作品、237 个文件，预演期间保持运行且未出现预演标记。
- 权威 Provider 根：`/home/beelink/Library/Application Support/Tianyan`，5 个文件；没有输出凭据正文。
- 权限受限备份目录：`/home/beelink/Documents/Codex/tianyan-upgrade-backups/2026-09-12-preflight-7eaf70f1`，目录模式 `0700`。
- `WorldOS-preservation-copy` 是源前后哈希一致、逐文件 SHA-256 与副本一致的静止窗口保全副本；由于日常服务当时仍运行，它不是最终一致性备份。
- `WorldOS-rehearsal-copy` 是可写预演副本；目标服务只写该副本。`rehearsal-provider-empty` 是空的隔离 Provider 根，明确启用本地假 Provider；真实 Provider 调用为 0。
- 切换获准后，最终一致性备份必须在旧服务停止、端口释放后重新生成。普通运行中复制不得冒充最终一致性备份。

## 实际预演结果

1. 目标前后端在 4391/4392 启动，健康回执代码版本为预演基线；日常 4191/4192 始终保持旧版本运行。
2. 从副本打开原活动作品，目录、角色与地点资料可读。
3. 编辑“雾港”资料并保存；仅重启目标 API 后，新修订仍恢复。正式作品未出现预演文字。
4. 从资料打开地图，选择“雾港 · 局部地图”，再返回同一资料。
5. 旧作品没有作品版本时，关系入口现在明确显示缺少版本、不读取其他版本关系，并可返回原资料。
6. 本地假 Provider 完成一次资料明确引用、发送和回答回执；回执记录 1 次模拟模型发送。资料随后再次修改，旧回答仍从历史 `revision.000002` 读取原正文，未替换成当前正文。
7. 用旧代码版本读取含上述新修订与回答回执的副本，健康检查、活动作品与世界入口均可读。代码回滚因此不需要覆盖正式数据；旧界面不认识的新资料能力在回滚窗口不得继续编辑，但文件会保留。

## 已验证与待执行

- `TIANYAN_E2E_SCOPE=world-materials-m1 npm run test:e2e`：通过，包含新增的无作品版本关系返回用例。
- `npm run typecheck`：通过。
- `npm run build`：首次因依赖软链真实路径含中文触发 Rolldown UTF-8 截断 panic；保留失败后改用 ASCII 工作树内的独立依赖副本，复跑通过。没有修改系统 Node 或放宽构建门禁。
- 只读升级预检：Node 22.22.0/npm 10 下通过；无锁、无进行中运行记录、正式作品根最近 120 秒无写入，备份空间充足。
- `npm run verify`：通过；1194 个单元测试、57 个集成测试、全套隔离浏览器 E2E 与生产构建均通过。
- 尚待：提交、远端 CI、正常合入、以合入后的完整 SHA 重跑预检并创建耐久候选工作树。
- 日常切换仍未获本轮授权，必须先展示最终结果并集中请求确认。
