# TIANYAN_RESTORE_GLOBAL_ACCOUNT_SETTINGS_R0_1_1 工作日志

## 范围

- 基线：`47272310e88c84c67c38bbc515426b7e209a4cdc`
- 开始 HEAD：`5b960c816299605e623b250116f5b8b7b4185802`
- 仅恢复空间轨底部的个人中心、设置入口、既有 Shell 回调合同、样式、翻译、合同与测试。
- 不接入旧产品页面、领域 owner、Provider、Pi Agent、业务数据或部署流程。

## 结论

`5b960c8` 在精简空间轨时删除了底部 `.shell-rail-utility` JSX，并同时移除了 `onAccount` / `onSettings` props；并非 registry 或 CSS 隐藏问题。`4727231` 中两个回调均为 `() => undefined`，且当前 R0 产品根入口未接入账户或设置页面，因此本轮只恢复既有 Shell 入口合同，不声称账户或设置功能已完成。

## 实施

1. 在空间轨底部恢复独立 utility 区，和故事空间 registry、合册入口分离。
2. 恢复图标、完整翻译文字、tooltip、可访问名称与键盘焦点；折叠控制保持在品牌区。
3. 将中部空间列表设为可滚动区域，确保低高度时 utility 不被裁切。
4. 补齐 `Personal center` 英文翻译、合同与回归测试。

## 约束

- Provider / 真实模型 / 业务数据写入：`0 / 0 / 0`
- Push / 部署：`0 / 0`
