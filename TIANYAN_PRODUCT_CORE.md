# 天衍产品核心

本文是一份面向完整产品的核心定义草案。

它不是当前工程状态报告，不是阶段验收文档，不是开发交接，也不是某一轮界面审计。它描述的是：当“天衍”成为一个完整产品时，它究竟是什么、为什么存在、各部分如何协作、作者与 AI 分别拥有什么权力，以及界面应当怎样帮助作者完成创作。

本文有两个用途：

1. 用来校正产品。任何页面、功能、数据结构或 Agent 行为，都可以回到本文检查是否偏离产品本意。
2. 用来让新的 AI、设计师或开发者迅速理解天衍。即使对方不了解历史，也应能在阅读本文后说清楚天衍的核心价值、八个空间、事件线、女娲、Agent、派生副本和创作输出之间的关系。

本文中的内容分为三类：

- **已确认的产品定义**：来自创始人已经明确表达和反复校正的想法，后续不能因局部实现方便而自行改写。
- **完整产品所需的结构补全**：用于把零散想法组织成可落地、可理解的完整产品。它必须服从产品本意。
- **创始人待确认项**：目前概念已经清楚，但最终名称、入口位置或默认行为尚未确定。它们会明确标注，不能被未来 Agent 擅自当成最终结论。

任何未来 Agent 在修改本文的核心概念前，都必须先与创始人重新对齐。不能因为当前代码没有某项能力，就从产品定义里删掉它；也不能因为某张截图里出现了某个入口，就把临时实现写成永久产品结构。

---

# 一、项目

天衍的立意，是帮助有想法但不擅长写作、缺少系统整理能力，或者难以长期维持庞大故事一致性的作者，创造出一整个能够持续生长的故事世界。

作者不需要先学会复杂的大纲方法，也不需要从几十张表格开始。作者可以只说一句话、讲一个模糊的念头，也可以导入已经写好的几十万字、几百万字内容。天衍负责理解这些材料，把其中的人物、物品、地点、事件、关系、规则、时间、记忆、伏笔、写作风格和未解决问题逐步整理出来。

天衍不是替作者“一键写完一本小说”。

天衍真正要做的是：

- 作者控制故事的宏观方向、价值判断、核心设定和最终取舍；
- AI 理解作者的意图，整理混乱信息，维护故事世界的一致性；
- 角色、物品、地点和规则能够以 Agent 的方式保有自己的状态、记忆、关系和边界；
- 系统能够在作者允许的范围内推演故事的微观发展；
- 作者可以观察、比较、修改、拒绝或吸收这些推演；
- 同一个故事世界可以被翻译、改编、切换视角、形成 IF 线，并交给不同的外部工具继续制作。

一句话定义：

> 天衍是一套由作者掌握最终决定权、由 AI 负责理解、记忆、维护、推演和协作的故事世界创造与演化系统。

它帮助作者让故事“长出血肉和灵魂”：作者决定这个世界为什么存在、最终走向哪里；系统负责让人物、关系、因果、时间、空间和世界状态在这些边界中自然地发生变化。

---

# 二、天衍不是什么

这些不是阶段性取舍，而是产品长期不应偏离的边界。

天衍不是普通的 AI 聊天机器人。聊天只是作者与故事世界交互的一种入口。

天衍不是传统的资料库或项目管理软件。资料、事件和设定不是为了填表，而是为了让 AI 与作者共同理解和维护一个活着的故事世界。

天衍不是要求作者先完成整套大纲的写作工具。作者可以自由思考、自由表达，结构由系统逐步提炼，作者再决定是否采用。

天衍不是一个在内部重新制造小说编辑器、剧本软件、漫画软件、漫剧软件和互动叙事引擎的巨型套件。天衍负责故事内核、理解、组织、推演和交付边界，具体格式与后续制作可以通过受信任的外部插件和工具完成。

天衍不是让 AI 偷偷修改正式故事的自动驾驶系统。AI 可以分析、建议、生成候选、在派生副本中自主推演，也可以在作者明确授权后执行编辑；但作者始终拥有正式故事的最终决定权。

天衍也不是让所有角色共享全知视角的模拟器。每个角色只应依据自己经历过、听说过、相信、误解或怀疑的内容行动。

---

# 三、完整产品的层次

为了让功能、数据、交互和界面不再互相混淆，天衍应按以下层次理解。

## 1. 产品意图层

回答“为什么做天衍”。

它规定作者与 AI 的关系、产品服务的人群、故事如何自然演化，以及什么是天衍不能牺牲的核心价值。

## 2. 故事语义层

回答“天衍理解的故事由什么组成”。

它包括来源、作者意图、Agent、角色、物品、地点、组织、关系、规则、事件、节点、集点、单元、主线、支线、暗线、时间、记忆、知识、伏笔、世界状态、写作风格、剧情想法、灵感和创意。

## 3. 智能与 Agent 层

回答“AI 怎样理解、记忆、协作和推演”。

它包括天意、页面 Agent 模式、各类故事 Agent、上下文组装、记忆、知识边界、候选提炼、剧情检测、预测、女娲排演和后台任务。

## 4. 功能层

回答“用户到哪里完成什么事情”。

它由世界、天意、事件线、多元、女娲、资料、创作、数据八个空间，以及设置和个人中心组成。

## 5. 工作流与权限层

回答“信息如何从想法变成正式故事”。

它规定来源、候选、作者审查、正式内容、派生副本、同步、对照、融入、输出产物和操作历史之间的边界。

## 6. 交互层

回答“作者怎样操作这些能力”。

它包括对话、语音、拖入引用、上传资料、选择节点、画布操作、批量审查、预览修改、版本对照、同步和确认。

## 7. UI 层

回答“复杂能力怎样被清楚地呈现”。

它包括全局空间导航、左侧工程目录、主工作区、对象详情、右侧天意 Agent、移动端抽屉、状态视觉、语义缩放、空状态和渐进披露。

## 8. 扩展与输出层

回答“天衍怎样连接外部世界”。

它包括中立故事包、GitHub 项目适配、受信任插件、安装与更新、外部工具调用、输出产物、回执和安全隔离。

## 9. 产品质量层

回答“怎样才算一个完整、可信、好用的天衍”。

它包括一致性、可解释性、可恢复性、长篇处理能力、权限安全、移动端可用性、无障碍、性能、视觉统一和真实作者体验。

---

# 四、作者与 AI 的基本关系

## 作者

作者是这个故事世界的创造者、导演、观察者和最终决定者。

作者可以只提出宏观要求，也可以进入细节逐字修改。作者可以允许 AI 在某个范围内自由推演，也可以要求每一步都先询问。天衍不能强迫所有作者使用同一种工作方式。

作者拥有：

- 核心设定的最终解释权；
- 正式故事内容的最终确认权；
- 派生副本是否同步、保留、融入或删除的决定权；
- Agent 是否采纳、分类、修改或删除的决定权；
- AI 可访问范围、可执行能力、预算和自动化程度的控制权；
- 历史版本、外部输出和插件使用的控制权。

## AI

AI 是理解者、整理者、记忆维护者、协作者、分析者、模拟者和执行助手。

AI 可以：

- 理解作者口语、文字、图片和导入文档；
- 提炼角色、物品、地点、事件、关系、规则和想法；
- 发现矛盾、遗漏、未解问题和潜在线索；
- 根据角色知识边界和世界规则进行推演；
- 生成候选 Agent、候选节点、候选关系和候选描写；
- 在作者授权的派生副本中持续演化；
- 在作者明确授权后执行增删查改；
- 解释自己使用了哪些上下文、为什么得出某个结论；
- 为外部工具准备完整、可追溯的故事包。

AI 不得：

- 把自己的猜测伪装成作者已经确认的事实；
- 在没有权限的情况下修改正式故事；
- 让角色知道其不可能知道的信息；
- 隐藏自己修改了什么；
- 用一段新的摘要覆盖原始来源；
- 因为上下文不足而悄悄编造精确时间、关系或设定；
- 让外部工具直接读取和改写整个故事数据库；
- 把一次推演结果自动当成正式故事。

## 权限不是只有“允许”与“不允许”

完整产品应支持不同的作者控制方式：

- **只读理解**：AI 只读取、解释和回答。
- **提供建议**：AI 给出建议，不创建内容。
- **生成候选**：AI 可以建立候选对象或候选节点，等待作者审查。
- **授权编辑**：作者明确指定对象和范围后，AI 可以直接修改，但必须保留修改记录和回退能力。
- **派生副本自主运行**：AI 可以在副本内根据权限连续推演，不能越界修改原始主线。
- **后台持续工作**：在作者设置的时间、预算、范围和停止条件内持续整理、检查或推演。

确认不等于每改一个字都弹一次窗口。作者可以对一次操作、一个批次、一个单元或一个派生副本预先给出范围授权；系统仍需保留清楚的修改记录、影响说明和撤销能力。

## R0 作者决定与正式写入串行化

R0 锁定单作者，并且同一目标故事在同一时刻只有一个正式写入者。分析、预测、Agent Run 和候选生成可以并行；任何正式采纳、融入、恢复或撤销都必须通过目标故事的唯一写入链串行执行，并在提交前校验 `BaseVersion`。

`BaseVersion` 过期、实际范围变化或出现新冲突时，原操作必须拒绝提交，不得 last-write-wins，也不得产生部分正式版本。作者此后面对的是基于新事实的新决定，不是对同一个决定重复确认。高影响操作只增加采纳前的信息密度、展开说明和必要字段，不增加第二次确认。

---

# 五、故事信息的基本层次

天衍必须始终区分以下内容。它们即使在 UI 上都显示为卡片，也不能被当成同一种事实。

## 1. 原始来源

包括作者原话、语音转写、上传文档、图片、关系图、旧稿、参考资料和外部导入内容。

原始来源需要保留，不能被 AI 摘要替代。系统应记录来源版本、位置、时间和引用范围。

## 2. 作者意图

作者明确表达的目标、限制、偏好、禁止事项和未来想法。

作者意图不一定已经成为故事事实，但它是 AI 推演和建议的重要约束。

## 3. AI 候选

AI 从来源或对话中提取、推断或生成的角色、物品、地点、事件、关系、设定和想法。

候选必须说明依据和不确定性。候选可编辑、合并、拒绝、重新生成、保留或采纳。

未被选择的候选自动进入可恢复的候选历史。R0 不执行静默永久删除；候选可以由作者明确清理或归档，但不能因为模式切换、页面关闭或一次采纳结束而无声消失。

以下动作必须使用不同术语：

- **保存草稿**：只保存当前创意或工作过程，不改变故事事实；
- **保留候选**：继续把 AI 结果作为可恢复候选保存，不赋予正式权威；
- **保留可能性**：把某个未选方向跨会话持久保存，仍不是正式故事或独立分支；
- **升级为 IF**：从明确来源故事与基础版本建立派生副本和独立版本线；
- **采纳**：作者让当前候选对当前明确目标故事正式生效。

## 4. 已确认内容

由作者确认或在明确授权范围内写入的资料、事件、关系、规则和设定。

这里是正式故事世界的可信基础。正式内容依然可以被修订，但修订必须形成新版本并保留历史。

## 5. 派生副本内容

由女娲、多元或其他派生能力基于某个明确故事来源与版本形成的副本内容。

派生副本可以包含自己的事件、Agent、关系、设定和临时分支。它们在副本内是真实有效的，但不会自动成为原始主线的正式内容。

## 6. 外部输出产物

由创作空间交给外部插件或工具后产生的小说稿、剧本、漫画脚本、分镜数据、互动叙事工程、翻译稿、视频工程或其他文件。

外部产物需要保留来源故事包、工具、版本、参数和回执。外部产物不能自动反向成为正式故事；如需回写，必须经过导入、对照和作者确认。

---

# 六、核心术语

以下名称用于本稿。名称可以在创始人确认后调整，但概念关系不能被随意改变。

## 原始主线

作者当前认可并持续维护的正式故事版本。

它包含当前有效的资料、事件、关系、规则、设定和世界状态。它不是永远不可修改的“唯一真理”，而是当前被作者选为主要版本的那条线。

## 派生副本

从原始主线或另一个副本的某个版本完整拎出的一份可独立演化的故事副本。

派生副本不是一个单独节点，也不等于临时分支。它拥有自己的内容、历史、Agent、事件线、时间线和后续变化。女娲副本、翻译副本、视角副本、IF 副本和本土化/改编副本都可以采用这一模型。

独立故事分支是具有稳定来源、版本身份和独立后续变化的故事方向。在本稿中，它通常由派生副本承载；一次筛选、一次女娲运行、一个预测结果或一组待审修改不会因此自动成为独立故事分支。只有作者明确选择长期保留，并建立来源与版本身份后，它才进入分支和副本管理。

“派生副本”是当前工作名。创始人曾使用“派生时间线”“副本”等表达，最终产品名称仍需确认。

## 临时分支

原始主线或派生副本内部，为了尝试某种局部可能性而形成的短期分叉。

临时分支可以比较、合并、保留、放弃或继续成长为新的派生副本。它比完整副本范围更局部。

## 候选历史与保留可能性

候选历史保存尚未采纳、拒绝或升级的 AI 候选及其来源、`BaseVersion`、上下文范围和生成依据。它是可恢复的工作历史，不是故事分支、历史版本或第二份事实库。R0 不静默永久删除候选历史；具体清理周期可以由产品策略后续确定，但清理必须可见、可恢复并由作者控制。

“保留可能性”是作者对候选的显式持久化操作。被保留的可能性跨会话存在，绑定来源故事、`BaseVersion`、上下文、证据和决策点，但不自动继续演化，也不拥有独立 Canon、Event、Relation 或 WorldState。只有作者明确选择“升级为 IF”后，它才建立派生副本与独立版本线。

来源故事继续变化时，基于较早版本的候选和可能性必须标记“基于旧版本”，不得被 AI 静默当成当前事实或自动重放到当前版本。

## 待合并变化

女娲、多元、事件预测或其他 AI 工作产生、已经可以对照但尚未写入目标故事的变化集合。

待合并变化可以包含候选事件、关系变化、Agent 状态变化、世界状态变化和写作建议。它必须绑定来源、目标、运行或候选身份、差异和作者决定，但不因此拥有独立 Canon、Event 或 WorldState。

它可以在事件线空间中作为只读叠加层观察，也可以进入候选审查、影响审查和选择性融入。“叠层”只是这一类展示和处理方式的待研究工作名，不是已经冻结的新领域对象或新架构。

## 历史版本

同一原始主线或派生副本在某次重要变化后的不可混淆版本记录。

历史版本用于对照、恢复、追溯和解释，不是当前仍在独立发展的故事分支。恢复旧版本应形成可追溯的新操作，不能抹掉后续历史。

恢复旧版本不是把历史指针倒退或删除后续版本，而是以所选历史状态为来源，在当前故事上建立一个新的可追溯版本。撤销采纳同样不删除历史；它针对明确的采纳版本创建补偿版本，并在回执中连接原操作与补偿操作。

## 采纳、结构化差异与回执

“采纳”表示作者让候选对当前明确目标故事正式生效。采纳发生前，系统必须展示目标故事、`BaseVersion`、变化范围、受影响对象、来源、冲突和风险；作者点击“采纳”就是对这些已展示内容作出唯一一次正式决定。采纳通过目标既有的唯一写入者原子提交，形成新版本、结构化回执和撤销入口；采纳之后不得再对同一个决定要求二次确认。

结构化故事语义 diff 是采纳的唯一权威依据。它应表达 Event、Relation、Canon、WorldState、Agent 状态、知识与记忆边界、叙事编排以及其他受影响故事对象的稳定身份和变化。文本或文件 diff 只能作为来源证据和表现补充，不能独立定义采纳内容。

系统可以展示面向作者的自然语言影响说明，但它必须从结构化 diff 派生，每一项都能追溯到对应对象和变化。Agent 的自由摘要、解释或回执说明不能自动成为采纳依据、Canon 或长期事实记忆。

结构化回执至少记录 `BaseVersion`、目标故事、变化范围、受影响对象、来源和安全的工具参数快照。密钥、令牌和其他敏感凭据禁止进入快照；大型输入只保存不可变引用、范围、摘要校验和必要哈希，不复制整份敏感正文。

RelationType 不明确时，包含该关系的采纳必须阻断。作者可以在采纳前明确把该关系排除出范围，再将剩余变化作为一个新的、完整的原子操作处理；系统不得用 AI 自由摘要替代关系类型，也不得产生半确认关系。

## 筛选与保存视图

作者针对同一批事件设置的查看条件，例如只看某个角色、某条暗线、某个时间范围、候选结果或某次女娲运行。

筛选视图只保存查询条件、显示范围、排序、折叠和叠加选择；它不复制事件，不建立新的故事身份，也不成为新的事实所有者。保存视图仍然只是可复用的观察方式。

## 同步

派生副本发现其来源版本发生变化后，将来源的新变化带入副本，并重新计算受影响内容的过程。

同步不是直接覆盖。系统需要区分：

- 来源版本新增或修改了什么；
- 派生副本自己增加了什么；
- 两边是否发生冲突；
- 哪些内容可以安全同步；
- 哪些内容需要作者决定；
- 同步后哪些推演或 Agent 状态需要重新计算。

重要变化需要询问作者是否同步。作者可以全部同步、局部同步、暂不处理或建立新的副本。

## 对照

把来源故事与派生副本，或两个故事分支，直观叠加比较。

对照需要显示：

- 新增内容；
- 修改内容；
- 删除或替换内容；
- 未同步的来源版本变化；
- 冲突；
- 受影响的 Agent、关系、物品、地点、记忆、知识和世界状态；
- 需要重新推演的区域；
- 每项变化的来源和原因。

作者可以对单个节点、连续片段、集点、单元、Agent 或完整副本进行批量保留、删除、重新生成或融入。

## 融入

作者把派生副本中的内容带回原始主线或另一个目标副本。

融入可以针对：

- 一个节点；
- 连续节点片段；
- 一个集点；
- 一个单元；
- 一个临时分支；
- 一个 Agent 或一组 Agent；
- 一组关系、物品或地点变化；
- 一整份派生副本。

融入不是把旧版本抹掉。它先形成绑定来源版本、目标 `BaseVersion` 和选择范围的结构化故事语义 diff，再由作者对当前明确目标执行一次正式采纳。系统在目标故事中创建新版本并保留此前历史；来源副本或 IF 的原版本不因被融入而自动改变，只增加可追溯的融入回执。

采纳永远针对当前明确目标生效。在 IF 中采纳只改变该 IF；从 IF 向原始主线或其他副本带回内容必须另行执行明确的“融入”，不得静默写入原始主线。

## 事件线与事件线空间

事件线是以事件为核心的故事语义结构，用来表达故事发生了什么、事件之间如何关联、故事如何推进，以及主线、支线、暗线、因果、伏笔和未解问题如何展开。每个原始主线、独立故事分支或派生副本都可以拥有自己的事件组织关系。

事件线不必完全按时间顺序排列。倒叙、插叙、信息揭示顺序和真实发生时间可以不同。

事件线空间是七个顶级产品空间之一，是统一管理、观察、筛选、叠加、比较和处理这些事件结构与相关结果的工作空间。它不等于单独一条物理事件线，也不为每个筛选条件、女娲运行或临时预测建立新的事件事实库。

事件线空间可以同时观察已确认事件、候选事件、预测和 IF 结果、女娲待合并变化、多元派生来源、冲突、归档和历史版本；这些内容必须保留各自的来源、版本、权威状态和所属范围。跨来源叠加只是观察与对照，不能静默把一个分支的事实写进另一个分支。

## 时间线

同样以事件为核心，但按故事世界中的时间顺序观察事件。

时间不明确时，应显示范围、相对顺序或不确定状态，不能为了画图而伪造精确日期。

事件线与时间线是同一批故事事件的不同观察方式，不应各自维护一套相互冲突的事实。

## 节点

故事中最小的完整表达块。

节点可以记录：

- 谁在什么地点、什么时间发生了什么；
- 动作与结果；
- 对白；
- 心理描写；
- 情感描写；
- 环境、风景和感官描写；
- 物品的出现、使用、转移或损坏；
- 关系变化；
- 角色知道了什么、误解了什么；
- 世界状态变化；
- 前因、后果、伏笔和未解问题。

系统内部可以把节点内容结构化，方便 AI 理解和检查；但不应强迫作者为了数据库而把一个自然完整的节点拆成很多难用的小表单。

## 集点

多个相关节点的组合。

集点用于表达一个局部段落、行动阶段、冲突阶段、场景组合或叙事节奏。它不是每个故事都必须使用的强制层。

“集点”是产品术语，不能未经创始人同意擅自改名为 Beat、情节段或其他名称。

## 单元

比集点更大的完整故事板块，可以对应一章、一幕、一个任务段、一个篇章或作者自定义的阶段。

单元是推演、审查、同步、对照、输出和版本管理的重要范围。

## Agent

天衍中的角色、物品、地点、组织、规则对象和自定义对象，都可以是 Agent。

Agent 不等于每个对象都长期运行一个大型模型。Agent 的本质是：它拥有稳定身份、状态、记忆、关系、规则、能力、知识边界和可被调用的响应方式。

---

# 七、Agent 系统

## Pi Agent 与领域权力

Pi Agent 是天衍当前优先采用、但必须可以替换的 AI Agent 运行底座。它可以承载模型循环、工具调用协议、流式输出、暂停、恢复、取消和重试等运行能力，但不能反过来定义天衍的故事语义。

Canon、Event、WorldState、来源、分支与版本身份、权限、持久化、候选状态和作者确认权必须继续由天衍领域层拥有。Pi Agent、Provider、Skill、插件和未来其他运行底座只能在天衍授予的范围内读取、推理、执行工具和返回候选或回执。

角色 Agent、物品 Agent、地点 Agent、组织 Agent 和规则 Agent 也不等于各自常驻一个 Pi Agent 实例。它们的稳定身份、状态、记忆、关系和知识边界属于天衍；是否以及何时调用某个运行底座，由当前任务、权限、预算和作者意图决定。

## 1. Agent 的共同结构

每个 Agent 都可以拥有：

- 稳定身份与名称；
- 类型与子类型；
- 当前状态；
- 长期属性；
- 记忆；
- 与其他 Agent 的关系；
- 所属地点与可活动范围；
- 能力和限制；
- 来源与可信度；
- 当前所属故事来源、分支或运行范围；
- 允许使用的工具；
- 可被谁调用；
- 是否允许后台活动；
- 变更历史。

不同 Agent 的表现不同：

- 角色 Agent 侧重目标、动机、认知、记忆、关系、选择和对话；
- 物品 Agent 侧重状态、持有人、能力、限制、历史和触发条件；
- 地点 Agent 侧重空间、环境、规则、容量、相邻关系、进入条件和事件记忆；
- 组织 Agent 侧重成员、结构、目标、资源、影响力和内部关系；
- 规则 Agent 侧重适用范围、条件、例外、代价和冲突；
- 自定义 Agent 按作者定义的能力、字段和行为参与故事。

## 2. Agent 的运行状态

Agent 可以处于：

- **休眠**：保留状态和记忆，不持续消耗资源；
- **按需响应**：被天意、女娲或作者调用时进行分析和回应；
- **事件触发**：相关事件或条件发生时更新状态；
- **后台活动**：在作者允许的范围、预算和时间内持续工作；
- **排演活动**：在某个女娲 Run、待合并结果、派生副本或临时分支中参与推演。

作者可以限制 Agent 的调用频率、工具、预算、上下文、可生成内容和是否允许创建子 Agent。

## 3. 未归类与已归类

所有 AI 新生成或从来源提取的 Agent，默认先进入“未归类”。

未归类 Agent 依然可以在当前对话、推演或副本中使用，但它们需要清楚显示来源、用途和所属范围。

作者可以：

- 修改名称和类型；
- 批量归类；
- 合并重复 Agent；
- 替换为已有 Agent；
- 保留为 Run、待合并结果或副本专属 Agent；
- 融入一个明确的目标故事；
- 删除；
- 要求 AI 重新生成。

归类状态、所属故事或运行范围、权威确认状态是三个正交概念。一个 Agent 可以已经归类，但仍然只是候选或只属于某次女娲 Run、待合并结果或派生副本；也可以尚未归类，却已经由作者确认并被允许在某个明确范围使用。任何界面都不得把“已分类 / 未归类”和“候选 / 待确认 / 已确认”混成同一组互斥状态。

## 4. Agent 辅助创建

创建角色、物品、地点和其他对象时，AI 辅助应是重要入口，而不是把所有工作都交给作者填表。

创建方式至少包括：

- 从一句话引导创建；
- 从天意对话提炼；
- 从导入来源提取；
- 从已有节点或单元生成；
- 从图片、关系图或地图理解；
- 自由手动创建；
- 从模板开始；
- 由女娲在派生副本中临时生成。

AI 起草后，应把已知、推断和未知内容分开。作者可以逐项确认，也可以按区块或批次确认。

## 5. 角色 Agent 档案

角色的简单创建可以只需要：

- 角色名；
- 角色定位；
- 一句话介绍；
- 已知来源。

完整档案可按需展开：

- 身份与别名；
- 出生信息；
- 当前故事时间下的年龄；
- 角色定位；
- 介绍；
- 生平；
- 备注；
- 角色核心或“角色灵魂”；
- 欲望、恐惧、底线和价值观；
- 智力与判断方式；
- 文化水平与知识领域；
- 素质、习惯和性格；
- 外貌、发型、穿着和辨识特征；
- 能力、属性、武器和“金手指”；
- 长期记忆；
- 短期记忆；
- 最近记忆；
- 知道、相信、怀疑、误解和不知道的内容；
- 角色关系图谱；
- 核心关联关系；
- 出现的节点、集点、单元、章节和相关经过；
- 角色成长变化；
- 丰富度、一致性和缺失项检查。

年龄、关系、最近记忆和出现位置应能随事件和时间变化自动投影，避免作者在多个页面重复维护相互冲突的字段。

### 角色命运 K 线

角色命运 K 线不是只读统计图，也不是用一个分数评价角色好坏。它是作者设计、观察、比较和解释角色长期命运变化的工作面。

作者可以直接设计和拖动目标轨迹。AI 根据作者调整后的目标，定位可能需要修改的事件、连续片段、集点或单元，说明受影响的剧情区间，并提出一个或多个可审查的剧情方案。目标轨迹本身不直接改写正式事件；AI 方案仍需经过候选、影响审查和作者确认。

命运变化由多个维度共同造成。完整产品需要表达可解释的维度变化，但默认只展示综合命运波动，其他维度折叠到需要时再展开。具体维度名称和计算方式必须有来源、规则或作者判断，不能由模型给出无法解释的神秘分数。

命运 K 线至少支持对照：

- 已确认事件形成的实际轨迹；
- 作者直接设计的规划轨迹；
- 尚未确认的候选轨迹；
- 一次或多次女娲排演形成的轨迹；
- 原始主线、IF、翻译、改编和其他故事分支中的轨迹。

每个轨迹变化都必须能够回到关联事件、来源版本、变化原因、角色当时的知识边界和仍然未知的内容。不同分支的轨迹只能显式对照，不能静默借用或合并。

## 6. 物品 Agent 档案

物品的简单创建可以只需要：

- 物品名；
- 物品定位；
- 一句话介绍。

完整档案可按需展开：

- 类型与子类型；
- 外观、材质、尺寸和状态；
- 来源与制造者；
- 当前持有人；
- 多个持有人；
- 不同时间段的持有人；
- 所在地点；
- 能力、属性、限制和代价；
- 使用条件；
- 损坏、修复、遗失和转移历史；
- 与角色、地点、事件和其他物品的关系；
- 象征意义；
- 在剧情中的作用；
- 已知者与未知者；
- 相关节点、集点和单元；
- 丰富度、一致性和冲突检查。

物品持有人不是一个永远不变的文本字段，而是一条可按时间和事件追踪的关系。

## 7. 地点 Agent 档案与地图

地点的简单创建可以只需要：

- 地点名；
- 地点定位；
- 一句话介绍。

完整档案可按需展开：

- 所属区域与包含关系；
- 相邻地点；
- 大小与容量；
- 方向；
- 距离或移动耗时；
- 通行方式；
- 进入条件；
- 时间、天气或身份限制；
- 环境、气候、光线、声音和气味；
- 建筑、道路、房间和关键设施；
- 常驻角色、组织、物品和资源；
- 历史事件；
- 当前状态；
- 世界规则；
- 相关节点、集点和单元；
- 风险、秘密和未解信息。

地点需要一套简单地图。它不是一开始就要求作者使用复杂制图工具，而是用清楚的节点、区域、包含、相邻、方向、距离和通行关系表达空间。

前台可以是简单地图、拓扑图或可切换的列表；后台必须保持 AI 可理解的结构化空间关系。高级制图工具可以通过插件接入，但核心空间关系不能只存在于一张 AI 无法可靠理解的图片里。

## 8. 自定义 Agent 与分类库

美食、坐骑、家具、服饰、武器、法术、种族、制度、疾病、技术、交通工具等，都可以建立自定义 Agent 类型。

AI 在分析长篇来源时，可以建议新的分类；作者决定是否建立永久类型、合并到现有类型，或暂时保留在未归类。

自定义类型需要：

- 名称与说明；
- 基础能力类型；
- 字段结构；
- 默认显示方式；
- 与事件、关系和其他 Agent 的连接规则；
- Agent 能力与权限；
- 版本与迁移历史。

---

# 八、天衍的八个空间

八个空间不是八套彼此无关的软件，也不应各自维护第二份故事数据。它们是同一个故事世界的八种主要工作视角。

## 1. 世界

世界是整个作品的首页和总览。

它回答：

> 我的故事世界现在处于什么状态，我接下来最需要关注什么？

世界首页可以呈现：

- 当前作品与当前正式版本；
- 当前写作位置和当前单元；
- 最近确认的事件；
- 当前主要角色、地点和组织；
- 主线、支线和暗线的进度；
- 当前故事世界时间；
- 未解决的伏笔、问题和冲突；
- 未归类 Agent；
- 等待作者确认的候选；
- 正在运行的女娲 Run 与等待同步、审查或合并的结果；
- 多元派生副本的状态；
- 最近修改和版本变化；
- 世界丰富度与一致性检查；
- 数据统计与日志解释；
- 下一步建议和常用功能导航。

世界首页不应把所有数据一次堆满。第一屏突出当前作品、当前状态、最重要异常和一个清楚的下一步；更详细的数据通过展开、下钻和右侧天意 Agent 查看。

点击世界空间标题应回到世界首页。

## 2. 天意

天意是作者与 AI 交流、理解、整理和执行的主要入口。

天意是一个统一的智能底座。每个连续创作上下文采用一个 `TianyiConversation`，内部包含 `Creative lane` 与 `Work lane` 两条泳道；它们不是两个互不相关的会话 Owner。

两条泳道共享作者与项目身份、统一可见且带泳道标记的历史、来源、附件、显式引用和候选注册表。每条消息、候选、回执和上下文引用都必须保留所属泳道、来源与 `BaseVersion`；全局可见不等于自动进入另一泳道的 ContextPack。

两条泳道分别保留自己的未发送草稿、当前范围、临时选择、上下文装配和工作进度。模式切换不能清空任一泳道，也不能丢失统一会话历史。创意内容进入正式工作范围、工作结果回到创意探索，均通过显式引用、转为候选或其他可追溯交接完成。

天意大页面提供创意模式与工作模式。侧边天意提供工作模式，并且只在支持页面专属能力的页面提供 Agent 模式。大页面与侧边栏的工作模式是同一 `Work lane` 的不同表面，共享工作消息、来源、候选、目标故事、`BaseVersion`、审查状态、Agent Run 索引和回执；未发送草稿、光标、滚动位置、展开状态、页面选择、临时筛选、工具输入态和布局按表面隔离。工具一旦执行，实际使用的安全参数快照必须进入 Run 回执。

### 创意模式

创意模式适合只有一个想法、想通过聊天逐步把故事说清楚的作者。

它类似会议记录，但不是单向口述。天意应像一个真正理解故事的合作者：

- 通过文字或语音和作者自然聊天；
- 在合适的时候提问，而不是不断打断；
- 记录作者原话；
- 生成可编辑的会话摘要；
- 提炼角色、物品、地点、组织、事件、关系、规则、剧情想法、灵感和创意；
- 标记不确定问题和冲突；
- 把提炼结果送入候选审查；
- 允许作者继续聊、修正或忽略提炼结果。

语音模式需要显示：

- 当前是否正在录音；
- 实时转写；
- 天意的语音回应；
- 说话人与时间；
- 哪些内容被提炼；
- 可以随时编辑的转写；
- 隐私和保存范围。

创意模式不是“按下录音后让 AI 自己总结”，而是人与 AI 共同把模糊想法逐步发展为可用的故事世界种子。

### 工作模式

工作模式接近 Codex 式精细工作，用于把已经形成的想法收敛为明确任务、候选、差异、作者决定和可恢复结果。

作者可以：

- 查询资料、事件、关系、版本和来源；
- 增删查改明确对象；
- 把左侧工程目录中的角色、物品、地点、节点、单元或文档拖入对话；
- 使用“添加引用”按钮代替拖拽；
- 上传大量文字、图片、地图和关系图；
- 要求天意解释、对照、补全、重写、检查或批量处理；
- 预览修改前后差异和影响；
- 对一个操作、一个批次或一个范围进行授权；
- 查看完整操作回执并撤销。

引用必须指向稳定对象和版本，不能只复制一段失去来源的文字。

### 天意中的 Agent 管理

在支持天意专属能力的页面打开侧边 Agent 模式时，它主要负责：

- 当前会话管理；
- 上下文清单；
- 已引用对象；
- 长对话压缩与清理；
- 来源和附件管理；
- 候选合并与查重；
- 会话目标、预算和权限；
- 与指定故事 Agent 对话；
- 把复杂任务拆为受控步骤。

### 天意 UI

天意大页面必须清楚看到“创意模式 / 工作模式”的切换；侧边天意必须清楚看到“工作模式 / Agent 模式”的切换，并且只在当前页面声明了专属能力时开放 Agent 模式。

页面应包含：

- 当前作品和当前会话；
- 模式切换；
- 会话主区；
- 语音入口；
- 引用与上传入口；
- 候选摘要；
- 可开关的会话历史；
- 与同一 `Work lane` 相连的侧边天意；
- 清楚的发送、停止、继续和撤销状态。

空会话不应预留一整块没有意义的会话栏；有历史时再展开历史抽屉或列表。

## 3. 事件线

事件线是统一管理和观察故事事件、发展关系与多种故事来源的核心空间。它首先是一个顶级工作空间，不等于单独一条物理事件线。

它回答：

> 在当前选择的故事来源和版本中发生了什么，不同故事方向与待合并结果怎样连接、冲突或演化，哪些内容还没有解决？

### 事件线空间管理的内容

事件线空间可以统一查看、筛选、叠加、比较和处理：

- 正常故事事件；
- 主线、支线和暗线；
- 已确认事件与候选事件；
- 局部预测结果和作者保留的 IF 线；
- 同时存在的多条女娲待合并结果；
- 翻译、视角、本土化、改编和其他派生来源；
- 来源冲突、待处理冲突、归档内容和历史版本。

这些内容可以在同一个空间中被选择和对照，但不会因此获得相同的权威状态。一个已确认 Event、一个 Candidate、一次 Nuwa Run 的结果、一条独立故事分支、一个历史版本和一个筛选视图必须保持不同身份。

事件线空间中的四种重要边界是：

1. **独立故事分支**具有来源和版本身份，可以长期独立演化；
2. **待合并变化**是绑定来源与目标的候选差异，不自动成为独立分支；
3. **历史版本**是同一故事身份过去的状态，用于对照和恢复；
4. **筛选或保存视图**只保存观察条件，不复制事件或建立新的故事身份。

不需要为每一种筛选条件、每次女娲运行或每个临时预测建立独立事件线。只有当作者明确要长期保存某个故事方向，并建立来源与版本身份时，它才成为独立故事分支。

### 事件线的结构

事件线以节点为基础。单元是侧栏中的容器，可以直接包含节点；集点是引用已有 Event 的结构化编排容器，不是强制层级，不复制 Event，也不等于 Story Unit 或故事分支。R0 的集点不支持嵌套；一个 Event 在同一 Story Unit 内最多有一个主要集点归属。主线、支线、暗线、角色线等是事件之间的故事关系和观察轨道，不是各自复制一份 Event 的数据库。

#### 正式叙事编排权威

Event 的叙事位置属于既有 Story Structure／Story Unit 权威，不属于 Event 自身。正式模型是版本化 `NarrativeArrangement` 与具有稳定身份的 `NarrativePlacement`：Placement 只引用 Event ID，并明确绑定当前 story/work、WorkVersion 来源谱系、既有 Story Unit 叙事路径、目标 Story Unit 和 arrangement revision。同一个 Event 可以因倒叙、回忆、再次揭示、不同分支或派生作品而拥有多个 Placement；不得复制 Event 来表达重复呈现，也不得给 Event 增加一个全局单值 `narrativeIndex`。

`linkedEntityIds` 与 Collection Point `eventIds` 继续只表示无序成员关系，不能承载作者叙事顺序。正式顺序只能由 Story Unit 唯一 Writer 通过带 expected version、receipt、冲突和回滚的作者操作写入。UI、localStorage、画布坐标、世界时间、Event ID、标题、创建时间、API 返回数组和 AI 推断都不是正式排序 Owner。

旧项目没有正式 arrangement 时，所有 Event 必须读取为 `unplaced`，不得自动补造 confirmed Placement，也不得在读取时写回。AI 或 UI 未来只能提出候选；候选经 AuthorControl 确认后，仍由同一个 Story Unit Writer 更新正式 arrangement。不同 WorkVersion 来源谱系和既有 Story Unit 分支保有独立 arrangement identity 与版本；筛选、视觉相交和普通保存视图不创建分支或合流。

它需要支持：

- 主线；
- 支线；
- 暗线；
- 角色线；
- 地点线；
- 物品线；
- 伏笔线；
- 自定义故事线。

相邻不自动等于因果。因果、影响、信息传播、物品转移和关系变化都需要明确记录或标记为推测。

### 事件线的主要视图

事件线按作者任务分为三组：`结构`中的故事脊柱，`编排`中的事件线与时间线，以及`观察`中的关系与视角。故事脊柱负责纵向层级、单元、主干和分支；事件线按横向叙事顺序推进；时间线使用独立的时间编排；关系与视角作为观察投影作用于同一批 Event。它们复用 Event ID、来源版本和正式事实，但不强制复用同一种节点外观、端口、位置或全部 Relation，也不创建第二套 Event。

#### 故事结构

最快看出故事进度、主要冲突、主线、核心分支、待合并结果和开放问题。故事结构强调叙事推进，不要求严格按世界时间排序。

#### 编排 · 事件线

事件线默认从左向右表达叙事推进，纵向轨道表达主线、支线、暗线或独立路径。分支从真实分支点展开，合流明确回到目标轨道；新增分支不能让已经固定的主干整体跳动。缩远优先展示集点、分支和主路径，中景展示关键 Event，近景才展示完整事件内容。

#### 观察 · 关系

用可视方式查看节点、集点、单元以及它们与人物、地点、物品、关系和因果的连接。

画布需要语义缩放：

- 缩远时看到单元和主脉络；
- 中等缩放看到集点和重要节点；
- 放大时看到完整节点、描写、关联 Agent 和状态变化。

#### 编排 · 时间线

按故事世界中的时间观察事件。

它支持明确时间、相对时间、时间范围、未知时间和多地并行事件。AI 可以根据证据提出时间候选，但不能把推断直接当成确定时间。

时间线是引用同一批 Event ID、正式时间、关系证据和来源版本的独立时间编排组件，不是关系图换一层背景，也不建立第二套时间事实库。它可以使用适合时间语义的点、区间块、轨道和端口，不要求复用关系图节点外观、位置或全部关系边。默认只显示严格时间约束、当前选择所需的因果链和作者主动开启的关系叠加。

连续 X 轴表达正式或相对故事时间，Y 轴表达作者选择的稳定轨道；明确时间使用正式锚点，模糊时间使用区间，未知时间进入未定位托盘，冲突进入冲突区域。故事分支可以在世界时间上与主线交错，不能因为属于分支就被排到最后。

#### 观察 · 视角

视角是只读观察投影。作者可选择 1–5 个正式人物、地点或物品 Owner 对象：选择一个对象进入“单人视角”，选择 2–5 个对象进入“比较视角”。单人视角默认只展示该对象有正式证据的亲历、目击、参与、被告知、推断或误解事件；没有正式证据时显示诚实空状态。“作者知道但角色不知道”的盲区属于默认关闭的独立观察开关，打开后必须明确标记“角色未知”。比较视角展示共同事件与各对象不同的知情状态，不能用单人投影或简单交集冒充。角色视野与第一人称正文改写是两个不同产品合同；后者仍属于多元派生副本，不在视角观察中执行。

切换对象、取消对象或在单人/比较模式间切换，只使用正式 Event 元数据、正式 Owner 引用、已确认关系与缓存候选，Provider 调用为零。深度语义分析允许恰好一个或 2–5 个版本化 Owner 引用，但必须由作者从唯一 AI 工具入口确认范围、tokens 和费用后触发。视角观察不是多元中的第一人称正文派生，不写入 Event、Relation、Canon 或 WorldState；第一人称正文改写属于多元派生副本。

已有明确时间、相对时间和时间范围是可信时间锚点。正式时间未确认的 Event 不得默认堆入最后一个“时间未定”栏；Tianyi Agent 可以结合版本化事件引用、已确认关系、时间约束和故事语义，为其提出连续坐标上的 inferred temporal position。该位置始终是只读投影，不是正式 worldTime；产品必须标明“正式时间未确认 · AI 推断位置”，不得将推断静默写入 Event、Canon 或 WorldState。只有完全缺乏可用证据的节点才进入画布边缘的“暂无法定位”状态，这是异常兜底，不是默认大栏。

时间投影采用作者触发的有界建模合同，并按 graphRevisionHash、来源清单与输入版本缓存。切换故事脊柱、关系图或时间轴，缩放、平移、React 重渲染、刷新页面或重新打开项目时，Provider 调用与新建 Agent Run 都必须为零。缓存为 current 时直接显示；为 stale 时可以继续显示旧投影，但必须标记变化范围并给出更新建议；为 missing 时显示基于正式 Event / Relation 的本地确定性“基础布局”，不得把它标记为 AI 推断。只有作者明确点击 AI 工具、确认范围与费用预估后，才可用 author-requested 触发一次有界 temporal projection Run；失败后也只能由作者以 author-retry 显式重试。Dialogue 模式保持普通对话，不出现时间推断、Run、重试或内部执行控制。

正式 Relation 使用正式实线，Relation candidate 使用待确认虚线；切换故事脊柱、事件线、关系、时间和视角投影时，同一 Event ID、来源版本与当前选择必须保持可追踪。各投影只显示完成其任务所需的关系与视觉语法，不强制复制关系图的全部 Relation。时间视图保持只读；拖动只可形成临时视图覆盖，不能直接改写 Event 的世界时间。正式时间采纳必须以后经过 Event owner 和 AuthorControl 的独立合同。

事件画布使用统一输入语法：普通点击单选，Shift/Ctrl/Cmd 点击切换多选，空白拖动框选，Space 或中键拖动画布，滚轮缩放，空白双击只打开带画布位置的 Event 草稿检查器，右键或 Shift+F10 打开与对象身份匹配的菜单，Escape 关闭临时菜单但不清空推演依据。画布通用选择与 Tianyi 的 predictionSelectionIds 必须分离；只有作者明确执行“围绕所选节点推演”时，通用选择才成为一次推演输入。

剧情逻辑检测分两级。悬空关系、版本失效、重复 ID、严格时间循环、孤立单元引用、已删除引用、未决关系类型和过期缓存由本地确定性检查完成，零 tokens；因果缺口、动机断裂、知情边界、物品和地点连续性、时间合理性、伏笔回收、故事线脱节与情绪节奏弧属于作者触发的 AI 语义检查。AI 结果必须保留置信度、证据、影响和作者处理状态，只能生成候选或建议。

故事建模的正式来源清单优先使用现有 owner 中版本化的章节、场景和导入文档；Event 只作为结构化证据，不能冒充全书正文。增量建模只把变化来源及必要依赖送入 Provider，全书建模按有界分块提取并全局合并。StoryModeling 的真实实现只能通过服务端现有 aiProviderGateway；未配置 Provider 时应诚实显示不可用，基础布局、范围规划与本地检查仍可使用。测试 Provider 只用于自动化验证，不能冒充真实 AI 能力。

### 跨来源观察与对照

作者可以选择一个主要故事来源，再显式叠加其他分支、候选、预测、女娲结果或历史版本。系统需要持续显示：

- 当前主要来源和版本；
- 叠加内容来自哪里；
- 哪些是已确认事实，哪些仍是候选或推演结果；
- 当前筛选范围；
- 冲突、缺失和过期来源；
- 目标写入位置和进入作者审查的动作。

叠加、筛选和比较本身都是只读观察。任何融入或修改都必须先形成明确差异，再经过对应目标的作者确认和唯一写入链。

#### 结构目录

事件线打开时，左侧工程目录应自动聚焦“剧情”，以树状方式看到单元、集点和所有节点。

作者可以快速定位、搜索、折叠、拖动引用、复制节点 ID 或选择连续片段。这里是统一工程目录对剧情结构的聚焦，不是另一套独立数据。

深蓝八空间导航是一级导航；顶栏“目录”打开的工程目录是二级导航。二级目录首页只展示类别、单元与数量，不得默认平铺全部 Event，也不得一次自动展开所有 group 和 category。作者进入“故事结构 → 单元 → 某个单元”后，才看到该单元的直接节点、可选集点和其他真实内容，并且必须有稳定的面包屑或返回路径。

同一个 Event 在目录中只有一个稳定身份。Event 的版本、来源、规划依据、正式依据、立即揭示和其他投影不能成为同级的重复目录节点；这些信息进入事件检查器、来源详情或版本历史。集点只引用或组织已有 Event，不复制 Event，也不成为强制层级。搜索可以跨层定位，但结果必须标明所属单元和目录路径，并把作者带入对应二级页面，而不是展开整棵目录树。Candidate 在作者采纳前不得进入正式目录。

### 事件线预测

事件线预测是轻量、直接的作者辅助。

作者可以选择一个或多个已有节点作为天意推演依据；连续片段和单元只是帮助选择这些已有节点的方式，不能创建重复的节点身份。天意可以给出若干种可能发展。每一种发展都可以包含连续节点链，也可以分叉或合流；它们必须保留所依据 Event 的身份和版本。

这些候选只用于预览，不写入正式事件线。画布中的候选节点和候选关系必须与正式 Event / Relation 清楚区分，不能因为可见、被比较或被选中就成为事实；故事脊柱、关系图与时间轴始终投影同一份正式 Event / Relation 数据。

AI 输出 CandidateSubgraph 时必须同时输出候选节点与候选边，并明确边的方向、作者可读标签、所属路径、共享节点成员关系和来源证据。包含两个或更多节点的候选路径如果没有连接这些节点的路径边，必须被合同拒绝；候选节点不能以无连接方框散落在画布中。系统负责自动创建可见连线、选择端口、从左向右布局和自动路由，分叉表达一对多、合流表达多对一，共享节点和共享边都只渲染一次，不要求作者手工逐条拉线。

这些自动边首先是 Candidate，不是正式 Relation。作者部分采纳节点时，只能保留两端都被采纳或引用的边，不能留下连接被跳过节点的悬空线；Candidate ID 到 draft Event ID 或 existing Event ID 的映射必须可恢复、可重放并保持幂等。符合已有 RelationType 的边仍需通过唯一 Relation Owner 进入结构化采纳范围。无法安全匹配已有 RelationType 时，边必须明确标为“关系类型待确认”，系统不得自动创建 RelationType，也不得把叙事相邻静默猜成因果；包含该边的采纳被阻断，除非作者在采纳前明确排除该关系，并把剩余变化作为新的完整原子操作处理。

作者可以：

- 直接编辑候选；
- 审阅并采纳整条候选路径或其中部分节点；
- 保存草稿、保留候选或保留可能性，而不改变正式故事；
- 将明确选择的长期方向升级为 IF 派生副本；
- 删除；
- 提供新想法后重新预测；
- 把候选送入更深入的女娲推演。

保存草稿、保留候选和保留可能性都不叫“采纳”，也不改变正式故事。作者选择采纳时，系统先从结构化故事语义 diff 展示目标故事、`BaseVersion`、范围、来源、影响与冲突；作者点击“采纳”就是唯一一次正式决定。通过版本与完整性校验后，既有 Event 与其他领域 Owner 通过唯一写入链原子建立目标故事的新版本、结构化回执和撤销入口，不再追加第二次确认。采纳目标如果是 IF，则只改变该 IF，不得静默写入原始主线。

未选候选自动进入可恢复的候选历史。作者选择“保留可能性”后，它跨会话持久但仍不是正式事实；只有“升级为 IF”才建立派生副本与独立版本线。

默认候选数量大于等于一个。只有一个合理可能时，应诚实给出一个，不需要为了数量制造表面差异。

事件线预测与女娲不同：它是围绕当前局部范围的快速建议，主要由作者直接决定；女娲则基于作者选定的故事来源，在有界运行或派生副本中进行持续、自主和更深的演化。

天意始终位于全局最右侧。普通作者只看到天意、来源、候选、审阅与草稿结果，不暴露 Pi Agent 或内部编排、模型循环和工具执行细节。

### 剧情 AI 检测

事件线需要长期维护故事问题，而不是只临时弹出一句建议。

它可以检测：

- 未回收伏笔；
- 未回答问题；
- 未完成承诺；
- 人物动机断裂；
- 角色知识越界；
- 时间冲突；
- 地点移动不合理；
- 物品持有人冲突；
- 关系变化缺少经过；
- 世界规则被破坏；
- 主线停滞；
- 支线失联；
- 暗线提前暴露；
- 节奏和情绪失衡；
- 剧情想法与当前事件不一致；
- 灵感或创意中存在可对齐的内容。

每个问题都应有来源、影响范围、严重程度、AI 建议、作者决定和处理状态。

### 事件线 Agent 模式

右侧天意 Agent 在事件线中可以：

- 读取当前选择；
- 增删改查节点；
- 拖入或引用节点 ID；
- 检查因果；
- 补充对白、心理、环境和情感描写；
- 预测后续；
- 查找伏笔和剧情缺口；
- 比较两个片段或副本；
- 解释某个角色为什么会做出某种选择；
- 预览修改会影响哪些 Agent、关系和世界状态。

### 事件线 UI

事件线应使用和全产品一致的顶部、按钮、状态和详情样式。

桌面端可以由以下部分组成：

- 顶部视图切换；
- 左侧剧情结构目录；
- 中央脉络、画布或时间视图；
- 可切换的详情与关系面板；
- 最右侧天意 Agent。

中等宽度不应同时挤压所有栏。详情与天意 Agent 可以切换显示。移动端使用全屏层或抽屉，不允许透明侧栏覆盖内容。

## 4. 多元

多元来自“多元宇宙”的产品概念，负责把选定故事来源发展为不同故事分支、IF、预测、翻译、视角、本土化、改编和其他派生可能性。

它回答：

> 如果保持来源可追溯，这个故事还可以用另一种语言、视角、前提或文化环境怎样存在？

多元不是把所有能力堆在一个复杂页面里。它需要让作者清楚选择派生目的、来源和已有版本，并进入适合该任务的工作流程；最终首页、页面数量和信息架构仍待研究，不能在产品核心中提前冻结为四个永久独立页面。

### 翻译

建立翻译派生副本，保持角色、事件、关系、术语、称谓、文化说明和来源版本的对齐。

翻译副本可以在来源更新后由作者手动拉取变化，并清楚看到哪些段落需要重新翻译、哪些专名发生变化、哪些本地化选择需要作者确认。

### 视角切换

从另一个角色或观察者的知识边界重新组织故事。

它必须区分：

- 角色真正知道的内容；
- 角色相信或误解的内容；
- 角色看不到的内容；
- 叙述者可以补充的内容。

视角切换不是简单把“他”改成“我”，而是重建信息、情绪和叙事重点。

### IF 线

从一个明确改变的前提开始，形成独立的派生副本或临时分支。

系统需要记录改变了什么、从哪里开始、哪些原始内容仍有效、哪些后续必须重新推演。

### 本土化 / 改编

为了不同文化、时代、媒介、受众或发行环境，对故事进行有合同和边界的转化。

它可以改变名称、环境、制度、节奏、表达方式和媒介结构，但必须保留来源对照，并明确哪些变化是翻译、哪些是改编。

### 多元副本管理

每种多元结果都不是一次性导出文件，而是可以持续维护、同步、对照和继续创作的派生副本。

多元默认不能反向覆盖其来源故事。作者可以手动拉取来源更新、查看差异、选择同步范围或继续保留当前版本。跨分支覆盖和融入属于高风险操作，必须明确来源、目标、冲突、影响、作者权限和回退方式。

多元的管理体验至少需要呈现：

- 可选的派生目的；
- 已有派生副本；
- 来源版本；
- 同步状态；
- 最近修改；
- 冲突；
- 待作者确认；
- 可以继续的下一步。

### 多元与女娲的区别

多元通常有明确的转换目标，例如翻译、视角、IF 前提或本土化合同。

女娲更像开放的故事模拟器和高级虚拟机，会在约束下持续推演整个故事世界。

两者都可以使用派生副本、同步和对照，但目的不同。

### 多元的稳定边界与未冻结事项

多元负责管理派生关系、来源版本、同步状态、差异、冲突和作者选择，但不能成为第二个 Event、WorldState 或 Canon 所有者。派生内容在自身分支内有效；向其他故事目标融入时，仍复用目标已有的候选、影响审查、版本和正式写入边界。

以下事项尚未冻结，后续可以研究和提出方案，但不能冒充创始人已经确认的产品定义：

- 多元页面的最终信息架构；
- 忠实翻译与改编式翻译在底层是否采用同一种分支模型；

临时预测、候选或保留可能性不会自动成为长期故事分支。只有作者明确执行“升级为 IF”，并选择来源故事与 `BaseVersion` 后，系统才建立派生副本和独立版本线；这一升级门槛已经确认，不再属于未冻结事项。

## 5. 女娲

女娲既是故事模拟器，也是高级虚拟机。

它不是凭空生成一段续写，而是选择一个明确的故事来源与版本作为工作蓝本，再在作者指定的范围、目标、权限和停止条件内，让事件、Agent、关系、记忆、地点、物品和世界状态继续演化。

女娲的工作蓝本可以来自：

- 当前主故事；
- IF 线或预测线；
- 翻译、改编、视角或其他派生版本；
- 人工新增或长期保留的其他故事分支；
- 特定单元、事件范围或故事 Agent 范围。

选择来源不等于复制全部故事，也不等于自动建立长期分支。女娲既可以在有界、可回放的运行沙盒中工作，也可以在作者明确保留的派生副本中持续演化。

### 女娲工作蓝本、Run 与待合并结果

女娲需要分清下列概念：

- 工作蓝本是被选中的故事来源、版本和作用范围；
- Run 是一次可记录、暂停、恢复、回放和结束的执行过程；
- 候选结果是 Run 提出的未确认变化；
- 待合并变化是作者保留、等待差异审查的结果，不是新的事件所有者；
- ChangeSet 是通过审查后、准备作用于明确目标的写入计划；
- 只有作者明确要求长期保留，并建立独立来源和版本身份时，结果才成为派生副本或故事分支。

同一个或不同的工作蓝本可以同时有多次 Run 和多份待合并结果。它们必须各自保留来源快照、范围、导演目标、作者约束、权限、运行历史、停止与恢复点以及对照关系，不能因为在同一页面展示就混成一条故事线。

### 女娲的默认工作优先级

女娲默认应优先：

1. 理解当前故事；
2. 检查并修复问题；
3. 补全必要缺口；
4. 丰富已有内容；
5. 加强结构、因果、节奏和情感；
6. 只在作者明确要求时，再优先创造大量新故事。

这个默认顺序不限制女娲的创造能力。在作者授权下，它仍可以创造主线、支线、暗线、角色、物品、地点、组织、世界观和规则；“完善故事”默认高于“另造故事”。

### 女娲始终理解选定来源的变化

女娲不是一次读取后永远脱离选定来源。

来源发生变化后，女娲应提示：

- 来源发生了哪些变化；
- 当前 Run、候选、待合并结果或派生副本的哪些部分受到影响；
- 是否同步最新内容；
- 可以全部同步还是局部同步；
- 哪些已有推演需要保留、调整或重新生成；
- 是否出现冲突。

作者决定是否同步。同步后，女娲保留被允许继续存在的增设内容，并根据新的来源版本重新理解受影响区域。

### 女娲的两层结构

#### 女娲导演层

负责：

- 理解作者目标；
- 选择推演范围；
- 调度 Agent；
- 控制节奏；
- 处理世界规则；
- 决定在授权工作范围内如何继续；
- 生成临时分支；
- 检查故事是否圆满；
- 解释关键选择；
- 控制预算、停止条件和后台运行。

#### Agent 层

由角色、物品、地点、组织、规则和自定义 Agent 组成。

角色依据自己的知识、记忆、目标和关系行动；物品和地点依据状态、能力与规则响应。Agent 不共享不该知道的信息。

### 女娲可以生成什么

女娲可以在授权的 Run、待合并结果或派生副本内生成：

- 节点、集点、单元和临时分支；
- 对白、动作、心理、情感和环境描写；
- 角色 Agent；
- 物品 Agent；
- 地点 Agent；
- 组织、规则和自定义 Agent；
- 关系变化；
- 记忆和知识变化；
- 伏笔、线索和未解问题；
- 世界状态变化。

女娲通常应谨慎修改主要角色、核心历史和根本规则，但这是可配置的导演权限，不是女娲永远不能做到。临时工人、路人、传信者、短期地点和剧情所需物品，可以在允许范围内生成。

所有新 Agent 默认先进入未归类，并清楚显示只属于当前 Run、待合并结果或派生副本，直到作者决定将它融入某个明确的目标故事。

### 女娲的运行方式

作者可以：

- 选择来源单元、连续片段或完整范围；
- 给出问题、目标和禁止事项；
- 选择参与 Agent；
- 让女娲自己补充必要的临时 Agent；
- 限制生成数量和类型；
- 设置预算和运行长度；
- 单步运行；
- 连续运行；
- 暂停；
- 恢复；
- 从某一步创建临时分支；
- 回放；
- 比较多个结果；
- 与某个 Agent 对话；
- 通过右侧天意 Agent 给导演下指令；
- 让女娲解释为什么发生了某项变化。

### 女娲结果的阅读与对照

女娲需要一套像事件线一样可阅读的界面，而不是只有任务进度和结果文本。

作者应能：

- 沿女娲事件线阅读完整推演；
- 在时间视图观察先后关系；
- 看到临时分支；
- 把选定的目标故事与一份或多份女娲结果叠加；
- 看出增加、修改、替换和删除的内容；
- 查看新增 Agent 和关系；
- 在多次 Run 和多份待合并结果之间切换、筛选和比较；
- 选择单个节点、连续片段、完整分支或完整副本；
- 批量保留；
- 批量删除；
- 要求重新生成；
- 融入当前作用的目标故事；
- 继续保留为独立副本。

### 女娲的受控合并

女娲结果可以选择性合并回它当前作用的目标事件线或故事来源，但不得静默写入。每次合并必须具备：

- 明确的来源、目标、版本和作用范围；
- 对应对象与操作的权限设置；
- 在写入前由结构化故事语义 diff 生成的差异与影响审查；
- 每项变化的来源、理由和作者决定；
- 可撤销、可回退且可溯源的正式写入回执。

合并复用目标故事已有的候选、影响审查、版本和正式写入边界。作者点击“融入”时，就是对已展示范围作出的唯一正式决定；成功后在目标故事建立新版本和回执，来源结果保持不变。女娲不因为能够提出变化，就成为第二个 Event、WorldState 或 Canon 所有者。

### 女娲与事件线预测的区别

事件线预测是作者围绕局部剧情获得可直接编辑的虚线候选。

女娲基于选定故事来源，在有界运行或派生副本中工作。它可以在授权范围内自行决定、持续演化、创建 Agent 和临时分支。作者不需要每一步都批准，但结果默认只是可审查的候选；只有作者选择融入后，它才进入明确的目标故事。

## 6. 资料

资料空间负责管理故事世界中的对象、设定、来源和结构化知识。

它回答：

> 这个世界里有哪些人、事、物、地点、关系、规则和创作资料，它们从哪里来，现在处于什么状态？

### 资料首页

资料首页应提供：

- 全局搜索；
- 按类型浏览；
- 最近更新；
- 待确认；
- 未归类；
- 导入与审查；
- 自定义类型；
- 文件夹与来源；
- 关系；
- 地点地图；
- 批量 AI 整理；
- 最近使用；
- 明确的创建入口。

空项目时应告诉作者可以从天意聊天、导入来源或创建第一个对象开始，不能只留下大面积空白和零计数。

### 资料类型

核心类型包括：

- 角色；
- 物品；
- 地点；
- 组织；
- 关系；
- 自定义 Agent；
- 来源；
- 规则与设定；
- 世界观与背景；
- 时间系统；
- 写作风格；
- 剧情想法；
- 灵感；
- 创意。

这些内容不必全部成为资料首页的永久大按钮。它们可以由左侧工程目录、资料首页分区、搜索、标签和独立页面共同承载。

### 创建与编辑

每种资料支持：

- 简单创建；
- AI 引导创建；
- 从来源提取；
- 从节点生成；
- 模板；
- 高级档案；
- 关系与反向引用；
- 历史版本；
- 来源与可信度；
- 批量编辑；
- 查重与合并；
- 未归类和已归类；
- 影响检查。

资料编辑不应默认要求作者填完复杂表单。首先建立最小可用对象，再由 AI 和后续事件逐步丰富。

### 关系

关系是资料空间的重要组成部分。

关系需要支持：

- 来源与目标；
- 关系类型；
- 方向；
- 有效时间；
- 状态；
- 证据；
- 历史变化；
- 候选与已确认；
- 同一对 Agent 的多条不同关系；
- 关系图谱；
- 通过事件解释关系为何发生变化。

关系图谱不应成为另一套事实库，而是统一关系数据的可视投影。

### 资料空间的 AI 管理

右侧天意 Agent 可以：

- 批量补全；
- 查重；
- 建议分类；
- 从来源提取；
- 查找缺失字段；
- 检查冲突；
- 生成关系候选；
- 解释来源；
- 批量重命名；
- 把未归类内容整理到适当类型；
- 查找长篇作品中遗漏的 Agent；
- 生成资料质量和丰富度报告。

## 7. 创作

创作空间负责把已经形成的故事成果交给合适的外部工具继续制作。

它回答：

> 我要把哪一份故事成果，以什么方向，交给哪个工具，最后得到了什么产物？

天衍不要求用户自己研究怎样拼接 GitHub 项目。天衍负责研究、审查、适配并把合适的 GitHub 项目做成可选择安装的插件。用户像选择 Codex 插件一样安装并使用。

### 创作流程

完整流程是：

1. 从明确的故事来源、分支和版本中，选择作者允许交付的单元、事件范围或其他故事成果。
2. 形成中立故事包；
3. 选择输出方向；
4. 选择已安装或可安装的受信任插件；
5. 预览将交付的内容、范围和格式；
6. 作者确认；
7. 外部工具执行；
8. 保存输出产物和回执；
9. 如需回写，通过导入、对照和确认进入天衍。

### 输出方向

可以包括：

- 小说；
- 剧本；
- 漫画；
- 漫剧；
- 视觉小说；
- 互动叙事；
- 翻译与改编；
- Markdown / 文档；
- 音频或其他由插件提供的方向。

### 中立故事包

中立故事包应包含：

- 故事范围；
- 结构；
- 人物与 Agent；
- 地点与物品；
- 事件和顺序；
- 对白与描写；
- 关系；
- 世界规则；
- 写作风格；
- 来源锚点；
- 版本信息；
- 作者明确允许交付的内容。

插件只接收作者确认的故事包，不直接读取整个项目数据库，也不能自动修改正式故事。

### 创作首页不能大面积空白

创作首页可以显示：

- 可交付的故事包；
- 输出方向；
- 已安装插件；
- 推荐插件；
- 最近输出；
- 正在执行的任务；
- 失败和可重试任务；
- 输出产物；
- 来源副本与同步状态；
- 下一步操作。

当尚无可交付故事时，应明确引导作者去天意、事件线或女娲形成故事成果。

### 创作中的天意 Agent

根据输出方向，右侧天意 Agent 提供不同能力：

- 检查故事包是否完整；
- 解释插件需要的输入；
- 建议缺失资料；
- 预览格式转换；
- 检查人物、场景、对白和媒介要求；
- 解释失败；
- 比较不同插件；
- 整理外部返回结果；
- 发起回写审查。

## 8. 数据

数据空间负责把同一故事内核的只读投影、运行状态、目录成熟度、操作日志和可解释性信息集中呈现。

它不创建第二份 Canon、Event、WorldState、会话或作者确认数据。所有展示必须注明来源 owner、投影时间和权威状态；R0 仅定义其导航与静态外壳，不接入领域数据。

---

# 九、设置与个人中心

## 设置

设置应是独立页面，而不是把复杂系统管理全部塞进一个大弹窗。

弹窗只适合短确认、小选择和危险操作确认。长期配置、插件管理、模型管理和存储管理需要独立页面、清楚路由和可返回的位置。

设置可以包括：

- AI Provider；
- 模型；
- 语音；
- Tianyi 行为；
- Agent 默认权限；
- 后台任务；
- 上下文与记忆；
- Skills；
- Workflows；
- 插件；
- 插件权限；
- 外观与主题；
- 字体；
- 编辑区域；
- 左侧工程目录；
- 快捷键；
- 当前项目位置；
- 存储；
- 备份；
- 导入与导出；
- 同步；
- 隐私与安全；
- 诊断；
- 版本与恢复。

插件中心固定属于“设置 → 插件”。

创作空间可以提供“选择外部工具”入口，但插件的安装、更新、停用、卸载、权限和安全状态都在设置中管理。

## 个人中心

个人中心管理：

- 账号与身份；
- 设备；
- 云同步；
- 订阅与额度；
- 团队与协作身份；
- 隐私；
- 通知；
- 个人偏好；
- 已授权服务。

个人中心和设置是全局能力，不应混入某个故事空间的资料目录。

---

# 十、天意 Agent 模式

天意 Agent 模式只位于支持专属能力页面的侧边天意，是“当前页面专属的天意执行能力”。它是绑定当前页面、对象、工具、来源、`BaseVersion`、作用范围和权限的一次 `Page-scoped Run`，不是与 Creative / Work 并列的会话 Owner。

Agent 模式不拥有独立 Session、Canon、Event、Relation、WorldState、版本或作者确认权。它读取 `TianyiConversation` 与当前 `Work lane` 被授权的来源和记忆，在不同页面加载不同工具、对象和操作模板，并把结构化 Candidate、ChangeSet、运行回执和可展示说明返回同一 `Work lane`。Agent Run 的执行状态必须与故事领域状态分开；切换表面不会把运行节点、计划步骤或工具状态写入故事脊柱。

Agent 的自由摘要和回执说明只用于解释与定位，不会自动进入下一次采纳的 ContextPack、Canon 或长期事实记忆。需要继续使用时，作者必须显式引用对应结构化候选、来源或回执。任何采纳影响说明都从结构化故事语义 diff 派生，不能从自由摘要复制。

Agent Run 回执必须记录 `BaseVersion`、目标、范围、对象、来源与安全的工具参数快照；密钥、令牌和敏感凭据禁止进入快照，大型输入只保留不可变引用与摘要校验。

## 世界中的 Agent 模式

- 统计写作和世界数据；
- 解释日志；
- 查看当前进度；
- 分析开放问题；
- 汇总待确认内容；
- 建议下一步；
- 查询当前版本与派生副本状态。

## 天意中的 Agent 模式

- 管理会话；
- 整理上下文；
- 清理重复内容；
- 合并候选；
- 控制附件和来源；
- 与指定 Agent 交流；
- 管理长任务。

## 事件线中的 Agent 模式

- 操作节点、集点和单元；
- 分析因果；
- 增补剧情和描写；
- 预测；
- 检测漏洞；
- 对照副本；
- 解释角色选择；
- 预览影响。

## 女娲中的 Agent 模式

- 给女娲导演下指令；
- 与某个故事 Agent 对话；
- 修改运行目标；
- 调整约束；
- 暂停、恢复或分支；
- 要求解释；
- 查看预算；
- 选择同步和对照范围。

## 多元中的 Agent 模式

根据翻译、视角、IF 或本土化页面提供：

- 术语对齐；
- 角色知识检查；
- 前提变化影响；
- 文化差异解释；
- 同步冲突处理；
- 派生副本对照。

## 资料中的 Agent 模式

- 批量创建和整理；
- 提取；
- 分类；
- 查重；
- 关系建议；
- 档案补全；
- 来源解释；
- 冲突检测；
- 地点拓扑和物品持有人检查。

## 创作中的 Agent 模式

- 故事包检查；
- 插件选择；
- 格式适配；
- 输出预览；
- 失败诊断；
- 外部产物回写审查。

---

# 十一、上下文、记忆与知识边界

## 上下文是什么

上下文不是左侧侧边栏。

上下文是某一次 AI 操作被允许看到和引用的内容范围，例如：

- 当前页面；
- 当前选择的节点；
- 当前单元；
- 相关 Agent；
- 关系邻域；
- 最近事件；
- 作者拖入的资料；
- 上传来源；
- 当前派生副本；
- 开放伏笔；
- 写作风格；
- 作者明确固定或排除的内容。

左侧侧边栏是工程目录。右侧天意 Agent 是操作界面。上下文是 AI 的参考范围，三者不能混为一个概念。

## 上下文清单

每个重要 AI 操作都应可以查看：

- 这次使用了什么；
- 为什么使用；
- 来源版本；
- 哪些内容被排除；
- 是否存在过期内容；
- token 或预算限制；
- 是否越过了角色知识边界；
- 输出影响哪些对象。

作者可以增加、移除、固定或禁止某些上下文。

自由摘要、解释性回执和模型推理文本即使显示在会话中，也不因此成为权威上下文或长期事实记忆。采纳所需的影响与范围必须从结构化候选和故事语义 diff 重建；自然语言说明应逐项链接到对应来源、对象和变化。

## 分支感知的注意力隔离

每次重要 AI 操作的注意力范围，必须同时识别：

- 当前故事或事件来源；
- 当前分支与版本；
- 选中的单元、事件、Agent 或其他筛选范围；
- 本次启用的候选、预测或女娲结果；
- 作者允许的目标写入位置；
- 明确排除的其他分支、版本和结果。

IF、翻译、改编、不同视角、女娲候选和主故事事实不得静默串线。跨来源内容只有在被明确引用时才可作为只读参考；它不因为被模型看到，就变成当前分支的事实。

来源版本、筛选范围、启用结果或目标写入位置发生变化时，已有上下文回执应被标记为过期并重新校验。作者应能看到本次包含了什么、排除了什么以及原因。

## 角色记忆

角色记忆不是简单的三个大文本框。

完整系统可以同时表达：

- 角色核心；
- 长期记忆；
- 短期记忆；
- 最近记忆；
- 确认经历；
- 关系记忆；
- 知识；
- 信念；
- 怀疑；
- 误解；
- 被遗忘或被压抑的信息；
- 当前注意力；
- 某个派生副本中的临时记忆。

记忆需要来源和时间。主故事记忆、派生副本记忆、女娲 Run 或待合并结果中的临时记忆不能互相污染。

## 知识传播

一个事件发生，不代表所有角色都知道。

系统需要记录：

- 谁亲历；
- 谁听说；
- 谁从谁那里得知；
- 信息是否完整；
- 是否被欺骗；
- 是否误解；
- 何时知道；
- 是否相信；
- 是否改变了后续选择。

这是角色自然行动、视角切换和女娲推演的基础。

---

# 十二、剧情想法、灵感与创意

这三者相关，但不是同一种内容，不能简单合并成一个“灵感箱”后失去区别。

## 剧情想法

与剧情发展有明确关系。

例如：

- 希望第三十话到第六十话之间出现某个桥段；
- 想改变过去某段剧情；
- 某个角色未来要完成一次背叛；
- 某条暗线要在特定范围回收。

剧情想法可以关联时间范围、单元、节点、角色、故事线和目标。

## 灵感

作者有一个火花，但暂时不知道放在哪里、何时实现或是否属于当前故事。

灵感可以保持未归类，之后由 AI 建议关联到剧情想法、角色、地点、规则或新项目。

## 创意

范围更宽。

它可以是：

- 内容创意；
- 情感创意；
- 形式创意；
- 世界机制；
- 叙事手法；
- 视觉或声音感觉；
- 某种特殊表达；
- 不一定最终落实为剧情的点子。

## 三者之间的流动

三者可以在一个“创意”父分组中共同管理，但必须保留类型。

它们可以相互转换：

- 灵感被补充后变成剧情想法；
- 创意被落实到某个单元后变成剧情想法；
- 剧情想法被作者采用后生成候选节点；
- 已使用内容仍保留原始来源与历史。

AI 可以建议转换和关联，作者决定。

---

# 十三、左侧工程目录

左侧侧边栏的核心意义，是让作者在整个作品中稳定浏览和引用内容。

它不是每个页面各做一套完全不同的侧边栏，也不是所谓“上下文侧栏”。

工程目录可以按“已分类 / 未归类”组织浏览，也可以提供“待确认”收件箱，但两者不能成为同一组互斥状态或互相替代。分类状态只说明对象是否已经放入稳定目录；权威确认状态说明内容是候选、待确认还是已确认。一个对象可以“已分类且待确认”，也可以“未归类但已确认”。工程目录只拥有导航、组织、排序和稳定引用，不拥有 Canon、Event、WorldState 或故事正文。

完整目录可以包括：

## 资料

- 角色；
- 物品；
- 地点；
- 组织；
- 关系；
- 自定义类型；
- 未归类。

## 剧情

- 节点；
- 集点；
- 单元；
- 主线；
- 支线；
- 暗线；
- 其他分支或副本入口。

## 设定

- 时间；
- 世界观；
- 规则与设定；
- 背景。

## 写作

- 写作风格；
- 语言与术语；
- 叙事偏好。

## 创意

- 剧情想法；
- 灵感；
- 创意。

## 来源

来源文档、图片、音频和导入记录需要稳定入口。

“来源”最终是独立分组，还是放在资料分组下，由创始人确认；但它不能在完整产品中消失。

## 目录的页面聚焦

工程目录的数据与结构统一，但不同空间可以自动展开最相关的分组：

- 世界默认收起或显示摘要；
- 天意方便拖入所有类型；
- 事件线聚焦剧情结构；
- 多元聚焦来源版本与副本；
- 女娲聚焦来源范围、Agent、Run、待合并结果和副本；
- 资料聚焦资料与设定；
- 创作聚焦可交付故事包和来源。

自动聚焦不能删除其他入口，也不能形成第二套数据。

## 首页入口

空间标题本身可以返回该空间首页。

例如点击左侧上方“资料”或“资料库”即可回到资料首页，不需要长期占用一个“资料库首页”导航项。

## 桌面与移动

桌面端目录可调整宽度、折叠和固定。

移动端目录必须是明确的抽屉或全屏层，关闭后不占据页面宽度、不透明遮挡主内容，也不保留一列看不见的空宿主。

---

# 十四、副本与版本管理

天衍需要一个专门管理所有故事版本和副本的功能。

最终名称与固定入口位置待创始人确认，可以暂称“副本管理”。

它需要管理：

- 原始主线；
- 女娲副本；
- 翻译副本；
- 视角副本；
- IF 副本；
- 本土化/改编副本；
- 用户手动复制的副本；
- 临时分支；
- 旧版本；
- 单元版本；
- 同步状态；
- 冲突；
- 融入记录；
- 删除与恢复。

## 底层版本思想

天衍适合采用类似 Git 的版本思想：

- 每次重要变化有稳定版本；
- 历史可追溯；
- 可以比较；
- 可以恢复；
- 可以分出副本和分支；
- 可以把选择的变化融入目标版本。

但用户界面不需要强迫普通作者理解 commit、rebase、cherry-pick 等代码术语。产品应使用“版本、同步、对照、融入、恢复”等作者容易理解的语言。

底层可以内嵌版本库，也可以支持把作品作为可移植工程导出；无论技术实现如何，故事语义不能只依赖 Git 文件差异。

## 历史保留

旧版本默认保留。除非作者明确永久删除，否则覆盖、同步和融入都不能抹掉历史。

每次正式采纳、融入、恢复或撤销都建立不可混淆的新版本。撤销通过补偿版本反向表达被撤销的变化，不删除原采纳版本；恢复通过以历史状态为来源创建新的当前版本完成，不把版本指针静默倒退。版本历史必须能够从新版本追溯到作者决定、结构化差异、来源和回执。

候选历史、保留可能性和 IF 与历史版本不同：候选历史是可恢复的未决工作记录；保留可能性是跨会话持久但不演化的只读方向；IF 是由作者显式升级后建立的派生副本，拥有自己的正式内容和独立版本线。不能为了保留未选方向而给每个 AI 候选自动建立分支。

同一目标故事在 R0 同一时刻只有一个正式写入者。并行分析和候选生成不受此限制；正式采纳必须串行校验 `BaseVersion`。过期、范围变化或新冲突会拒绝提交，而不是覆盖其他版本或产生不可解释的兄弟正式版本。

删除应区分：

- 从当前视图移除；
- 归档；
- 放入可恢复区域；
- 永久删除。

永久删除需要清楚说明影响。

## 对照 UI

副本对照是天衍的核心产品能力。

推荐的主视图是事件线叠加：

- 原始内容稳定显示；
- 副本新增内容以另一种状态显示；
- 修改内容并排或重叠显示；
- 删除内容保留轮廓；
- 冲突突出；
- 未同步内容有标记；
- 点击任意差异可查看 Agent、关系、记忆和世界状态的影响。

还可以提供：

- 左右分屏；
- 变更列表；
- 单元摘要；
- Agent 差异；
- 关系图差异；
- 地图差异；
- 时间差异。

所有对照最终都应落到结构化故事语义 diff；自然语言总结帮助作者理解，但不是采纳、撤销、恢复或融入的权威变化集合。

---

# 十五、长篇导入与理解

天衍必须能处理“已有大量内容”的作者，而不只服务空白项目。

以导入《红楼梦》前七十回这样的长篇内容为例，理想流程是：

## 1. 保留来源

完整保留原文、章节、段落位置和版本，不让摘要覆盖原文。

## 2. 分段与结构识别

识别：

- 章节；
- 段落；
- 可能的节点；
- 集点；
- 单元；
- 叙事顺序；
- 世界时间候选。

## 3. Agent 提取

提取并建立候选：

- 角色；
- 物品；
- 地点；
- 组织；
- 美食；
- 坐骑；
- 家具；
- 服饰；
- 规则；
- 自定义分类。

## 4. 关系与状态

分析：

- 角色关系；
- 物品持有人变化；
- 地点移动；
- 信息传播；
- 身份变化；
- 组织关系；
- 角色知识和误解。

## 5. 事件线

提炼：

- 主要事件；
- 前因后果；
- 主线；
- 支线；
- 暗线；
- 伏笔；
- 回收；
- 未解决问题；
- 章节与故事线对应关系。

## 6. 描写素材

尽可能保留并标注：

- 对话；
- 心理描写；
- 人物刻画；
- 风景与环境描写；
- 动作描写；
- 情感变化；
- 特殊语言和称谓。

这些内容既可以属于节点，也可以作为可引用的写作素材存在；不能在“精炼”过程中只剩事件梗概。

## 7. 写作风格

分析：

- 叙述视角；
- 句法和节奏；
- 用词；
- 对话习惯；
- 人物描写方式；
- 环境描写方式；
- 情感表达；
- 章回结构；
- 讽刺、象征和留白；
- 不同角色的语言差异。

风格分析需要引用来源片段和范围，避免只有抽象标签。

## 8. 作者审查

AI 不能假装一次分析就完全正确。

系统应让作者按类型、章节、单元或置信度审查、合并、修正和确认。批量操作不能破坏来源追溯。

## 9. 继续创作

续写首先从故事节点和可能走向开始，而不是直接生成整章成文。

作者可以：

- 选择 AI 给出的走向；
- 修改走向；
- 提出自己的想法；
- 送入女娲深入推演；
- 确认节点后再交给创作插件形成小说、剧本或其他内容。

这样才能在尽量保持原作逻辑与风格的同时，避免只做表面模仿。

---

# 十六、外部插件与 GitHub 项目

天衍应主动研究和接入合适的 GitHub 项目，而不是让消费者自己想办法。

完整插件体系包括：

- 天衍维护的受信任插件目录；
- 对上游 GitHub 仓库、版本和许可证的记录；
- 天衍适配层；
- 插件包；
- 一键安装；
- 更新；
- 停用；
- 卸载；
- 权限说明；
- 安全隔离；
- 兼容性测试；
- 失败回滚；
- 输出保留；
- 来源和构建证明。

## 插件原则

- 只允许受信任目录中的插件进入普通用户安装流程；
- 不能允许粘贴任意 GitHub 地址后直接执行代码；
- 插件只能获得声明并由作者确认的能力；
- 插件接收故事包，而不是整个项目数据库；
- 插件不能自动写入正式故事；
- 卸载插件不能删除已有故事包和输出产物；
- 更换插件不需要迁移天衍的核心故事数据；
- 插件执行必须在真实能力隔离中运行；
- 安装、更新和执行前需要重新验证完整性；
- 失败、超时、取消和不可用都需要明确状态与回执。

## 适配方向

可研究和适配：

- 小说与长文工具；
- Fountain 和其他剧本工具；
- Ink、Twine 等互动叙事工具；
- 漫画工作流；
- 分镜工具；
- 漫剧和视频工作流；
- 翻译与排版工具；
- 地图与图谱工具；
- 音频工具；
- 其他符合故事包合同的创作工具。

Pi Agent 是当前优先采用、但必须可替换的 AI Agent 运行底座。Codex、OpenCode 或未来其他 Agent 框架也可以作为底层能力或插件运行底座，但都不能反过来定义天衍产品。天衍的故事语义、权限、记忆和作者体验必须独立于某一家模型或框架。

---

# 十七、UI 与交互设计

天衍功能很多，但默认界面必须清楚、克制和好看。

“功能完整”不等于把所有功能同时摆在第一屏；“页面简单”也不等于大面积空白。

## 1. 总体布局

桌面端可以由以下稳定区域组成：

- 最左侧八空间全局导航；
- 左侧工程目录；
- 顶部作品、版本和全局状态；
- 中央主工作区；
- 页面对象或事件详情；
- 最右侧天意 Agent。

R0.2 的权威横向顺序是：全局空间轨、工程目录、中央页面、可组合页面工具面板栈、紧凑工具轨、天意。工程日志是工具轨首项；多个页面工具可以同时开启并上下拼接，工程日志只投影连续操作回执，专家分析只给出带依据且可采纳或忽略的建议。

天意大页面提供“创意模式 / 工作模式”，侧边天意提供“工作模式 / Agent 模式”；Agent 模式只在当前页面支持专属能力时出现。它们共同归属于一个 `TianyiConversation`：Creative / Work 两条泳道共享统一可见且可追溯的历史、来源、附件、显式引用和候选注册表，但各自保留草稿、范围、上下文装配和工作进度。模式切换不清空任何泳道。

大页面与侧边栏的工作模式是同一 `Work lane` 的不同表面，不建立第二套工作会话或第二个 `messages` / `sessionId` Owner。工作消息、目标、`BaseVersion`、来源、候选、审查状态、Run 索引与回执共享；未发送草稿、光标、滚动位置、页面选择、临时筛选、工具输入态和布局按表面隔离。

Agent 模式承载 Page-scoped Run 的任务、作用范围、推演方式、执行过程、候选审阅与回执，但不拥有独立 Session ID 或独立长期消息流。Run 通过稳定引用挂接同一 `Work lane`；切换到工作模式不会取消 Run，Run 的计划、工具状态和执行节点也不会混入故事领域状态。自然语言摘要只作为说明，结构化候选和 diff 才能进入采纳链。

Agent 模式中的“查看执行过程”临时把中央工作区切换为独立的 Agent 执行过程图，顶部必须明确标识并提供“返回事件图”。完整执行图不塞入 348px 天意 Dock，也不与事件图并排挤压；返回后候选仍作为事件图上的独立预览叠加层。事件图、候选叠加层和 Agent 执行图具有不同节点身份、数据来源与操作权限，任何 Agent 执行节点都不能进入故事脊柱、时间轴或采纳链。

不同宽度下应主动减少并排栏：

- 宽屏可以同时显示目录、主区、详情和天意；
- 中等宽度在详情与天意之间切换；
- 小屏使用抽屉、全屏页或底部 Sheet；
- 任何隐藏栏关闭后都不能继续占据宽度或遮挡内容。

## 2. 八空间导航

世界、天意、事件线、多元、女娲、资料、创作、数据保持稳定顺序和一致选中状态。

设置与个人中心放在全局位置。

桌面端不能出现“当前空间已选中，但更多菜单也同时选中”的重复状态。移动端底部导航与更多菜单同样只能有一个清楚的当前空间。

## 3. 页面首页

每个空间都应有真正的首页，而不是把所有子功能堆成一页。

首页应回答：

- 这里是做什么的；
- 当前有什么；
- 最重要的问题是什么；
- 下一步做什么；
- 最近从哪里继续。

点击空间名称返回首页，避免重复的“某某首页”永久导航项。

## 4. 一个主要动作

每个页面状态应有一个清楚的主要动作。

其他能力可以作为次要按钮、菜单、右侧 Agent 模板或渐进展开内容。多个橙色主按钮同时争抢注意力会让用户不知道该从哪里开始。

## 5. 渐进披露

复杂角色档案、节点详情、插件技术信息和女娲导演设置都应按层次展开：

- 首先显示作者当前需要理解的内容；
- 然后显示常用编辑；
- 最后显示高级字段、技术来源和完整历史。

信息不能因为“高级”就被隐藏到找不到；展开路径必须稳定。

## 6. 语义状态视觉

全产品应统一表达：

- 已确认；
- 候选；
- 预测；
- 派生副本；
- 未归类；
- 已过期；
- 冲突；
- 未知；
- 已归档；
- 外部输出。

颜色不是唯一表达方式。需要同时使用标签、边框、线型、图标和文字：

- 已确认内容稳定、实线；
- 候选内容可编辑、有明确候选标签；
- 预测节点使用虚线或半透明；
- 派生副本显示来源；
- 冲突有警告和处理入口；
- 未知明确写“未知”，不显示假数据。

## 7. 画布巧思

事件线、关系图和地图可以共享一套空间交互语言：

- 缩放；
- 适应视图；
- 聚焦当前；
- 缩略图；
- 折叠；
- 搜索；
- 筛选；
- 选择连续片段；
- 复制稳定 ID；
- 拖入天意；
- 预测幽灵节点；
- 对照叠加；
- 差异高亮；
- 影响范围预览。

这些画布看起来可以不同，但操作语法应一致。

## 8. 拖拽与引用

作者可以把工程目录中的对象、事件线节点、来源或副本拖入天意、女娲、创作和其他页面。

拖入后显示引用卡片，包含名称、类型、版本和范围。

所有拖拽操作都必须有点击式“添加引用”替代，支持键盘和触控。

## 9. 空状态

空状态必须说明：

- 为什么为空；
- 什么内容会出现在这里；
- 用户可以从哪里开始；
- AI 可以怎样帮助。

不能只显示一个图标和“0 项”，也不能为了填满页面而制造无意义卡片。

## 10. 视觉统一

八个空间需要共享：

- 字体层级；
- 间距；
- 卡片半径；
- 边框；
- 按钮；
- 标签；
- 输入框；
- 空状态；
- 抽屉；
- 对话；
- 详情面板；
- 状态颜色；
- 加载、错误和断连提示。

空间可以有自己的主题图标和工作气质，但不能像七个不同团队做出的产品。

## 11. 移动端

移动端不是桌面端缩小。

R0.2 当前仅纠正桌面工作台；移动端将使用独立产品外壳和信息架构，暂不创建移动端路由、组件或业务流程。未来只与桌面端共享账号、项目数据、领域合同、API 和必要状态，不复用桌面多栏布局。

需要：

- 真实 390px 等小屏布局；
- 可触控尺寸；
- 底部导航；
- 工程目录抽屉；
- 天意 Agent 全屏页或底部 Sheet；
- 详情页的返回路径；
- 不被底部导航遮挡的主要操作；
- 无横向溢出；
- 无透明栏遮挡；
- 输入框在键盘弹出时仍可用；
- 长卡片分段呈现；
- 画布可聚焦、缩放和回到当前节点。

“横向溢出为 0”不代表页面可用，还必须人工检查内容是否被覆盖、裁切、挤压或藏在不可触达区域。

## 12. 无障碍与可理解性

- 图标需要文字或提示；
- 重要状态不能只靠颜色；
- 支持键盘；
- 支持屏幕阅读名称；
- 对话和语音有清楚状态；
- 技术术语默认翻译为作者能理解的语言；
- 高级技术信息可以展开查看；
- 错误提示要告诉用户发生了什么、数据是否安全、下一步怎么办。

---

# 十八、完整作者体验

## 体验一：只有一个想法

1. 作者进入天意创意模式，说出一句话或开始语音聊天。
2. 天意与作者交流，记录原话并提出必要问题。
3. 系统提炼角色、地点、故事问题、剧情想法和灵感候选。
4. 作者快速审查，角色和地点进入未归类或已归类资料。
5. 第一个候选节点进入候选注册表和可恢复候选历史，不因模式切换而丢失。
6. 作者可以继续在同一 `TianyiConversation` 的工作泳道中修改、保留候选、保留可能性或升级为 IF。
7. 需要正式生效时，系统先展示结构化影响，作者一次采纳后生成新版本、回执和撤销入口。
8. 世界首页开始显示当前故事状态和下一步。

## 体验二：导入已有长篇

1. 作者上传文档。
2. 系统保留原文并分章节、节点、集点和单元。
3. AI 提取 Agent、关系、事件、时间、伏笔、描写和风格。
4. 作者按章节或类型批量审查。
5. 已确认内容进入资料和事件线。
6. 系统建立开放问题和冲突清单。
7. 作者选择某个范围继续规划、预测或送入女娲。

## 体验三：事件线预测

1. 作者选择一个节点或连续片段。
2. 右侧天意 Agent 给出至少一个有依据的虚线后续。
3. 作者直接修改、组合、拒绝、保留候选、保留可能性或升级为 IF。
4. 需要正式生效时，作者选择当前明确目标，系统从结构化故事语义 diff 展示 `BaseVersion`、范围、来源、影响与冲突。
5. 作者点击一次“采纳”；版本校验通过后，唯一正式写入链原子创建新目标版本、结构化回执和撤销入口，不追加第二次确认。
6. 未选候选自动进入可恢复候选历史；在 IF 中采纳只改变该 IF。

## 体验四：女娲深入推演

1. 作者选择当前主故事、IF、预测、翻译、改编或其他明确的故事来源与版本。
2. 选定单元、事件或 Agent 范围，设置目标、限制、预算和权限。
3. 女娲在有界 Run 或作者明确保留的派生副本中推演。
4. 同一目标可以存在多次 Run 和多份待合并结果，作者可随时阅读、对话、暂停、干预或继续。
5. 选定来源变化时，系统提示同步、过期范围和冲突。
6. 作者在对照视图查看每份结果增加、修改、替换或删除了什么。
7. 作者选择节点、片段、单元、Agent 或完整副本，在明确目标与权限下查看结构化故事语义 diff，并一次决定是否融入。
8. 系统串行校验目标 `BaseVersion`，建立可撤销、可回退、可溯源的新目标版本，旧版本和来源副本仍保留。

## 体验五：多元派生

1. 作者选择一个来源版本。
2. 选择翻译、视角、IF、本土化或改编等派生目的。
3. 建立派生副本。
4. 来源变化时由作者手动拉取更新、查看差异并决定如何处理冲突，派生副本默认不反向覆盖来源。
5. 需要时把结果交给创作插件输出。

## 体验六：外部创作

1. 作者选择已形成的故事成果。
2. 天衍生成中立故事包。
3. 作者选择小说、剧本、漫画、漫剧、互动叙事等方向。
4. 从受信任插件目录安装或选择工具。
5. 预览交付内容并确认。
6. 外部工具运行。
7. 输出产物和回执进入创作空间。
8. 如需回写，经过导入、对照和确认。

---

# 十九、完整产品的质量要求

## 故事一致性

- 同一 Agent 只有稳定身份；
- 名称修改能影响所有引用；
- 关系、持有人、地点、年龄和记忆随事件与时间正确变化；
- 原始主线与派生副本不互相污染；
- 角色不能越过知识边界；
- 时间未知时不伪造；
- 事件相邻不自动等于因果；
- 所有重要结论可追溯到来源或作者决定。
- 分类状态、所属范围和权威确认状态保持正交，不以“已分类”冒充“已确认”。

## AI 可解释性

- 能说明使用了什么上下文；
- 能说明为什么提出某个候选；
- 能说明哪些部分是推断；
- 能说明修改影响；
- 能说明为什么需要新 Agent；
- 能说明同步和对照结果；
- 能说明外部工具收到什么。
- 采纳说明逐项追溯到结构化故事语义 diff，不以 Agent 自由摘要替代权威差异。

## 可恢复性

- 重要操作有版本；
- 批量操作可撤销；
- 派生副本可恢复；
- 同步不会抹掉历史；
- 外部插件失败不破坏故事数据；
- 删除有明确层级；
- 崩溃后任务和回执可恢复。
- 未选候选进入可恢复候选历史，R0 不静默永久删除；保留可能性与 IF 具有清楚的升级边界。
- 撤销建立补偿版本，恢复建立新的当前版本，二者都不删除历史。

## 长篇能力

- 能处理大量来源；
- 分段、索引和上下文按需加载；
- 不需要把整部作品一次塞入模型；
- 记忆和来源可定位；
- 长任务可暂停、继续和重放；
- AI 分析结果可分批审查；
- 成本、时间和进度可见。

## 安全与隐私

- 作者决定 AI 和插件可读取的范围；
- 密钥与作品数据分离；
- 插件权限最小化；
- 外部调用显式；
- 敏感来源可设为本地；
- 日志不泄露正文和密钥；
- 安装包与更新可验证；
- 后台任务可停止。
- 回执和工具参数快照不包含密钥、令牌或敏感凭据；大型输入只保留不可变引用与摘要校验。

## 体验质量

- 八空间职责清楚；
- UI 统一；
- 页面不靠大面积空白假装简洁；
- 页面也不把所有能力一次堆满；
- 一个状态一个主要动作；
- 桌面、1024 和真实 390 都可用；
- 不依赖某个模型或框架才能理解产品；
- 即使 AI 能力较弱，也不会越过正式故事边界。

---

# 二十、未来 Agent 阅读本文后必须理解的十件事

1. 天衍是故事世界创造与演化系统，不是普通 AI 写作器。
2. 作者控制宏观方向和最终决定；正式采纳只决定一次，通过结构化故事语义 diff、串行版本校验、回执和补偿版本保证安全与可恢复。
3. 世界、天意、事件线、多元、女娲、资料、创作、数据是同一故事内核的八个空间。
4. 角色、物品、地点、组织和自定义对象都可以是 Agent，但不等于都常驻运行模型。
5. 事件线空间是统一管理与观察入口；独立故事分支、待合并变化、历史版本和筛选视图必须分清，筛选不会自动生成事件线。
6. 事件线预测是局部、直接、作者主导的候选；未选结果进入可恢复候选历史，保留可能性后跨会话持久，只有显式升级为 IF 才建立派生副本和独立版本线。
7. 多元管理派生关系、来源、同步、差异与冲突，但不是第二个 Event、WorldState 或 Canon 所有者，派生结果也不得默认反向覆盖来源。
8. AI 新生成的 Agent 默认进入未归类，并且要区分它属于主故事、某个分支、女娲 Run 还是待合并结果。
9. 一个 `TianyiConversation` 包含 Creative / Work 两条泳道；大页和侧栏共享同一 Work lane，页面 Agent 只是无独立 Session 或事实权力的 Page-scoped Run。
10. 左侧工程目录、侧边天意、AI 上下文和 Agent 执行状态不是同一个东西；Pi Agent 只是可替换运行底座，它们都不能绕过天衍的领域边界、唯一 Owner 和作者采纳。

如果一个设计或实现违反其中任何一项，就需要回到产品核心重新讨论，而不是用局部页面方便为理由直接改变产品。

---

# 二十一、创始人待确认项

这些问题不阻断本文作为整体草案阅读，但在成为最终核心文档前需要确认。

## 1. “派生副本”的最终产品名称

当前用“派生副本”表达从某个明确故事来源与版本完整派生、可以独立演化并继续同步的版本。

可选方向包括：

- 派生副本；
- 派生时间线；
- 故事副本；
- 其他创始人更自然的名称。

概念已经明确，名称尚未冻结。

## 2. 副本管理的正式名称与入口

它需要管理原始主线、女娲、多元、临时分支、版本、同步、对照和融入。

待确认它主要从：

- 世界首页；
- 事件线顶部；
- 全局作品/版本选择器；
- 或一个独立入口进入。

它可以有多个快捷入口，但应有一个统一管理页。

## 3. “来源”在左侧工程目录中的位置

来源是完整产品不可缺少的能力。

待确认它是：

- 左侧独立一级分组；
- 资料分组中的入口；
- 或由资料和天意共同进入的统一来源页。

## 4. 翻译副本的默认同步粒度

翻译已经确认应作为可独立维护和同步的副本。

待确认默认以节点、集点、单元还是文档段落为主要同步与审查粒度。系统最终可以支持多种粒度，但需要一个默认体验。

## 5. 剧情想法、灵感与创意的默认首页

三种内容需要保持区别。

待确认它们共用一个“创意”管理首页，还是各自有独立首页但共享搜索、标签和转换能力。

## 6. “待合并变化”与“叠层”的最终命名

概念边界已经明确：它是尚未写入目标故事的可审查变化，不是独立事件线、历史版本或筛选视图。

“叠层”只是当前的展示与处理候选词，是否作为最终用户可见名称尚未确认。

## 7. 多元的最终信息架构与分支模型

多元的稳定职责已确认，但页面数量、首页组织，以及忠实翻译与改编式翻译是否共用底层分支模型，仍待研究和创始人确认。临时预测、候选或保留可能性只有在作者显式执行“升级为 IF”时才建立派生副本和独立版本线；该门槛已经确认。

## 8. 角色命运 K 线的固定入口

可编辑、多维、默认综合展示、支持多轨迹对照和可解释关联事件的产品行为已确认。它在角色档案、事件线空间或其他页面中的最终固定入口尚未确认。

---

# 结语

天衍真正的产品力，不是拥有最多的 AI 按钮，也不是一次生成最长的正文。

它的产品力来自：

- 能听懂作者还没有整理好的想法；
- 能记住一个庞大故事世界的长期变化；
- 能让角色依据自己的知识和记忆行动；
- 能把事件、时间、关系、地点和世界状态保持一致；
- 能让 AI 在派生副本中大胆推演，又不破坏作者的原始主线；
- 能让作者直观看到 AI 到底增加和改变了什么；
- 能让作者只取自己认可的部分；
- 能把同一个故事世界安全地交给不同外部工具继续制作；
- 最终让没有成熟文笔、但拥有真正想法的人，也能创造出复杂、连贯、有生命的故事。

这就是天衍。
