# 天衍日常入口

> 作者日常使用只看本文件。工程定位见 `项目目录导航.md`，产品定义见 `TIANYAN_PRODUCT_CORE.md`。

## Codex 恢复顺序

1. `AGENTS.md`
2. `TIANYAN_PRODUCT_CORE.md`
3. `docs/product/TIANYAN_PRODUCT_SEMANTIC_CORE_R1.md`
4. `docs/product/TIANYAN_BOUNDARY_MODEL_R1.md`
5. `docs/handoff/TIANYAN_ENGINEERING_GOVERNANCE_ALIGNMENT_R1.md`
6. `项目目录导航.md`

三份 R1 冻结文档已纳入工程日常恢复入口：先恢复产品语义和边界，再读工程准入对照，最后用目录导航定位真实代码。它们不取代 `TIANYAN_PRODUCT_CORE.md`，也不代表文档中的缺口已实现。

## 日常打开位置

- 唯一日常目录：`/home/beelink/Documents/Codex/worlding.world-天衍`
- 日常分支：`codex/world-materials`
- 访问地址：`http://127.0.0.1:4192/`

## 版本说明（避免写过期提交号）

- 整合来源：开发分支 `codex/map-real-ai-collaboration-r1` 的 `21050f4886e319d6ec38c9d3090fd7944b9bcf1d`（含 PR #24/#25 全部成果），2026-09-15 快进合入；整合前位置见标签 `checkpoint/world-materials-pre-integration-20260915`。
- **开发代码与日常运行版本暂不一致（2026-09-15 第三轮后）**：主分支代码含常规创作链、确认后一致性修复等新提交（以 `git log --oneline -8` 为准），而日常服务仍运行 `f95fb7c` 构建的服务端+前端（未重启，健康回执 `codeRevision=f95fb7c…`）。下一次按运行手册切换后即对齐。
- 实际运行版本：以日常服务健康回执为准——浏览器打开 `http://127.0.0.1:4192/__local/story-studio/health`，`codeRevision` 即当前运行代码。
- 当前 Git 状态：在主目录执行 `git log --oneline -3` 与 `git status` 查看；本文件不复制会过期的提交号。
- 本轮验收在隔离环境完成：41994 端口临时服务 + `/tmp` 隔离作品根，验收后已停止并清理；`apps/story-studio/dist` 已精确恢复为 f95fb7c 部署内容（内容指纹校验一致）。

## 日常服务

- 日常服务（4192）已于 2026-09-15 从主目录启动：`combined-static` 模式，作品根 `~/WorldOS`，Provider 权威配置，访问地址不变 `http://127.0.0.1:4192/`。
- 启动命令（在主目录执行）：

```bash
export PATH=/home/beelink/.cache/tianyan-runtime/node-v22.22.0-linux-x64/bin:$PATH
TIANYAN_STORY_STUDIO_RUNTIME_MODE=combined-static npm run serve
```

- 前端产物与后台服务对应同一版本：改动代码后先 `npm run build`，再重启上面的服务。
- 再次切换或回滚必须按 `docs/operations/TIANYAN_DAILY_4191_4192_UPGRADE_RUNBOOK.md`：只读预检 → 备份 → 作者确认 → 停旧启新。2026-09-15 切换前备份：`/home/beelink/Documents/Codex/tianyan-upgrade-backups/20260915-064403-pre-f95fb7c-cutover`（0700，含双向 SHA-256 清单）。

## 开发验证

```bash
cd /home/beelink/Documents/Codex/worlding.world-天衍
export PATH=/home/beelink/.cache/tianyan-runtime/node-v22.22.0-linux-x64/bin:$PATH
npm run verify       # typecheck + lint + unit + integration + e2e + build
npm run dev          # 4191 前端 + 4192 服务端（开发模式）
```

测试与 E2E 只使用 Mock、隔离 fixture 与本地伪 Provider，不触碰 `/home/beelink/WorldOS` 正式作品，不调用真实模型。

## 旧 worktree 的用途（本轮保留，暂不删除）

- `/home/beelink/.codex/worktrees/tianyan-map-m4-closeout`：分支 `codex/map-real-ai-collaboration-r1`（已推送）。自 2026-09-15 起不再承载日常服务，仅保存未纳入 Git 的验收证据：`data/2026-09-14_天衍第一阶段功能收尾/`（两段录像、截图与隔离身份文件）及 2026-09-13 各轮证据目录。证据确认迁移或归档前不可删除。
- 其余旧 worktree 与分支保持原样，未做清理。
