# 天衍日常入口

> 作者日常使用只看本文件。工程定位见 `项目目录导航.md`，产品定义见 `TIANYAN_PRODUCT_CORE.md`。

## 日常打开位置

- 唯一日常目录：`/home/beelink/Documents/Codex/worlding.world-天衍`
- 日常分支：`codex/world-materials`
- 当前 HEAD：`21050f4886e319d6ec38c9d3090fd7944b9bcf1d`（2026-09-15 由 `f0ec2a2` 快进整合，包含 PR #24 与 #25 的全部已提交成果；整合前位置见标签 `checkpoint/world-materials-pre-integration-20260915`）

## 日常服务

- 当前正在运行的日常服务（4192）仍由旧工作树 `/home/beelink/.codex/worktrees/tianyan-map-m4-closeout` 提供，健康回执 `codeRevision=54e5c86`，落后当前 HEAD 8 个提交（第一阶段收尾：指定续改、地图北向、图片检查尚未加载）。
- 切换日常服务到主目录新版本必须按 `docs/operations/TIANYAN_DAILY_4191_4192_UPGRADE_RUNBOOK.md` 执行：先只读预检、备份，再经作者集中确认后停旧启新；不得自动启停。

## 启动方法（开发验证）

```bash
cd /home/beelink/Documents/Codex/worlding.world-天衍
export PATH=/home/beelink/.cache/tianyan-runtime/node-v22.22.0-linux-x64/bin:$PATH
npm run dev          # 4191 前端 + 4192 服务端
npm run serve        # combined-static 单端口模式
npm run verify       # typecheck + lint + unit + integration + e2e + build
```

测试与 E2E 只使用 Mock、隔离 fixture 与本地伪 Provider，不触碰 `/home/beelink/WorldOS` 正式作品，不调用真实模型。

## 旧 worktree 的用途（本轮保留，暂不删除）

- `/home/beelink/.codex/worktrees/tianyan-map-m4-closeout`：分支 `codex/map-real-ai-collaboration-r1`（已推送，等于本仓库当前 HEAD 的来源）。
  - 正在运行上述日常服务（4192），停服切换前不可删除。
  - 保存未纳入 Git 的验收证据：`data/2026-09-14_天衍第一阶段功能收尾/`（两段录像、截图与隔离身份文件）及 2026-09-13 各轮证据目录。证据确认迁移或归档前不可删除。
- 其余旧 worktree 与分支保持原样，未做清理。
