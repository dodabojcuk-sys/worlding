# G1 视觉对照与设计 QA

状态：`CODEX_DESIGN_QA_PASS_WITH_P2_LIMITS`。独立工程审阅仍单列，不以本文件代替；这也不是创始人视觉或体验验收。

## 对照输入

- 视觉目标：`data/2026-09-05_天衍G1自适应任务主工作面/设计目标/`。
- 真实实现：`data/2026-09-05_天衍G1自适应任务主工作面/真实运行证据/`。
- 同视口合并对照：`data/2026-09-05_天衍G1自适应任务主工作面/对照/设计目标-真实运行-*.png`。每张左侧为设计目标，右侧为真实运行；分别保持 1440×1024 与 1195×900。
- 旧版与 G1：`data/2026-09-05_天衍G1自适应任务主工作面/对照/旧版-G1-1440-*.png`。左侧为 G0 现场，右侧为 G1 真实运行。

## 已修问题

| 级别 | 发现 | 修补与复核 |
| --- | --- | --- |
| P1 | 1195 宽度同时保留候选表、永久右栏和目录时，主表横向裁切 | 在该断点把来源/影响改为候选行内可展开信息；1195×900 重新截图后，候选、状态与底部批次动作均可达，无 body 横向溢出。 |
| P1 | 从 1440 动态缩到 1195 时，已打开目录曾继续以全屏 overlay 遮住候选审阅 | 把目录 overlay 断点统一为 76rem，并监听断点变化自动关闭；浏览器实走确认 1440 打开目录后缩至 1195，目录数由 1 变 0。 |
| P1 | 工作预览曾用硬编码“已有内容”，会把示意文案误当项目事实 | 改为读取 Story Unit Owner 返回的标题和摘要；两档工作截图均显示隔离项目的“已有主线”。 |
| P1 | 预览后服务重启会因对象字段顺序改变而误判 previewId 过期 | BaseVersion 先规范化再计算 previewId；集成测试现在覆盖“预览后重启再确认”。 |
| P1 | 刷新只恢复候选焦点，未恢复未发送草稿 | 增加项目与泳道隔离的本地草稿恢复键；浏览器填写草稿、刷新后逐字恢复。 |
| P1 | 重启后 Work 可以恢复候选引用，但“先看事件线”仍可能依赖丢失的瞬时 candidateId | 路由改用 Envelope/session 与持久候选引用回退；重启后实际点击进入带 1 个正式 Placement 的事件线。 |
| P1 | 早期 Work 的探索单选只是无处理器的视觉控件 | 改为受控选择，并提供“保留为下一轮创意草稿”；浏览器确认草稿带回 Creative，且不进入批次提交。 |
| P1 | 本地假服务在运行区曾显示“真实运行中 / Pi Runtime” | 现在明确显示“本地假服务 · 非真实 Pi”；真实 Pi 验收继续单列未完成。 |
| P1 | 目录待确认入口曾只改 URL，中央工作面仍留在 Creative | 同页导航现在显式通知已挂载工作面；浏览器按原路径复测立即进入 10 项候选 Review。 |
| P1 | Story Intake 候选曾由目录路径进入 legacy EventLine 候选槽 | 目录 Review 和全局 EventLine 入口都清理该槽；实走 `data-tianyi-candidate-active=false`，无空候选叠层。 |
| P1 | “全书位置”首次不稳定，且支线使主线过度拉宽、长标题被截断 | 改为显式全结构节点双帧 fit，支线复用竖向轨道的水平列，低缩放标题两行；1195/1440 各一次操作后 5 卡全部在边界内且标题未截断。 |
| P2 | 交叉导览长期覆盖画布，已归档补偿角色重复进入观察者 | 交叉导览默认收起且保留展开入口；观察者只投影 active 角色，当前只显示“顾澜”。 |
| P2 | 初版层层卡片、横幅和大圆角削弱连续阅读 | Review 改为稿纸式连续清单；Work 改为编排主轴加结构化影响侧栏；统一低圆角、细边框和焦点品牌色。 |

## 仍保留的 P2 差异

- 真实事件线继续复用现有可缩放 NarrativeArrangement 图形画布与任务工具条，因此比视觉目标更技术化；已修复首次 fit、标题截断和永久覆盖，没有移除已有能力进行像素匹配。
- 1195 宽度下来源证据收进当前候选的展开区，目录默认收起，结构化影响在 Work 中保留；这是为保持主工作面和底部动作可达做的响应式取舍。
- 中文界面仍夹有少量既有技术词（Owner、Story Unit、World），本轮把原始 ID 和版本长串收进诊断，但没有重写全产品词汇系统。

## 结论

G1 的 Review / Work 主结构、信息密度、焦点、边界、底部动作和两档响应式达到 Codex 设计 QA 的可验收状态。创始人仍需按交付路线独立确认长期阅读舒适度、术语和事件线密度。
